# GIFT · The back-office skill suite — built, never copied where it would run

Brought to the gate 2026-08-23, unrouted-gifts pass, whole-corpus sweep.
This card orders nothing. Mark it, or leave it — blank is a complete state.

**What it is** — Three full SKILL.md instruments at the repo root: `skills/dispatch` (the daily `_INTAKE` triage cycle — Grounder + Converger + Governor passes → `DAILY_BRIEF.md`), `skills/status` (a pure read-only four-domain map: Library/Work/Website/Substrate), and `skills/register-audit` (a runnable, scored port of `frameworks/register_audit.md`). Four siblings — `daily-cycle`, `mark-record`, `metabolizer`, `morning-pages-channel` — already made the same one-step move into `~/.claude/skills` and are live today; these three were left behind.

**Where it came from**
> "The daily triage cycle — scan _INTAKE/ for new material, run Grounder + Converger + Governor passes on each new item, write DAILY_BRIEF.md, update DISPATCHER_STATE.json."
— `skills/dispatch/SKILL.md`, built 2026-06-14 per its own header

**Latent capacity** — Three working instruments you could invoke by name today — a daily intake triage, a four-domain status map, a runnable register-drift audit — the same mechanical step already taken for their four siblings.

**Why it went unrouted** — Diffed root `skills/` against `~/.claude/skills/` directly: 4 of 12 root-skill names were promoted and are byte-identical there; `dispatch`/`status`/`register-audit` were never copied. (`converge` and `governor` were excluded — they reference files already moved to `_INTAKE/_COMPOST_2026-06-28/`, already dispositioned. `transmit` was excluded — superseded by the already-installed, more developed `~/.claude/skills/transmission-engine`.)

**Shortest routing** — Way in: copy `skills/dispatch`, `skills/status`, `skills/register-audit` into `~/.claude/skills/` — the same mechanical step already taken for their four siblings. Act: invoke by name ("status," "run the register audit"). Consequence: a live four-domain map or a scored register read the same session, no new build.

**Reading** — capacity M · routing effort L · confidence M

────────────────────────────────────────

Your mark:
