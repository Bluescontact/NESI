# CARDS — what was already standing, 2026-08-14 (session 3ac0472b)

The recognition pass ran before anything new was written. **Most of the
commission was already built.** These cards wait; nothing here orders anything.

Ranked by capacity, then by how short the route is.

---

## 1 · FIVE MECHANISMS BUILT AND WALKED, WITH NOWHERE TO SIT

`RAIN · CHANNEL · SHOAL · SOUNDING · STILLING` — in `ascent.html`, playable, and
not seats of the solid. Seventeen bodies, twelve places.

**The route is now short, and it was not before.** `solid.js` derives 24 members
and the solid carries 14 faces. Two of the five have an obvious member already
implied by what they do:

| mechanism | the member it reads like | why |
|---|---|---|
| SOUNDING | ↓GROUND—↓STATIONS (level 14) | the line reaches settled ground and routes what it finds |
| STILLING | ↑OVERWINTERING—↓TANK (level 18, a return) | the page falling quiet IS the day ending into the next intake |

`RAIN`, `CHANNEL` and `SHOAL` have no member that reads that way, and **that is a
real finding rather than a gap to fill** — the solid may simply not carry them,
which would make them faces rather than members. Filed, standing.

**Not decided, and not defaulted:** whether a level is an EDGE or a TETRA OF FOUR
FACES. Both models are live; held together they give 96 faces against 17
mechanisms. `solid.js` assumes the edge and forecloses nothing.

## 2 · THE INTERNAL OPEN LOOP IS BUILT — recognise it and price it

`daily.html:toTheLake()`. Water leaves `nesi.water` and arrives in `nesi.ascent`;
the arrival is written FIRST, so if the far store cannot take it the stone does
not leave and water is never in flight. Two stores, one member, loss at one end
and arrival at the other.

**Siting a standing thing counts fully as a build, and this one is standing.**
What it is not: priced. The transfer is irreversible and free.

## 3 · THE MERGE IS ALREADY UNDONE BY HAND

`daily.html:unmerge()`. The parts come back exactly as they were, in written
order; the merged stone leaves the reach by a stage, never by deletion; a click
on a seam does it and nothing suggests it. **Stage FOUR of the commission was
already complete before this pass began.**

## 4 · THE OTHER ELEVEN STATIONS

All 36 products derive in `solid.js` and are proven by `tools/solid_check.js`.
Only ↓TANK's three are wired to a real gesture. The other eleven are one wiring
pass away and live in `ascent.html`. → gate opened.

## 5 · TWO SESSIONS BUILT THE SAME SOLID, INDEPENDENTLY, AND AGREE

`THE_SOLID.md` and `solid.js` were written in parallel by two sessions that did
not see each other's work, from differently-rotated circuit tables, and they
generate the identical 24 members. Worth knowing because it is the strongest
form this could have been confirmed in — and worth knowing for a second reason:
**two sessions are building this tree at once.** One commit swept the other's
working files up under its own message. Nothing was lost; the member-naming
convention was.
