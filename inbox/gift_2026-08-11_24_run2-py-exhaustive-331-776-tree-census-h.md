# run2.py — exhaustive 331,776-tree census harness

**What:** Enumeration over every spanning tree of the dual via nx.SpanningTreeIterator, testing each with valid() and counting flat-unfolders and cupola-family members. run.py variant would dump all valid trees to good.json — never landed.

**Source:** `(temp) 87c5c4fc scratchpad/run2.py (+run.py) — preserved copy: skill ._work/rescued_87c5c4fc/`
**When:** 2026-08-09

**Evidence (verbatim):**
> print(f"UNFOLD FLAT (no overlap) = {good} ({100*good/n:.1f}%)") print(f" of those, cupola-family = {cup} ...")

**Capacity:** Regenerates alternate boards or the family census on demand; run.py's json.dump line is the one-flag path to a persisted library of every valid net.

**Unrouted because:** Temp scratchpad only; output never written; counts live only in a dead transcript.

**Shortest routing:** Carry along with nets.py/cupola.py as the census tool; not needed for the few-hours build itself.

**Reading:** capacity M · effort L · confidence H

---
This card orders nothing. It waits for Kevin's mark. Blank is a complete state.
