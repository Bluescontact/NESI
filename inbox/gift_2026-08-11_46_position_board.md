# GIFT · THE TETRA POSITION BOARD - holder derived, never stored

Routed to the gate 2026-08-11 by the unrouted-gifts extraction · second run: the DSS folder.
This card orders nothing. Mark it, or leave it — blank is a complete state.

**What it is** — Four fixed poles, six position-edges as regions, holder derived from edge, volume as MIN headroom, rendered to self-contained HTML with state embedded (works from file://).

**Where it came from** — nesi/nesi_bench_v0/position_board.py + .html + .json
> 'nothing here flips board_state on its own logic. state requires --source kevin or it denies. The HTML render has no state-flipping control at all -- it reads.'

**Latent capacity** — A positional read of where anything stands relative to self/other/world/time - the geometry the triangle world implies but never states.

**Why it went unrouted** — Sits in a v0 bench; nothing in the current builds links or regenerates it.

**Shortest routing** — Way in: a link from state_view.html. Act: --source kevin flips one board_state. Consequence: the board shows the region changed, holder recomputed.

**Reading** — capacity M - effort M - confidence H

────────────────────────────────────────

Your mark:
