# GIFT · osg_organ/ — the external organ of NESI, a full guest space, never hosted

Brought to the gate 2026-08-22, unrouted-gifts pass against oursharedgifts.org.
This card orders nothing. Mark it, or leave it — blank is a complete state.

**What it is** — A complete, separate Flask + SQLite application (`osg_organ/app.py` + six templates) implementing the membrane rules as running code: email-only entry with guest-set password, "delete my data now" on every page, automatic purge on logout/24h-idle, and an explicit OFFER mechanism where guest work only reaches Kevin as an inert file in `membrane_outbox/` awaiting his mark. Three preloaded worksurfaces (the circuit, the gift seed, the commons) plus a `/nexi` face presenting a full convergence run — 220 bodies read, 135 kept, 69 folded, 16 composted, 29 standing laws — and a `/floor` page readable without signing in.

**Where it came from**
> OSG ORGAN — the external organ of NESI, v0 (2026-07-31). A runnable, keeper-owned space for potential collaborators. Runs entirely on this machine; nothing calls out.
— `osg_organ/README.md`

**Latent capacity** — This is the only built answer anywhere in the corpus to OSG v6's own named hard blocker (`project_osg_v6_trust_infrastructure.md`: "the commons relay unnamed and unbuilt — STOP signal, nothing can run multi-person without this"). It is a working second-node intake surface with the membrane already enforced in code, not prose.

**Why it went unrouted** — Built local-only by design ("nothing calls out"); the README names the next step itself: "Making it reachable by a real collaborator (a small VPS, a tunnel) is a separate membrane crossing — Kevin's mark, not a rebuild." Confirmed unrouted: zero references to `osg_organ`, `organ.db`, or `nexi` anywhere in the deployed site clone (`oursharedgifts-deploy`) — it has never touched the live site at all, and the live site itself is currently returning a GitHub Pages 404 (see report).

**Shortest routing** — Way in: host `osg_organ` reachable (tunnel or small VPS) and link it from The Door or The Field. Act: a real second person signs in, works a circuit, makes one offer. Consequence: the first OFFER file lands in `membrane_outbox/`, awaiting Kevin's mark — the second-node precondition v6 has been blocked on since 2026-06-25 gets its first live test.

**Reading** — capacity H · routing effort M · confidence H

────────────────────────────────────────

Your mark:
