# THE FIELD — the VE with twelve mirrors and the order-12 collapse, running GDScript

**What:** The vector equilibrium at true coordinates (12 vertices = permutations of (±1,±1,0), scaled x10), one mirror per vertex carrying an organ's hard-limits line verbatim, empty held center, and the verified Kevin-initiated order-12 symmetric collapse (12 vertices -> 6, spring back) as a working, tested animation (test_field.tscn in the 9-scene harness). The only place in the corpus where the jitterbug is executable rather than prose.

**Source:** `nesi/world3d/scripts/field.gd`
**When:** 2026-08-01 (Stage 5)

**Evidence (verbatim):**
> # THE COLLAPSE is Kevin-initiated only ... the six standing pairs merge, 12 vertices -> the octahedron's 6 ... print("[field] sprung back — twelve vertices stand again")

**Capacity:** The cuboctahedron IS the VE — the 2D game's net is this object unfolded. The vertex data, six-pair organ placement, and collapse interpolation are exact geometry the 2D net can animate (fold/unfold, or a genuine 3D object standing INSIDE the 2D game).

**Unrouted because:** Reachable only in the 3D world's clearing; the 2D build draws its net from hand-set triangle constants and never touches these coordinates.

**Shortest routing:** Site the VE as a 3D-viewport object on one 2D face -> the collapse key at its edge -> the flex runs and springs back, nothing recorded.

**Reading:** capacity H · effort M · confidence H

---
This card orders nothing. It waits for Kevin's mark. Blank is a complete state.
