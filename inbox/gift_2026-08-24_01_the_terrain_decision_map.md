# GIFT · THE_TERRAIN.html — a built, four-ground decision map, unlinked from anywhere

Brought to the gate 2026-08-24, unrouted-gifts pass scoped to the workbench /
decision-surface / lineage trajectory now in motion. This card orders
nothing. Mark it, or leave it — blank is a complete state.

**What it is** — A finished, standalone HTML instrument titled "THE TERRAIN —
decision map." Renders every mark from `MARKS_LOG.jsonl` as a canvas/SVG
"overworld," clustered mechanically by the marks' own most-shared words —
explicitly not a judgment call — with four alternate grounds to view the same
marks through: **time · relationship · jurisdiction · discharge**, each
persisted per-ground in the browser. Edits (move a mark to a cluster, rename
a cluster) go through an explicit stage-then-copy tray — nothing auto-
applies. A sibling file, `THE_FIELD.html`, carries the same discipline one
step further: "No relation has been declared, so there is no tree — an
honest blank, not an empty graph. Nothing here infers an edge from text...
the tree grows from the words you write."

**Where it came from**
> "cluster names are the marks' own most-shared words, derived mechanically
> — not judgments"
> "No relation has been declared, so there is no tree — an honest blank, not
> an empty graph. Nothing here infers an edge from text."
— `THE_TERRAIN.html` and `THE_FIELD.html`, repo root, file-modified
2026-07-30 22:49 — the same date as Kevin's own mark that day: *"Kevin
always has decisions available — never hide a decision tree, map, or
topography"* and *"Adopt the locatable move as the governing criterion for
every future surface: locate, never steer."* (`THE_LEDGER_2026-08-23_
kevins_own_trajectory.md`, sourced to `MARKS_LOG.jsonl`.)

**Latent capacity** — This is the real, working answer to the complaint
Kevin has now repeated five times, 07-27 through 08-23 today: *"I can't
understand where the decision is located in your return."* It was built the
same day he first stated the "never hide a decision tree" requirement, and
then never linked from any page in the repo — grepped directly, no `href`
to `THE_TERRAIN.html` exists anywhere. It also already carries, built and
working, the exact two corrections from this session's own conversation:
narrative that's mechanically derived rather than authored (cluster labels),
and edges that require a human's own words rather than inferred meaning
(`THE_FIELD.html`'s tree). The lineage instrument prototyped this session
reinvented a version of this shape from scratch, standalone, with authored
node text — this file already solved both problems, three and a half weeks
earlier, and nothing routes to it.

**Why it went unrouted** — Its `MARKS` array is a hardcoded snapshot frozen
at generation time (07-30), not a live read of `MARKS_LOG.jsonl` — the marks
log has grown considerably since. Likely set aside as stale rather than
wrong, and never revisited to re-generate or re-wire live.

**Shortest routing** — Way in: link it from `state_view.html` or wherever
today's decision instruments are indexed, alongside a small script (or the
`node2water`/`bench_socket` seam already named elsewhere in this corpus) that
regenerates its `MARKS` array from the live log on demand instead of once.
Act: open it, pick a ground (time/relationship/jurisdiction/discharge). Visible
consequence: the exact "where is the decision located" answer Kevin has been
asking for since 07-27, in a form already built to his own two most recent
corrections about narrative and inferred edges.

**Reading** — capacity H · routing effort M (needs a live-data regeneration
step; the UI itself is finished) · confidence H

────────────────────────────────────────

Your mark:
