# GIFT · The Deepdive Chamber — a 606-line room to open one staged object in depth

Brought to the gate 2026-08-23, unrouted-gifts pass, whole-corpus sweep.
This card orders nothing. Mark it, or leave it — blank is a complete state.

**What it is** — A room to open one staged/canon object in depth (two at once, to compare), with three attributed verdict lines — library, governor, centre — governed by a documented visual-vs-language division of labour. Not a detail pane; a place to actually work inside one draft.

**Where it came from**
> "Not a detail pane — the room Kevin stands in and works one object inside, in depth. Two objects can be open at once (compare)."
— `nesi/conductor/deepdive.py`, pass-3 rewrite, 2026-07-21/22

**Latent capacity** — The only built object-level review surface deeper than the board's flat lists. Right now the board shows counts; this would let you actually stand inside one staged draft or canon pattern and work it.

**Why it went unrouted** — Its sole caller was `nesi_app.py` (the tkinter GUI), which `nesi/NESI_V2_ORGANS.md` marks COMPOST — superseded by the board. Neither `surface_bridge.py` nor `v2_board_data.py` — the two live NESI faces — imports it. It was orphaned by its caller's retirement, not by any judgment on itself.

**Shortest routing** — Way in: one CLI entry or a board panel calling deepdive's index/open functions. Act: open one staged object. Consequence: the three verdict lines render for that object, nothing else moves.

**Reading** — capacity H · routing effort M · confidence H

────────────────────────────────────────

Your mark:
