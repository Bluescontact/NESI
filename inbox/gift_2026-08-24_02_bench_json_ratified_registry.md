# GIFT · bench.json — a ratified twelve-organ registry, read by no runtime

Brought to the gate 2026-08-24, unrouted-gifts pass scoped to the workbench /
decision-surface / lineage trajectory now in motion. This card orders
nothing. Mark it, or leave it — blank is a complete state.

**What it is** — `nesi/bench/bench.json`, a ratified registry (`_meta.
authority: "nesi_job_grammar_2026-07-15.md — RATIFIED v2.1"`) of exactly
twelve real organs, each with a genuine `accepts`, `stages_to`, and
`hard_limits` field — e.g. `mark-record`: accepts "a mark just made in
conversation," stages to `SUBSTRATE_LEDGER.jsonl`, hard-limited to "never
writes/paraphrases/infers field 5; never scores; never cross-analyzes marks."

**Where it came from**
> "REGISTRY ONLY — nothing repointed... bench copies are mirrors-in-waiting."
— `bench.json`'s own `_meta.status` field, quoted in `nesi/game2d/
THE_WORKBENCH.md` §3, 2026-08-21.

**Latent capacity** — This is the exact data this session's own lineage
prototype (`lineage_instrument.html`) needed and didn't have: ratified,
Kevin-authored `hard_limits` and `accepts` text for a real, closed set of
twelve organs, sourced rather than composed. Two turns ago in this
conversation, the correction was made that a node's description text is
narrative and narrative is Kevin's, not the machine's, to write — `bench.
json` is precisely the sourced substitute: every field in it is already his
own ratified language, not prose a session would have to generate. Grepped
directly: no `.py`, `.js`, or `.html` file in the repo actually loads or
parses `bench.json` at runtime — the three files that reference its content
(`THE_TERRAIN.html`, `DEV_HANDOFF.html`, `THE_ASSEMBLY.html`) each embed a
frozen text snapshot rather than reading the file live. `THE_WORKBENCH.md`
§3 already built the pairing logic — twelve organs onto the platform's
twelve rising edges in `solid.js` — and named the one real gap plainly:
"none of these twelve organs can be invoked from inside [the game] as it
stands... a bridge does not exist yet."

**Why it went unrouted** — It was built as a registry for a different
routing question (which organ answers to which game-solid edge), and the
bridge `THE_WORKBENCH.md` itself says is still missing was never built as
its own pass — named, not designed, per that document's own closing section.

**Shortest routing** — Way in: any future lineage/decision-surface node set
reads `bench.json` directly for its twelve organ nodes' names and limit-text
instead of hand-authoring them. Act: a small loader (`fetch`/`readFileSync`)
replaces invented `desc` strings with the registry's own `hard_limits` and
`accepts` fields, verbatim. Consequence: the narrative half of any such
instrument stops being machine-composed and starts being Kevin's own
ratified words, exactly as this session's own correction asked for.

**Reading** — capacity M · routing effort L (the registry is complete;
only a read-and-display step is missing) · confidence H

────────────────────────────────────────

Your mark:
