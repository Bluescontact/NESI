# GIFT · THE CODEX INDEX - the pre-filter that refuses to judge

Routed to the gate 2026-08-11 by the unrouted-gifts extraction · second run: the DSS folder.
This card orders nothing. Mark it, or leave it — blank is a complete state.

**What it is** — Offline vector pre-filter (chromadb + sentence-transformers, CPU-only) scoring new text against all patterns/*.md, sorting intake into FOLD / HOLD / PASS.

**Where it came from** — tools/codex_index/ (build_index.py, screen_intake.py, query.py, run.bat, populated db/)
> 'It is a pre-filter, not a judge. It clears the obvious folds and the clean misses, and surfaces only the ambiguous ones - so your felt-read lands where it's decidable, not on all 35 patterns at once.'

**Latent capacity** — Puts the metabolizer's hardest cost - which of a pile is genuinely new - onto a mechanism that refuses to judge, leaving Kevin only the ambiguous band.

**Why it went unrouted** — run.bat/watch.bat exist but nothing in the daily-cycle close invokes it; CODEX_SCREEN.md is stale.

**Shortest routing** — Way in: one call in the daily-cycle close. Act: screen the session's own output. Consequence: a three-band digest where only HOLD asks for a read.

**Reading** — capacity M - effort M - confidence M

────────────────────────────────────────

Your mark:
