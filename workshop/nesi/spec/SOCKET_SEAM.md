---
kind: reading
author: claude
state: held
crosses: nothing
date: 2026-09-04
---

# THE SOCKET SEAM — held reading
Seven joints and one break, each carrying its card(s) from 1a and its site verdict from 1b (`../returns/SOCKET_SEAM_RECONCILE_2026-09-04.md`). A reading, not a source. It does not cross on its own authority.

**J1 · field ↔ tensegrity** — cards C1, C2. *Names an absence:* the membrane code the joint would test (skin.py, tension_table.py, whole_body.py) is not in this tree. Whether context is assembled as prestress or instruction cannot be read here.

**J2 · basins ↔ lint** — card C3. ASSUMED: `conductor/preflight.py` exists unexamined; the 07-22 two-representation question (vector store vs browse index) has no store to check against in this repo.

**J3 · holographic ↔ four depths** — card C4. ASSUMED: `tools/codex_index/` present, depths unexamined; the binding-graph artifacts are elsewhere.

**J4 · layers-as-instants ↔ heliostat** — card C5. *Names an absence:* no heliostat table, no LEXICON.md on this tree; *turn* remains ruled, unbuilt, unlisted — unverifiable from here.

**J5 · weighs ↔ articulate → candidate → mark** — card C7 ("The model does not choose, it weighs"). ASSUMED, and the pass's own rule applies: the seam doc takes no citation line while J5 is unverified. What stands instead is the report: no engine path to canon found; `engine.py` verified read-only; the gate guard is jurisdictional not identity-based; and `marks.py catch` defaults `source` to "the keeper" — the one place where an engine's word could wear Kevin's name by omission. That default is the finding that outranks this reading.

**J6 · perturbation ↔ intake drop** — card C8. COLLIDES: the metabolizer lives in the skill system, not in the tree the reading sites; "blind to the author" is still prose, not code.

**J7 · terminal law ↔ the sequence** — no card (S11 rides with C1). *Names an absence:* no glance/deepdive pair on this tree; the same-sequence-two-gate-modes claim has no site to test.

**THE BREAK** — absent from the source, which ends at x_L. VERIFIED (partial) on this tree: `continuity.py` returns the last sitting's marks and open threads as the next sitting's resume context — the return half of candidate → mark → next x₀ is wired. The comparison half is documented-not-built by its own SEAM note. The break the reading named is half a break.

---

## Lexicon candidates — for his line, not acted on
LEXICON.md is not in this repo, so all ten are listed as unverifiable-against-the-list: socket · seam · basin · field · perturbation · candidate · weigh · attractor · resolution · instant. The cheapest unblock on the board remains the word-list; said once.

## Dead-branch pulls — named, refused
S18 "resonance of meanings" and S05's holographic whole-in-any-region are the two lines that would tempt the retired interferometer/phase reading (2026-07-26). Both filed as `figure` in the cards; neither routes anywhere; the branch stays dead.

This deposit adds no organ, renames no organ, reframes no tetra. Connective tissue only; held until his line.
