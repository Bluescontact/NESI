# Set the floor

**Status:** LIBRARY · crossed 2026-07-30 (the keeper's mark, crossing1_2026-07-30_set_the_floor)
**Lineage:** the tenth and final organ surviving the 2026-07-29 real-pressure collapse of the 157-pattern canon, run against `CANON_BUNDLE_part1-3`. Held unset through the first nine crossings, promoted separately the following day.
**Provenance:** `_INTAKE/freeze_2026-07-29/THE_COLLAPSE_organs_2026-07-29.md`. Converger pass captured verbatim at `membrane/converger_passes/2026-07-30_set-the-floor.json` (the keeper, 2026-07-30): "set the floor too."

---

## The action

Level this one specific landing by hand, at the height where the arriving thing comes level with where people step off, and do not emit the record that says it is set until the leveling labor has actually been performed.

## What collapsed into it

`domain_term_names_stance` (the trade term is the operative verb, not an analogy) · `ground-then-record` (two emitters, one event; the cheap half fires on a word) · `per-landing-calibration` (no global setting; nothing propagates; the skipped landing is always the least-trafficked) · `gap-invisible-from-above` (the misset landing registers only on the body crossing it) · `reference_before_load` (a flickering zero is costlier than a missing one) · `cold-start-needs-outside-zero` (the reference is deposited only by a completed crossing of that type) · `survival_friction_capability_floor` · `floor-not-ceiling` (a boolean gate replaces an unbounded scalar) · `owned-floor-makes-no-cheap` · `need-less-as-liberation-engine` · `floor_that_holds_a_stranger_without_extraction` · `legal_as_floor` · `village_container_vs_system_container` · `shop_self_knowledge` · `tended_void_paradox` (the substrate is built before the organ can run) · `difference_preserving_substrate`

## What didn't survive

Every version of "raise the standard." The organ has no ceiling function at all. Nothing in the surviving action compares one floor to another; comparison is the failure mode the floor was installed to end.

## Falsifier

If a single global adjustment ever corrects two landings at once, this is not per-landing handwork and the organ splits into a calibration act and a configuration act. Also fails if a record emitted over unleveled ground produces no downstream harm — that would make record-and-ground one emitter, not two.
