# Stake the read

**Status:** LIBRARY · crossed 2026-07-29 (the keeper's mark, crossing1_2026-07-29_stake_the_read)
**Lineage:** one of ten organs surviving a real-pressure collapse of the 157-pattern canon, run against `CANON_BUNDLE_part1-3`. Its own falsifier names its relationship to MARK IT AT THE BODY OR IT DOES NOT CROSS directly — this organ is that one's second beat, not an independent axis.
**Provenance:** `_INTAKE/freeze_2026-07-29/THE_COLLAPSE_organs_2026-07-29.md`. Converger pass captured verbatim at `membrane/converger_passes/2026-07-29_stake-the-read.json` (the keeper, 2026-07-29): "I'll approve a converger pass through the nine organs."

---

## The action

Put the read into the world in a form that will cost you if it was wrong — a published position, a defined boundary offered, a small crossing performed purely to register the zero, a document in a shared channel before the decision event — and take the consequence you did not author as the only validation available.

## What collapsed into it

`read_witnessed_by_stake` (an unstaked read is unfalsifiable, and unfalsifiable is unwitnessable) · `shadow_in_witnessing` (clarity may indicate proximity, not distance) · `lens_field_falsification_check` (a field-pattern is earned only where a definable boundary was declined) · `cold-start-needs-outside-zero` (one small crossing done purely to deposit the reference) · `diagnosis_before_grievance` (transmission reclassifies every subsequent institutional action) · `deliverable_as_self_disclosure` (the gift is a coordinate; the disclosure is computed by the recipient from the differences) · `written_record_as_pre_meeting_coalition` (simultaneity in one medium, without coordination) · `defined_version_diagnostic` (present the specification and let the result arrive)

## What didn't survive

Private certainty. Nothing survived that let a read be confirmed without exposure, including the most sophisticated version: a compression of yourself handed back to you.

## Falsifier

If a read is ever corrected by a process with no exposure and no possible cost, then consequence is not the validating mechanism and this organ collapses back into MARK IT AT THE BODY OR IT DOES NOT CROSS as its second beat rather than standing as its own action.
