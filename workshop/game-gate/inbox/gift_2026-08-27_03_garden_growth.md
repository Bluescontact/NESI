*Crystallized 2026-08-31 per the commons rule: pattern crosses, instance does not. The working record stays in the source corpus.*

# GIFT — the local rule with no objective

- **pattern:** a small reaction-diffusion field — each cell's next value a function of its neighbors alone, run to a fixed point on entry, never iterating while the hand is present. No fitness function, no target, no score anywhere in it; the absence of an objective is the organ, not an oversight. Any optimiser is refused at this seat by definition, because an objective is a statement about what the growth means.
- **source shape:** found in an earlier build's rendering layer, cut whole in a later full rewrite and kept in compost.
- **capacity:** structural facts the system already computes (counts, orders, gaps) can feed a visual form that grows from what already happened — something to look at growing from a session's own activity, with nothing to chase, nothing scored, and no new capture in the read path.
- **why such value goes unrouted:** a full rewrite orphans working organs; the surface that later needed this did not yet exist when the organ was cut, so there was nowhere to re-site it.
- **routing shape:** a way in → feed the field from facts already computed → an act → run to a fixed point once on load, exactly as built → a visible consequence → a standing form grown from the work itself, answerable to no target.
- **reading:** capacity: M · effort: M · confidence: M
