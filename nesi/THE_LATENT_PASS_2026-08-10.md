# NESI · THE LATENT PASS — the record, 2026-08-10 (evening)

*Written on Kevin's ask ("offer a knowledge doc to report the progress made in
this session"), session c9e756f4. Everything below was run and seen this
session; nothing is assembled from memory. The pass's charter: give existing
things doors — build nothing new.*

---

## Where it stands tonight

**The world is no longer orphaned.** `nesi/world2d/export/NESI.exe` (packed
20:59, swapped in at Kevin's word after he closed his own walk of the old
build) opens on the intake — nothing precedes it — and now carries **THE WORLD
— through the glass** at the bottom-right, with **THE INTAKE — the writing**
waiting on the other side. Both directions walked by the harness, screenshots
read. Kevin's live stores (`writing_stones.json`, `writing.txt`, `world.json`)
were never opened by a tool at any point.

## The door in (before step 0)

The session opened blocked: the two files the brief names do not exist
anywhere in the tree. Kevin ruled `PROGRESS_2026-08-10.md` is the converged
output; `NESI_STATE_OF_THE_WORLD.md` stays absent (globbed twice, two
patterns) and nothing was substituted for it. The pass ran on the one ruled
file.

## Step 0 — the route restored, the set recorded

- One door each way, connection only, the opening not redesigned:
  `world_btn` in `writing_main.gd` → `scenes/main.tscn`; `writing_btn` in
  `main.gd` → back. A `Check` guard added in `main.gd` so the intake's
  harness is not hijacked by the world's when it rides the door. The walk
  extended (`tools/writing_check.gd`) to travel the route both ways. Every
  identifier built from words already on `LEXICON.md`; the list untouched.
- One real failure caught by its own screenshot: both door faces clipped at
  the window edge — repositioned and re-proven.
- **THE WALKABLE SET, from launch, a screenshot each, in `.shots/`:** the
  intake blind · TILES with GIVEN/FETCHED and MERGE/JOIN · THE TARP laid and
  folded · SEQUENTIAL with the Ctrl+Enter deposit · hold through reload ·
  THE WORLD through the door (tank, four stations, the triangle, the dams) ·
  the four station panels with their three outputs (24-shot interior walk) ·
  the intake again through the door back. 26/26 harness checks true, run
  twice; re-walked after step 2's sitings — nothing left the set.
- Edge: the interior station walk was launched at `main.tscn` directly with
  `--harness` (the world's own eyes cannot ride the route yet); the route
  itself was proven separately in both directions.

## Step 1 — the lexicon candidates

**`LEXICON_CANDIDATES_2026-08-10.md` at the DSS root — 28 words, unranked, no
recommendation, LEXICON.md untouched.** Each entry: the word · where the build
wanted it · what it was rebuilt as. Among them: intake→writing_*, tile→stone,
tarp→ground, blind→paper, sequential/page→sheet, merge→pool, source→kind,
label→mark, link→line, seam→the string key only, turn→nothing (THE TURN never
shipped). Two findings ride the list: six rebuilds landed on words the lexicon
already gives to a different object across the glass (pool, ground, release,
drop, mark, line) — the substitutions manufactured collisions; and `pick` is
live in code (`picks` in `pool_stone`) authorized by nothing.

## Step 2 — the siting record

Inventory: every named candidate VERIFIED on disk; two paths corrected from
the brief (the terrain tools at `tools/`, THE_DOOR.html at `nesi/`); nothing
MISSING, nothing ASSUMED. patterns/: 176 files present; the 62/173 counts are
the brief's own, not re-derived per file.

**SITED (2):**
- The 2d bodies of the dam, spires, rivers, lake, tank and four stations —
  through step 0's door; the walk is the proof.
- The membrane router's output — it already reaches the build through the
  GIVEN lane by Kevin's own paste; the door existed unrecorded. An automatic
  feed would put a machine sorter on the path law 5 reserves for the
  operator's hand; the absence of one is the design.

**FORK-BLOCKED (4), defaults named and NOT taken:**
- **All of world3d** (3d dam/river/spires/tarp, the Shore round-trip, the web
  export, PLAY_WORLD.bat) — behind the §10 front-door fork, unruled. Default
  not taken: a third door shell-opening PLAY_WORLD.bat, which would have
  closed §10 silently toward world2d-as-front-door.
- **THE FIELD** (`world3d/scripts/field.gd`, recorded UNPLACED) — placement is
  the undecided thing. Default not taken: instance it into world3d's main
  scene.
- **THE_DOOR.html** (`nesi/`, beats 1–4, playable) — a third front-door
  candidate; siting it IS the fork. Default not taken: mark it superseded by
  `writing.tscn`, killing the 2026-08-04 playable-opening lineage unmarked.
- **`tools/terrain_layout.py` · `tools/export_pile.py`** — consumers behind
  the same fork; they inherit its default.

**SITELESS (3), the absences named:**
- The board (`nesi/world/logbook.html` — THE BAR, THE PAGE, the thought
  strip): it reads the store the 3d Shore writes; the running build writes
  `writing_stones.json`. A door to it would be a port, not a siting — stopped
  on that item. The absence: nothing in the world2d loop reads or writes the
  world/ store.
- The bench (`nesi_bench_v0`) and the Composer
  (`bench/composer/composer.py`): no consumer inside the running build
  exists, and none was proposed.
- patterns/ (176 files): no reader carries the library into the FETCHED
  lane; FETCHED-by-paste stands as the by-hand route.

## The re-export (Kevin's separate asks)

First attempt failed on the rename — Kevin's own walk of the old build held
the file (PID 10756, open since 16:22); it was not touched. Exported clean to
`export/staged/` (exit 0), and on his word after he closed the window, swapped
onto `export/NESI.exe` (109,160,752 B) with the process list checked first.
The swapped exe was not itself launched: it shares the live store, and the
project it packs was walked in the editor the same evening.

## Open — named, not asked

- The §10 front-door fork, still open, now with three candidates on the
  record (NESI.exe · PLAY_WORLD.bat/world3d · THE_DOOR.html) and one default
  per blocked siting written down.
- The 28 lexicon candidates, waiting on Kevin's line — the list's only intake.
- The three siteless absences above.
- `NESI_STATE_OF_THE_WORLD.md`, still absent under that name.

## Edges

The exported build was verified by the editor walk of the identical project,
not by launching the exe. Merging stones of differing sources and the tarp
with sheet stones remain unexercised (carried forward from PROGRESS). The
world's own harness (`tools/click.gd`) cannot yet ride the door from the
intake side — extending it was not ordered and was not done.

WALKABLE.
