# OVERNIGHT BUILD SPEC — the full orchard + the crossing stones
*Approved: Kevin's mark 2026-08-01 12:37 (MARKS_LOG #292) · unattended · report in the morning*
*Explicitly NOT in scope: stage 5 (NESI commands — spec adopted but build not approved tonight) · the tetra/fog/visibility build (parked on Kevin's mark behind its three gaps: living-intake felt read, fog fork, threshold numbers).*

## Inherited law (binding, from THE_WHOLE_NAMING §7 + prior stage reports + the signed TETRA BUILDERS AGREEMENT)
Mechanism-works claims only — whether it is the world Kevin wanted is his walk-read. Declared material only, embedded at build time; no text analysis in-engine. No telemetry, no network. Nothing directs Kevin's body. Light palette. Writes only inside `nesi\world3d\`. Every build proven: headless test where applicable + a real-browser walk with screenshots. Report names what ran AND what wasn't checked.

## Build A — THE FULL ORCHARD
The grown shelf becomes the library as terrain: **one tree per canon pattern file** in `patterns/` (~175; exact count fixed and reported at build time).
- Placement seeded deterministically from the filename (a pattern keeps its spot across builds). Size from file mtime (older = larger). Same low-poly wireframe line-tree style as stage 2's shore trees.
- On approach (r≈6): a Label3D shows the pattern's plain name (filename de-slugged); first line of the file as one subtitle line. Embedded at build time — declared material.
- The ten stage-2 decorative shore trees stay as they are (they are shore, not shelf).
- Performance edge: if 175 individual trees break the web export's frame rate, thin the geometry, never the count — every pattern gets its tree.

## Build B — THE CROSSING STONES
The MEMBRANE arch threshold paved with **one stone per transition record** in `membrane/transition_records/` (113 at approval time; count fixed at build).
- Flat low-poly stones along the dashed threshold line, ordered by record date. On approach: the crossed pattern's name.
- The 62 baseline-gap files (from `membrane/falsifier_baseline.txt`) appear as visually distinct **uncut stones** with no name shown — the record's own edge made visible, never smoothed.
- The arch's existing panel and gift law text unchanged.

## Proof required before "done"
Headless: tree count == pattern-file count · stone count == record count (+ uncut == baseline count) · all placements at sane terrain heights. Browser: walk to the shelf, approach a tree, screenshot its label · walk the threshold, screenshot a named stone and an uncut stone. Export clean via `tools\godot\Godot_v4.7.1-stable_win64_console.exe --headless --export-release "Web"`.

## Output
Updated export behind PLAY_WORLD.bat · `nesi\world3d\OVERNIGHT_REPORT_orchard_stones_2026-08-01.md` · the open gate ("Overnight build running…") is closed by the MORNING SESSION only after Kevin sees the report — the builder never closes it.
