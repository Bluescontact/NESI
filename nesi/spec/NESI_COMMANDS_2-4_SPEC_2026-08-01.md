# NESI COMMANDS [2][3][4] — BEHAVIOR SPEC
*Status: ADOPTED · Kevin's mark 2026-08-01 11:12 ("yes to all three"), caught in MARKS_LOG (#289)*
*Assigns the stage the STAGE_2-4_REPORT flagged as missing: "commands 2–4 name no stage because no document assigns one." This document assigns it. Build itself remains gated on a pre-flight manifest + Kevin's go.*
*Names carried verbatim from `nesi/world3d/scripts/main.gd` (already in-world). Behaviors adopted from the 2026-08-01 candidate pass — Dreamer material taken whole by Kevin's mark.*

## [2] search the deep for names
When asked (and only when asked), NESI dives once into the deep-water disc and returns carrying **one** unnamed thing that already exists in the system — an unfilled slot, an unnamed friction, a held fork — shown **verbatim from its file**, never named or interpreted for Kevin. One dive, one object, then she surfaces. She never dives unasked and never says what the thing means.
- **Material (declared, embedded at build time — no runtime file reads, no text analysis in-engine):** a compiled list of unnamed/held items, e.g. THE UNNAMED slot (builders agreement), held forks from memory records, OPEN_GATES entries flagged unmarked. Exact list fixed at build time and named in the build report.
- **Laws:** recognition law (she never does the recognizing) · no scores · verbatim only.

## [3] water the orchard
When asked, NESI waters **one** tree on the grown shelf and that tree shows its **root**: the canon pattern or built organ it stands for — name and one line, drawn from `patterns/` at build time. One tree per asking; no tour, no sequence, never a health report on the grove.
- **Material:** tree→pattern mapping compiled at build time from `patterns/` file names + first-line descriptions (declared material only).
- **Laws:** no judgment of the grove · one per asking · the library is touched, never evaluated.

## [4] hold the vortex
When asked, NESI stills her spiral and the world goes quiet with it — **no panel, no text, no object returned**. The only command that produces nothing: a held pause in the world's one perpetual motion, ending on its own (after a while) or on E.
- **Laws:** nothing is measured, timed for Kevin, or reported · the quiet is the whole behavior.

## Shared laws (all three)
Act only when asked · never judge, score, or interpret · nothing directs Kevin's body · all material embedded at build time and named in the build report · no network, no telemetry (world laws carried from stage 1).
