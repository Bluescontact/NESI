# THE ORPHANS — two built things that govern nothing

**Written 2026-08-05 on Kevin's mark:** *"ROUTE THE ORPHANS: name where THE FIELD and THE_DOOR.html sit in the naming, or record them as instruments outside the world and stop carrying them in the world's inventory."*

The mark offers two exits. **One half is machine labor and is done below: both are recorded as instruments outside the world, and the world's inventory stops counting them as world material.** The other half — naming where they sit — is not mine, and the slot for it is left open and empty rather than filled with a proposal.

---

## 1 · THE FIELD

**What it is.** `nesi/world3d/scripts/field.gd` (13.4 KB) + `scenes/test_field.tscn`. A vector equilibrium at true coordinates. Twelve mirrors, each carrying one organ's `hard_limits` **verbatim** from `bench.json`. The centre visibly empty and held. On `[C]`, the order-12 collapse: all six pairs to gap 0.0000, then exact spring-back to rest.

**Its state.** Built and browser-proven 2026-08-01 (STAGE_5_REPORT). Re-verified headless 2026-08-05 — still passes, unchanged by the bedrock swap. `BUILD_SPEC_v1` §5 marks it **UNPLACED**: it appears in no naming from 2026-08-04 and the spec explicitly declines to force it into one.

**Why it does not fit the water.** The watershed has four kinds of matter (bedrock, deposit, water, the crossing) and a closed set of indicators. THE FIELD is none of them. It is not terrain, nothing settles in it, and it does not erode. It is a **geometry that demonstrates a law** — that twelve limits can merge to an octahedron and spring back without any of them being surrendered.

**Recorded as:** an instrument, outside the world. It stands beside NESI rather than inside it, in the same class as the Governor and the Converger — something you run, not somewhere you are. It stays exactly where it is on disk, keeps its test, and is **no longer counted as unplaced world material**.

**The slot only Kevin fills:** ▢ *where, if anywhere, THE FIELD sits in the naming.*

---

## 2 · THE DOOR

**What it is.** `nesi/THE_DOOR.html` (13 KB), built 2026-08-04.

**How it came to exist.** In answer to *"there's no build, only lint."* The session close records it without softening: *"Answered by building `nesi/THE_DOOR.html`. Stopped. The file stands, unmarked, governing nothing."* It was a build made where a naming was asked for — the failure named the same day, not discovered later.

**Recorded as:** an instrument, outside the world, and **explicitly not a specification of anything.** `THE_DOOR_AND_THE_SHAPE_2026-08-04.md` is the naming that governs the door; the HTML file governs nothing and is not read by any build. It stays on disk under this project's standing rule against destructive cleanup — retired, not erased.

**The slot only Kevin fills:** ▢ *whether THE_DOOR.html is worth keeping at all.*

---

## 3 · WHAT THIS CHANGES

- The world's inventory is **`nesi/world3d/` + `nesi/world/logbook.html` + `nesi/THE_WORLD.html`** — the three surfaces now joined on one deposit path. THE FIELD and THE_DOOR are not in it.
- Neither file is deleted, moved, or edited. Nothing that ran yesterday stops running.
- **Falsifier:** if a future session again reports THE FIELD as "unplaced world material" or treats `THE_DOOR.html` as a spec, this record failed to route them and the two slots above are still the only real question.

*Nothing here names anything. The naming is Kevin's, and the slots stay empty until he fills them.*
