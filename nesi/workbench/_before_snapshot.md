# TUNED SURFACE — PROJECT SCOPE + ASSEMBLY MANUAL

**Status:** SCOPE drafted · ASSEMBLY SPINE named · vertices STAGED (filled one per turn, on Kevin's mark).
**Date:** 2026-07-23 · session 56d2727d.
**Grammar:** built in Kevin's construction language ([[seed_construction_language]]) — tetrahedral vertices, elevator sequence (plumb before load), 9-field organ spec, falsifier per element.

---

## 0 · THE REFRAME (why this exists)

The last two turns exposed a category error: asked for the Tuned Surface, I ran the **architecture** vertex first — I improvised a canvas (`tuned_surface_v0.html`) — skipping **naming, limits, connection.** That is why it felt wrong and Kevin stopped it.

Kevin's correction, verbatim: *"what I'm asking for isn't a build, it's a project scope and manual for assembly."*

**The move this names (candidate — resolves the whole "named is not built" session):**
> A name is a vertex. A build is the assembled tetrahedron. **The assembly manual is the object that carries a name across to a build without losing its shape** — and without Kevin holding the spec in his head or re-steering every improvisation. The manual is the missing middle. Building the manual is not building the thing; it is what makes building the thing deterministic.

---

## 1 · SCOPE

**What the object is.** The Tuned Surface is NESI's single-occupant spatial bench surface: a canvas Kevin drives directly, where intake enters upstream, ideas hold under visible tension, hard limits are rigid, and the gate crystallizes a mark only at the moment of decision, then relaxes to silence. Engine-independent — it is an interaction surface, not the metabolizer.

**Boundary (what is NOT in scope).**
- Not the two-person Workbench. That is M3-gated (a live "no") and out of scope here. This is Kevin's own surface.
- Not the engine. No generation, no `/login` dependency. The surface runs dark.
- Not a replacement for the native window. This surface may later be hosted inside the geometric-bench/pywebview shell or the tkinter app; the manual specifies the object, not which shell hosts it.

**Definition of Done (for the MANUAL, not the build).** The manual is done when a fresh session could assemble the Tuned Surface from it **without Kevin re-steering the shape**, and when the assembly it specifies ends by **wiring the gate to the real ledger** (a mark writes to `marks.jsonl` / moves a real `staged/` object) — the single wire that turns surface into infrastructure.

**The manual's own falsifier.** If building from this manual still requires Kevin to improvise or course-correct the object's shape, the manual didn't hold — it was notes, not a manual. (Same discipline as ARTIFACT_GRAMMAR's self-falsifier.)

---

## 2 · THE ASSEMBLY SPINE — four vertices, elevator order

Kevin's tetrahedral build verbs (his dreaming-pass method: *name → connect → limit → assemble*, run one stage per turn). Reordered here by the **elevator sequence** — *plumb before load* — so LIMIT (the plumb-lines) precedes ARCHITECTURE (the load):

**① NAME** — the parts and what each MEANS.
*Preview:* field · node · gate · membrane (no-strut) · tether · upstream edge · canon zone. Each gets one line: what it is, what it means in the system's grammar (e.g. the gate = the crossing where a mark crystallizes; the membrane = a hard limit that does not deform). This is the 9-field spec's "name/parts" layer.
> STAGED — fill on Kevin's mark.

**② LIMIT** — the plumb-lines and no-struts (laws, set before any feature).
*Preview:* no prompts, ever · no telemetry / no reading the writer · no auto-crystallize (only Kevin's mark crystallizes) · engine-dark independence · the one-wire honesty (surface labels itself surface until wired) · self-easing is mandatory not optional. These are the rigid struts the whole structure is trued against. Each carries its falsifier.
> STAGED — fill on Kevin's mark. (Elevator law: these are plumbed before architecture is loaded.)

**③ CONNECT** — the six edges: how the parts relate, and how the surface connects to the real system.
*Preview:* node↔tether (tension = visible load) · node→gate→canon (the crossing) · membrane∘gate (the strut with one opening) · upstream∘dissolve (the funnel) · idle∘self-ease (withdrawal) · **surface∘ledger** (the seam to `marks.jsonl` / `gate.py` / `staged/` — the wire that makes it infrastructure). Names which internal edges are decorative and which are load-bearing.
> STAGED — fill on Kevin's mark.

**④ ARCHITECTURE / ASSEMBLE** — the ordered build steps (the elevator run).
*Preview:* plumb (install ② limits as code-level invariants) → rails (the field + drag) → **false car** (a throwaway reference prototype that proves the shaft — see §3) → cables + motor (wire the gate to the real ledger, ③'s load-bearing seam) → cab + entrances (the visible surface) → single button (one end-to-end assembly test). This vertex is where a real build finally happens — as execution of ①–③, not improvisation.
> STAGED — fill LAST, on Kevin's mark, after ①②③ are ratified.

---

## 3 · THE PRIOR CANVAS, DEMOTED TO ITS CORRECT ROLE

`tuned_surface_v0.html` (built prematurely this session) is **not the deliverable.** In Kevin's elevator grammar it is a **false car** — a temporary stand-in that proves the shaft works (drag, jitterbug-at-gate, tensegrity tethers, upstream dissolve, self-easing all run) and is discarded when the real, ledger-wired car is assembled at vertex ④. Kept on disk as a reference prototype for the architecture vertex; it carries no authority and crosses nothing.

---

## 4 · MARKS OPEN (Kevin's gate)
- **The frame itself:** is the manual's spine these four vertices (NAME · LIMIT · CONNECT · ARCHITECTURE), assembled in elevator order? — ratify / change the verbs / hold.
- **Which vertex to fill first** (one per turn, per your method): NAME · LIMIT · CONNECT · (ARCHITECTURE last).
- **The named move** ("the assembly manual is the object; the build is its execution"): candidate for canon / hold / drop.
