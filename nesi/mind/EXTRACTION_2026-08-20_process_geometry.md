# EXTRACTION — the geometric shape of how Kevin works with AI

> **■ DEVELOPED, 2026-08-21, Kevin's mark ("i dont want to lose the process
> shape extraction... and i want to run full development on it").** Full
> nine-stage pass run (audit, ground, and diverge fanned to independent
> subagents). The original claim below is kept whole, unedited — what
> follows is what survived, and what didn't.
>
> **The claim was inflated, not false.** Adversarial audit found: the
> master shape is close to a truism ("humans decide, AI assists") dressed
> in precise vocabulary; all 12 extracting agents were told a shape existed
> before they looked and all 12 confidently found one — a real
> contamination risk, since none returned "no clear shape"; much of the
> evidence is a readback of templates and folder pipelines Kevin himself
> built to enforce exactly this property, not an emergent discovery;
> "layer, don't erase" is compliance with an explicit written corpus
> policy, not a second geometric pattern; the named "divergences" test how
> ambiguity resolves, a different axis from the master claim, so they never
> actually stress-tested it.
>
> **Grounded against its own real use.** This shape was used tonight as a
> stated lens to reconcile nine real tensions in the library. Checked
> against what was actually caught: **seven of nine are exactly what the
> pre-existing `authority-check` and `record-audit` skills already catch by
> name**, one is ordinary coherence-checking, and **one — the navigator
> reframe in `project_nesi_gift_workspace_navigator.md`, where a file
> honestly labeled an idea "unmarked" yet still let it de-scope a whole
> build — is the one place the specific framing caught something a
> label-only audit would pass clean**, because it requires the extra step
> the shape actually adds: a mark has to authorize the *act*, not merely be
> present somewhere in the record.
>
> **What's kept:** the underlying description is accurate and stays true —
> Kevin's mark verbatim-first, overriding automated screens in both
> directions within the same batch, corrections layered rather than
> edited in place. That's real, evidenced, and worth having on record. What's
> **not kept**: the framing of this as a discovered general law of
> human-AI collaboration, worth citing as an independent instrument.
>
> **Standing use, going forward:** a corroborating reference and a reminder
> to run `authority-check` / `record-audit` — not a standalone analytic
> lens. Cross-referenced into `authority-check`'s own `SKILL.md`. The one
> genuine addition it makes — *a mark must authorize the act, not just
> exist in the record* — is worth keeping in mind precisely because it's
> the one thing that survived being checked against everything already
> built to catch this class of drift.
>
> **The checklist this episode actually paid for**, for the next time a
> swarm pass is asked to find a corpus-wide pattern:
> 1. Did any agent's prompt state the pattern exists before it searched? If
>    yes, discount convergence — it may be the prompt, not the corpus.
> 2. Could the "finding" be a readback of a template, schema, or policy
>    already written by hand? If yes, it's evidence about the engineering,
>    not an emergent pattern.
> 3. Before treating a finding as a usable lens, run it against real cases
>    an existing skill already covers. If it duplicates that skill's
>    catches, it's a reminder to invoke the skill, not a new instrument.
> 4. Does a named "divergence" actually stress-test the master claim, or
>    test an adjacent axis dressed as a counterexample?
>
> ---
> **The original claim, unedited, below.**

**2026-08-20**, on Kevin's ask: *"the way Kevin works with, and processes
language with AI... that process has a geometric shape... run a swarm pass
to extract shapes and patterns around the data process at 4 different
altitudes across the whole library."* Workflow `wf_5a6fc756-aae`, 12 agents,
read across `nesi/mind/feedback_*.md` (61 files), `MARKS_LOG.jsonl` (1425
marks, 5 chronological chunks), `nesi/mind/project_*.md` (78 files, 2
batches), and `LEARNED.md`/`LEARNED_PROVENANCE.md`/`PROTOCOLS.md`. Full
report published as an artifact: *The Crossing Shape*.

**The finding, compressed to one line:** the same three-beat shape —
*the human's mark, verbatim and first; the AI's contribution, bounded and
named insufficient on its own; a seam crossed only from the human side* —
recurs, independently evidenced, from a single feedback file up to the
corpus's own laws about how its laws get built. Its structural guarantor,
*layer-don't-erase* (nothing edited in place, a dated layer stacks on top,
old text stays legible underneath), recurs alongside it at every altitude
checked.

**What doesn't fit, named rather than smoothed over:** ambiguity gets
resolved oppositely at the two ends of the scale (word-altitude sharpens a
fork into two named poles and picks one; corpus-altitude dissolves the axis
so the original choice becomes moot) — genuine divergence, not the same
move in different clothes. Multi-agent convergence and branch-and-converge
for outward-facing risk only exist at thread/corpus scale — not missing
patterns, just patterns that need more material than a word or a turn can
hold. Full detail, citations, and the diagram are in the artifact.

**What this pass did not do:** rule on whether this shape should become a
named standing pattern (a law, a skill, a citation point) — that's a
naming decision, his to make, not this extraction's.
