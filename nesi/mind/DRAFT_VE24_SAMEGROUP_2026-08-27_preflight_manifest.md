# DRAFT — the 18 same-group pairs: reopening the 66, preflight layer

Proposed 2026-08-27, arising from item 4 of
`nesi/mind/DRAFT_LENS_PANEL_VE24_2026-08-27_preflight_manifest.md` — Fuller's
lens-panel reading found that the VE24 build's own cuboctahedron geometry
was used not just to *order* the 24 real edges but to decide **which
relationships get built at all**, foreclosing the 18 same-group pairs (six
per RULE/READ/BUILD group) by construction, regardless of what the corpus's
own text says about any given pair. Kevin's mark, offered a fork between
"small lever" (extend the traversal citation grid) and "reopen the 66,"
chose **Branch B: reopen the 66** — treat the 18 foreclosed pairs as a
genuinely open batch, through the same scope/spec/verification/preflight
discipline the original 24 went through.

**This document is planning only. No HTML artifact is built yet. Nothing
below is authorized until Kevin marks it — per law 30, handed over unranked
and whole, all 18, not a curated subset.**

Three parallel research agents grounded all 18 pairs against real files
this pass (one per group — RULE, READ, BUILD), mirroring the original
24-edge preflight's own four-agent grounding method. Their findings are
below, formatted to the same scope/spec/verification/preflight shape used
throughout this corpus.

---

## Cross-cutting findings — before any single pair, read this

- **"Gate" is genuinely three or four unrelated things in this corpus**,
  confirmed again by this pass: the sovereign/enforcement distinction
  (`LEARNED.md:24-26`), root `gate/gate.py` (felt-read queue), `nesi/game2d/gate/*.mjs`
  (builder ledger, the one wired to `admit.mjs`), and a fourth,
  `tools/gates.py` (OPEN_GATES.jsonl, cited in `PROTOCOLS.md:407,611`,
  retired 2026-08-27) — sitting entirely outside domain D's stated file
  list. This needs disambiguating before any D-touching pair (A-D, B-D,
  C-D) is built, on top of the original 24's own two-gate warning.
- **Two of the six RULE pairs (A-D, C-D) have no direct textual/mechanical
  connection found** — the research agent's own honest finding, not a
  gap to smooth over. Building HTML for these would mean constructing an
  inferred argument rather than quoting an existing link, which conflicts
  directly with this corpus's own admission standard ("every line sourced,
  none composed," `LEARNED.md:9-10`). Flagged as the two weakest pairs of
  all 18.
- **Domain-boundary overlap, not just adjacency, recurs across the READ
  group**: `lens-panel` is claimed by both E and F in this task's own
  scoping; `full-development`/`external-extraction` are claimed by both F
  and H; `library_lens.js` is claimed by both G and H. Whichever READ pairs
  get authorized will need to state which side owns the overlapping file,
  or note the overlap explicitly rather than silently pick one.
- **`unrouted-gifts` still has no locatable local `SKILL.md`** — confirmed
  again by this pass (only `.claude/skills/unrouted-gifts_work/`, four
  rescued `.py` files, no SKILL.md), consistent with the original 24's own
  finding. Any F-H or G-H pair citing "the unrouted-gifts skill" as a
  corpus file should say this plainly.
- **`nesi/bench/*/SKILL.md` count is stale in this task's own brief**: the
  task said 11, the live count is 14 (two using lowercase `skill.md`).
  Named here so it doesn't recur as an unflagged citation inside a built
  pair.
- **K's boundary is doubly unstable**: the directory count is disputed
  within its own report (45 per the original VE24_07 census vs. 44 in
  `K_LENS_REPORT_2026-08-28.md`'s fresh count), and this task's own brief
  cited that report at the wrong path (`nesi/mind/` — it actually lives at
  `nesi/game2d/inbox/K_LENS_REPORT_2026-08-28.md`). Any K-touching pair
  (J-K, I-K, K-L) inherits this instability.
- **A dating inconsistency in the K-L material**: `K_LENS_REPORT_2026-08-28.md`
  is dated one day after the session bridge file that documents it
  (`SESSION_BRIDGE_2026-08-27_three_actions_and_the_return.html`) —
  possibly a filename/stamp mismatch, possibly a pass spanning midnight.
  Named, not resolved.
- **Several of the strongest BUILD-group connections trace to one shared
  mechanism** (`tools/library_lens.js`'s gift-extraction pipeline and its
  `inbox/` outputs) rather than four independent relationships — worth
  deciding, if several of I-J/I-K/I-L/K-L are authorized together, whether
  some collapse into fewer genuinely distinct documents rather than four
  separate ones repeating the same citation.
- **A live drift, found in passing**: `world2d/RETIRED.md`'s own
  accounting says 3 of 5 gift cards remain catalogued-but-unbuilt, while
  `index.html:657` and `:1822` (dated the same day) show two of those three
  already live in code — either RETIRED.md predates same-day ports, or the
  two documents have already drifted apart. Not resolved here; a candidate
  first target for `tools/stale_count_check.js` (item 2 of the lens-panel
  manifest) once that instrument is pointed at prose claims as well as
  numeric counts.

---

## RULE group (A–D): Laws, Protocols, Memory/Suspension, Gate/Ledger

### Pair A–B: Laws ↔ Protocols

**SCOPE:** The richest pair in the RULE group. `LEARNED.md:3` — "Read at
boot, beside `PROTOCOLS.md`. PROTOCOLS carries what a session must DO."
Wired concretely in root `CLAUDE.md:18`: "Read `nesi/mind/LEARNED.md` in
full before doing substantive work." Law 31 is sourced directly to
`PROTOCOLS.md`'s "THE COUNSEL'S STANDING BUILD MANDATE" (`LEARNED.md:211-214`).
**Open item:** law 11's citation is judged "general" against its own test
in `LEARNED_PROVENANCE.md:63-66`, itself flagged there as "not yet a
filesystem fact... declared and complied with, not machine-enforced" — a
live discrepancy, unresolved.

**SPEC:** Same minimum-tabs shape as the original 24 (Overview · A: Laws ·
B: Protocols · Relationship/Interface · Sources), same self-contained,
theme-aware, provenance-tagged format.

**VERIFICATION:** 1. Re-transcribe `LEARNED.md:3` and `CLAUDE.md:18`
exactly. 2. Re-check law 11's "not yet a filesystem fact" status at build
time. 3. Confirm law 31's citation to the Counsel Mandate section still
resolves, given the Mandate's own 2026-08-27 retirement (D4 of the
original VE24 pass).

**PREFLIGHT MANIFEST:** Must read: `LEARNED_PROVENANCE.md` in full (law 11
and law 31 entries), `PROTOCOLS.md`'s Counsel Mandate section as it now
stands post-retirement. Risk/cost: **low**. **NOT AUTHORIZED — awaiting
Kevin's mark.**

---

### Pair A–C: Laws ↔ Memory/Suspension

**SCOPE:** `LEARNED.md:85-86` (law 13) cites "`~/.claude/CLAUDE.md`
(governing brief, now suspended 2026-08-15 — see that file's own
history)" as its own source. `LEARNED_PROVENANCE.md:79-86` (law 3's audit)
states directly: seam S4's "host file was suspended 2026-08-15, three days
after S4 was seated... Its current binding status is genuinely unclear,"
adding "This is not a law-3 finding — it is a separate, real gap, left
open rather than resolved by this pass."

**SPEC:** Same minimum-tabs shape. **This pair sits inside the suspension's
own escape-clause question** — same category as edges 9, 10, 12 of the
original 24, which each needed an explicit framing confirmation from Kevin
before drafting (read-only characterization of the suspension's shape,
never argument for or against it).

**VERIFICATION:** 1. Re-check whether S4's binding status has been
resolved since this pass. 2. Confirm law 13's citation is quoted, not
paraphrased. 3. Confirm the edge-file stays strictly descriptive per the
suspension's own escape clause.

**PREFLIGHT MANIFEST:** Must read: `LEARNED_PROVENANCE.md` in full (seam
S4 / law 3 entries), vendor `MEMORY.md`. **Open question for Kevin — touches
the suspension directly, same as the original edges 9/10/12:** confirm
that read-only characterization here is within the escape clause's
allowance. Risk/cost: **medium**. **NOT AUTHORIZED — awaiting Kevin's
mark, and awaiting this framing confirmation before drafting starts.**

---

### Pair A–D: Laws ↔ Gate/Ledger

**SCOPE:** **No direct textual/mechanical connection found.** Law 22 ("a
gate is a filesystem fact, not an exhortation") cites `runner.js:9-15` and
`world.html:433-452` as its examples — never `gate.mjs` or `admit.mjs`.
`admit.mjs:6-14`'s own design (refusal must carry a ground, admission must
carry `--made` not `--prevented`) is a live *instance* of law 22's and law
21's shape, but is never cited back to `LEARNED.md` in either direction.
The honest finding: structural resemblance only, not a sourced citation —
the weakest kind of pair per this corpus's own admission rule.

**SPEC:** If built at all, the file would need to state this plainly —
"the resemblance, not a citation" — rather than manufacture a link the
corpus itself never drew, matching the discipline edges 9/10/11 of the
original 24 already modeled ("named plainly... the honest job is naming
the gap, not inventing a pipeline that doesn't exist").

**VERIFICATION:** 1. Re-run a full-text grep of `LEARNED.md` for "gate,"
"ledger," "admit" to confirm no citation was missed. 2. Confirm
`admit.mjs`/`gate.mjs` carry no LEARNED.md citation in comments.

**PREFLIGHT MANIFEST:** Must read: `gate.mjs`, `admit.mjs`, `lib.mjs` in
full for citation-comment check. Open question: is a "structural
resemblance, no citation" pair worth building at all, or does it belong on
a list of pairs correctly left unbuilt? Risk/cost: **high**. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair B–C: Protocols ↔ Memory/Suspension

**SCOPE:** The most densely cross-referenced pair of all 18. `PROTOCOLS.md:24-27`:
"The 2026-08-15 suspension of the vendor `~/.claude/CLAUDE.md`, of
`MEMORY.md`, and of the machinery listed with them stands as Kevin set it,
and lifting any part of it is his in his own words." `PROTOCOLS.md:1`
names the vendor file as what it sits behind. No contradiction found
between the backup file's "inverted 2026-07-16... only the pointer" note
and `PROTOCOLS.md`'s description of an amended, brief-carrying version —
the two are sequential, not conflicting, but must be read together.

**SPEC:** Same minimum-tabs shape. **Also sits inside the suspension's own
escape-clause question**, same category as A-C above.

**VERIFICATION:** 1. Re-transcribe `PROTOCOLS.md:24-27` and `:1` exactly.
2. Confirm the sequence (pointer → amended-with-brief → suspended) reads
correctly as history, not as a live contradiction.

**PREFLIGHT MANIFEST:** Must read: vendor `~/.claude/CLAUDE.md` in full,
`CLAUDE.md.backup_pre-suspension_2026-08-15.md` in full. **Open question
for Kevin — touches the suspension directly:** same framing confirmation
as A-C and the original edges 9/10/12. Risk/cost: **low** (richest
material of the 18, least reconstruction needed) **once the framing
question is answered**. **NOT AUTHORIZED — awaiting Kevin's mark, and this
framing confirmation.**

---

### Pair B–D: Protocols ↔ Gate/Ledger

**SCOPE:** Direct and verbatim: `CLAUDE.md:74-79` (root) — "producing a
Session Bridge file is not complete until it is admitted —
`node gate/admit.mjs session_bridge_<date>_<slug> --made "..." --at "..."`,
run from `nesi/game2d/gate/`." A live mechanism wiring B's boot-path rule
to D's admit tool. **Real discrepancy surfaced:** `PROTOCOLS.md:407,611`
cite a *third*, distinct file — `tools/gates.py` (OPEN_GATES.jsonl) — for
a different NEEDS-KEVIN mechanism, not in Kevin's own D-group file list
and retired 2026-08-27 alongside the Counsel Mandate per
`PROTOCOLS.md:325`.

**SPEC:** Same minimum-tabs shape. Must disambiguate the two-gate (now
arguably three/four-gate, see cross-cutting) naming collision explicitly,
following the same discipline the original edge 13 already modeled.

**VERIFICATION:** 1. Re-confirm the `admit.mjs` citation still matches
live `CLAUDE.md` text. 2. Confirm `tools/gates.py`'s retired status and
whether it belongs in domain D's definition at all, or is correctly
excluded as retired.

**PREFLIGHT MANIFEST:** Must read: `PROTOCOLS.md` boundary B4 section in
full, `tools/gates.py` (to confirm retired/inert), `gate/gate.py` (root)
header. Open question: does the D-group's own definition need amending to
name `tools/gates.py` as a fourth, retired "gate" system? Risk/cost:
**medium**. **NOT AUTHORIZED — awaiting Kevin's mark.**

---

### Pair C–D: Memory/Suspension ↔ Gate/Ledger

**SCOPE:** **No direct connection found.** No occurrence of `gate.mjs`,
`admit.mjs`, `LEDGER.jsonl`, or `MARKS.jsonl` in either backup file,
checked by grep. `nesi/game2d/gate/` postdates the 2026-08-15 suspension by
weeks. `MEMORY.md.backup...md` uses the word "gate" generically in two
unrelated entries — thematic adjacency (both are persistence/record-keeping
organs) and a coincidental vocabulary overlap, nothing more.

**SPEC:** If built at all, same "name the gap plainly" discipline as A-D
above — this is the weakest pair of all 18 per the research agent's own
assessment.

**VERIFICATION:** 1. Re-run the grep across both backup files at build
time. 2. Confirm no third document bridges the two (e.g. a mark referring
to both).

**PREFLIGHT MANIFEST:** Must read: both backup files in full (partially
done this pass). Open question: same as A-D — is a category-adjacency-only
pair worth building, or worth naming instead as "correctly unbuilt"?
Risk/cost: **high**. **NOT AUTHORIZED — awaiting Kevin's mark.**

---

## READ group (E–H): Lenses, Skills/Instruments, Tools/Checks, Gift-pipeline

### Pair E–F: Lenses ↔ Skills/Instruments

**SCOPE:** Direct and deliberate. `game-craft.md:276-277` — "`.claude/skills/cold-walk`,
`.claude/skills/conservation-harness`, `.claude/skills/threshold-sweep`,
`.claude/skills/the-closing-check` operationalize this seat's own
knowledge into procedures any session can invoke by name." Four F-skills
explicitly extracted from one E-lens. `lens-panel/SKILL.md` also exists
solely to fan the five E-agents out — but is itself physically an F-domain
file, an overlap named in cross-cutting above.

**SPEC:** Same minimum-tabs shape. Must state the `lens-panel` overlap
explicitly rather than silently assign it to one side.

**VERIFICATION:** 1. Re-confirm the four named skill citations against
`game-craft.md` at build time. 2. Confirm no additional E→F extractions
exist beyond the four named.

**PREFLIGHT MANIFEST:** Must read: the four named skill files in full,
`lens-panel/SKILL.md` in full (done). Risk/cost: **low**. **NOT AUTHORIZED
— awaiting Kevin's mark.**

---

### Pair E–G: Lenses ↔ Tools/Checks

**SCOPE:** Two anchors, both weakened on inspection. `game-craft.md:322`
cites `tools/first_four.js` — **this path no longer exists**; the file now
lives at `nesi/game2d/tools/retired/first_four.js`, a stale citation.
`kevin-lens.md:177` cites `tools/marks.py:313` accurately, but
`kevin-lens.md` itself is CLOSED/retired (`kevin-lens.md:3`). A live tool,
`tools/framing_check_skills.js:81`, mechanically scans both E and F
(`TARGET_DIRS = ["skills", ".claude/skills", ".claude/agents"]`) for a
named defect pattern — the strongest *currently-live* anchor.

**SPEC:** Must name the stale `first_four.js` citation plainly, and flag
kevin-lens.md's citation as historical-record-only.

**VERIFICATION:** 1. Confirm `first_four.js`'s new path and whether
`game-craft.md` should be corrected (a fix outside this edge-file's own
scope, per the corpus's "selects, arranges, tags — does not decide"
discipline). 2. Read `framing_check_skills.js` in full.

**PREFLIGHT MANIFEST:** Must read: `framing_check_skills.js` full,
`game-craft.md` full (only grepped so far). Open question: is a pair
resting on one dead citation and one retired-agent citation worth
building, or does it need the live `framing_check_skills.js` anchor to
carry the whole file? Risk/cost: **medium**. **NOT AUTHORIZED — awaiting
Kevin's mark.**

---

### Pair E–H: Lenses ↔ Gift-extraction pipeline

**SCOPE:** One clean but thin anchor: `nesi.md:52` quotes verbatim from an
H-domain inbox file — "a sea monster that composts what gets knocked into
the lake..." (`inbox/seed_2026-08-11/NESI_fresh_articulation.md:33`). The
nesi lens agent's own self-description is sourced directly from H-domain
material.

**SPEC:** Same minimum-tabs shape. Given the thinness (one line), the
Relationship tab should state plainly this is a single sourcing
relationship, not a structural one.

**VERIFICATION:** 1. Re-confirm the quote's exact source and that no
other E↔H citation exists (grep all five agent files for "inbox").

**PREFLIGHT MANIFEST:** Must read: `nesi.md` full (done),
`NESI_fresh_articulation.md` full. Risk/cost: **low-medium** (thin
material). **NOT AUTHORIZED — awaiting Kevin's mark.**

---

### Pair F–G: Skills/Instruments ↔ Tools/Checks

**SCOPE:** The deepest, best-evidenced pair of all 18. At least four
direct 1:1 skill↔script authorship citations: `route-map/SKILL.md:19` —
"The harness is `nesi/game2d/tools/route_map.js`"; `cold-walk/SKILL.md:3`
— "Born from `nesi/game2d/tools/cold_walk.js` and `cold_walk_prepare.js`";
`conservation-harness/SKILL.md:3` — "Born from `nesi/game2d/tools/conserve.js`";
`threshold-sweep/SKILL.md:3` — "Built and registered the same day
(`tools/sweep_thresholds.js`)."

**SPEC:** Same minimum-tabs shape. With four strong sub-relationships
available, the Relationship tab should pick a representative set rather
than try to cover every skill↔tool pairing in the corpus.

**VERIFICATION:** 1. Re-confirm all four citations at build time. 2.
Check for additional skill↔tool pairs beyond the four named (a fuller
grep across all 14 skill files for `tools/` paths).

**PREFLIGHT MANIFEST:** Must read: the four named skill files in full
(three of four already grepped), a full-text grep of all 14 skill files
for `nesi/game2d/tools/` and root `tools/` paths. Risk/cost: **low**. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair F–H: Skills/Instruments ↔ Gift-extraction pipeline

**SCOPE:** Real connective tissue, but boundary-ambiguous.
`the-closing-check/SKILL.md:52` cites `inbox/gift_2026-08-27_10_freshet_test.md`
directly. `route-map/SKILL.md:103` — "Bring it to the gate as a card in
`inbox/` and leave the routing to Kevin." `external-extraction/SKILL.md:3,56`
names itself "Sibling to unrouted-gifts" — but `external-extraction` is
itself claimed as an H-domain member in this task's own scoping, so F and
H overlap on the same file (see cross-cutting). **Confirmed again: no
`unrouted-gifts/SKILL.md` exists locally** — only
`.claude/skills/unrouted-gifts_work/`, four rescued `.py` files, no
SKILL.md.

**SPEC:** Must state the F/H overlap on `external-extraction` explicitly,
and must say plainly that "the unrouted-gifts skill" is not a locatable
local corpus file, matching the original 24's edge-14/19 discipline on the
same finding.

**VERIFICATION:** 1. Re-confirm the two direct citations. 2. Re-run the
`unrouted-gifts` SKILL.md search fresh at build time (in case it changes).

**PREFLIGHT MANIFEST:** Must read: `the-closing-check/SKILL.md` and
`route-map/SKILL.md` in full, `.claude/skills/unrouted-gifts_work/`
contents. Open question: which side owns `external-extraction` for the
purposes of this file? Risk/cost: **medium-high**. **NOT AUTHORIZED —
awaiting Kevin's mark.**

---

### Pair G–H: Tools/Checks ↔ Gift-extraction pipeline

**SCOPE:** One strong, largely self-referential anchor:
`library_lens.js:12-13,26-27` — "the same extensions the unrouted-gifts
skill already treats as build-capacity signals" and "the same siting the
unrouted-gifts pass already uses for candidates awaiting a mark." But
`library_lens.js` is itself claimed under H in this task's own scoping,
even though it physically lives in G's tree (`nesi/game2d/tools/`) —
same overlap pattern as F-H. `tools/lens_usage_check.js:9,20,71` names
`unrouted-gifts` as a skill under audit, not as an invoked mechanism —
weaker, secondary evidence.

**SPEC:** Must state the G/H overlap on `library_lens.js` explicitly — the
file cannot honestly belong to only one side.

**VERIFICATION:** 1. Confirm no other `tools/*.js`/`*.py` file references
inbox/unrouted-gifts/full-development/external-extraction beyond the two
named. 2. Re-confirm `library_lens.js`'s domain assignment question.

**PREFLIGHT MANIFEST:** Must read: `library_lens.js` full (done),
`lens_usage_check.js` full. Open question: same domain-ownership question
as F-H, one level down. Risk/cost: **medium**. **NOT AUTHORIZED — awaiting
Kevin's mark.**

---

## BUILD group (I–L): game2d live build, Retired builds, Library-at-large, Session-bridge format

### Pair I–J: game2d live build ↔ Retired builds

**SCOPE:** The strongest pair in the BUILD group. Direct, repeated,
load-bearing citations inside the live source itself:
`index.html:353` — "world2d/scripts/filters_panel.gd is a complete,
already-built filter sub-game... never crossed... see
inbox/gift_2026-08-27_02_godot_filters_panel.md"; `index.html:657` cites
world3d's `deposit.gd:129-160`; `index.html:1822` cites world3d's
`sorting_tarp.gd`; `index.html:2264` cites `nesi_bench_v0/render.py:262-427`.
This is the gift-extraction pipeline feeding cited, ported mechanics
directly into I's live code, cross-indexed by `world2d/RETIRED.md`'s own
accounting of the same cards. **Live drift found:** `RETIRED.md` says 3 of
5 gift cards remain unbuilt, but two of those three already appear live in
`index.html`, dated the same day — see cross-cutting.

**SPEC:** Same minimum-tabs shape. Must name the RETIRED.md/index.html
drift plainly rather than silently pick one as current.

**VERIFICATION:** 1. Re-confirm all four citations against live
`index.html` at build time (actively edited). 2. Re-check `RETIRED.md`'s
own gift-card accounting against current `index.html` state.

**PREFLIGHT MANIFEST:** Must read: `world2d/RETIRED.md` full, the five
named gift cards, `index.html` sections around the four cited line ranges.
Risk/cost: **low** — evidence is abundant and already cross-indexed. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair I–K: game2d live build ↔ Library-at-large

**SCOPE:** `K_LENS_REPORT_2026-08-28.md` (real path:
`nesi/game2d/inbox/K_LENS_REPORT_2026-08-28.md`) is a purpose-built I↔K
mechanism — a scan of all 44 (or 45, disputed) K-domain directories
checking each against "already cited in index.html" counts per root. A
direct, mechanical link. **Scope caveat:** the scan only covers
`.gd/.js/.py/.html`, explicitly excluding most of K's real textual content
— "44 directories, 128 candidates" undercounts K's actual connection
surface.

**SPEC:** Same minimum-tabs shape. Should scope tightly to the
K_LENS_REPORT mechanism rather than attempt to characterize all of K's
relationship to I directly.

**VERIFICATION:** 1. Re-run or re-read K_LENS_REPORT fresh (K is large and
churns). 2. Confirm the 44-vs-45 count discrepancy's current state.

**PREFLIGHT MANIFEST:** Must read: `K_LENS_REPORT_2026-08-28.md` full
(done), `library_lens.js` full (done). Risk/cost: **medium** — real
mechanism, but K's boundary is large and only partially instrumented. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair I–L: game2d live build ↔ Session-bridge format

**SCOPE:** `SESSION_BRIDGE_2026-08-27_three_actions_and_the_return.html`
documents I-work directly — cites `refusal_check.js`, `admit.mjs`, and
confirms "`node tools/check_all.js` run fresh from `nesi/game2d/`... all
18 instruments ok." A real, dated mechanism, but **one-directional**:
`session_bridge_check.js` (L's enforcement instrument) contains only one
incidental mention of game2d (an analogy, line 15) — it never actually
checks I's state, only L's own file-freshness.

**SPEC:** Same minimum-tabs shape. Must state the one-directional nature
plainly (bridges document I; nothing in L's own checker reads I back).

**VERIFICATION:** 1. Re-confirm the session-bridge citations against
`index.html`/`check_all.js` current state. 2. Re-grep
`session_bridge_check.js` for any I-domain reference beyond the one
analogy.

**PREFLIGHT MANIFEST:** Must read: the named session-bridge file in full,
`session_bridge_check.js` full (done). Risk/cost: **low-medium**. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair J–K: Retired builds ↔ Library-at-large

**SCOPE:** A genuine, fully-documented reclassification, not just an
edge: `nesi_bench_v0/RETIRED.md` — "nesi_bench_v0 sat in a genuine fork —
Library-at-large (K, unrouted) or Retired build (J)... Kevin's ruling,
verbatim: 'retired.'" Confirmed by directory listing. Worth noting `nesi/bench/`
(still K-classified — `geometric_bench`, `composer`, `diamond`, etc.) is a
separate, similarly-named sibling tree, easily confused with `nesi_bench_v0`.

**SPEC:** Same minimum-tabs shape. Should name the `nesi/bench/` vs
`nesi/nesi_bench_v0/` naming-confusion risk explicitly.

**VERIFICATION:** 1. Re-confirm `RETIRED.md`'s ruling text verbatim. 2.
Independently verify edge 12's "declarative artifact, kept whole"
convention (cited but not independently checked this pass) is applied
consistently to this reclassification.

**PREFLIGHT MANIFEST:** Must read: `nesi_bench_v0/RETIRED.md` full (done),
the original edge 12's file (`VE24_12_memory_retired_builds.html`) for the
convention it documents. Risk/cost: **low**. **NOT AUTHORIZED — awaiting
Kevin's mark.**

---

### Pair J–L: Retired builds ↔ Session-bridge format

**SCOPE:** **No mechanism-level connection found.** `world3d/RETIRED.md`
and `world2d/RETIRED.md` neither cite nor are cited by the session-bridge
format or its checker. The only tie is temporal coincidence: both
`world2d/RETIRED.md` and `nesi_bench_v0/RETIRED.md` were written the same
day (2026-08-27) L's mandatory-bridge rule was added to `CLAUDE.md`. No
session bridge found (of four checked) documenting the retirement acts
themselves — though the agent flagged this as worth a closer pass before
concluding "no connection" definitively.

**SPEC:** If built, must state plainly this is likely "category adjacency
only" pending a closer check, per the honest-gap discipline modeled by
several of the original 24's edges.

**VERIFICATION:** 1. **Highest priority: check whether any 2026-08-27
session bridge documents the world2d/nesi_bench_v0 retirement acts** — not
confirmed absent, only not found in the four checked this pass. 2.
Re-grep all RETIRED.md files for "session bridge"/"SESSION_BRIDGE."

**PREFLIGHT MANIFEST:** Must read: all SESSION_BRIDGE_*.html files in full
(only partially read this pass), both RETIRED.md files in full. Open
question: is this genuinely "no connection," or did this pass simply not
find the connecting bridge file yet? Risk/cost: **medium-high**. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

### Pair K–L: Library-at-large ↔ Session-bridge format

**SCOPE:** A real, dated, quoted mechanism:
`SESSION_BRIDGE_2026-08-27_three_actions_and_the_return.html:411` directly
documents a K-domain artifact — "`nesi/game2d/inbox/K_LENS_REPORT_2026-08-28.md`
... 128 unrouted candidates surfaced across the 44 scanned K directories."
L's format being used specifically to record a K-domain scan result.
**Dating inconsistency named in cross-cutting**: the K-report is dated one
day after the bridge that describes it.

**SPEC:** Same minimum-tabs shape. Must name the dating inconsistency
plainly rather than silently resolve it.

**VERIFICATION:** 1. Re-confirm the citation and both files' actual
timestamps/filenames. 2. Check whether the date mismatch is a filename
typo or a genuine cross-midnight pass.

**PREFLIGHT MANIFEST:** Must read: the named session-bridge file in full,
`K_LENS_REPORT_2026-08-28.md` full (done). Risk/cost: **low**. **NOT
AUTHORIZED — awaiting Kevin's mark.**

---

*All 18 entries above are planning material only. Every one ends "NOT
AUTHORIZED." Four (A-C, B-C on the suspension directly; A-D, C-D as
weakest/no-direct-connection pairs) carry an additional open question
before drafting should start. This document is unranked and whole per law
30 — nothing above has been curated down to a recommended subset, and the
18 pairs are not in priority order. Two pairs (A-D, C-D) were found to
have no direct sourced connection at all — named here as a live finding
for Kevin to weigh, not defaulted toward either building thin material or
dropping them.*
