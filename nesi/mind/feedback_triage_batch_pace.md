---
name: feedback-triage-batch-pace
description: "Kevin can handle 4 separate decisions per turn in a decision session, not just 2 — batch size preference for triage/review sessions"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d1a8f774-0fc8-41c7-aad9-5777e9116c87
---

Kevin's batch-size preference has scaled up as the triage session progressed: 2 → 4 → 8 candidates per turn.

**Why:** Said directly during the [[project_intake_triage_2026-07-15]] 54-file triage session. First: "I can handle 4 separate decisions in each turn" (after a 2-candidate batch). Later, with ~25 of 54 files still remaining: "can we do 8 marks per surface now? theres alot to get through... batches of 8 makes better use of kevins attention." The direction is toward larger batches as a session's total item count grows and he's warmed into the rhythm — not a fixed number.

**How to apply:** When running a multi-item decision session (triage sweeps, substrate pattern marks, gate dispositions), default to 8 candidates per turn for large sweeps (dozens of items) once Kevin has done a few smaller batches to establish rhythm; use 4 for shorter or denser sessions. Watch for him asking to scale further. He also answers by typing plain replies directly in chat ("compost 1, promote 2...") rather than using the widget's click-to-copy buttons — the widget (toggle switches per [[feedback_widget_toggle_ui]]) is a staging/display surface, not his actual input method for this kind of session.

**Update 2026-07-15 (pattern-marks session):** on the denser [[project_substrate_brief_pattern_marks]] pile, Kevin's actual reply was a blanket "proceed as marked" per batch rather than naming each pattern's mark individually — he's accepting the widget's Converger-matched toggle defaults wholesale when he has no override. Both modes are live: per-item typed marks when he's overriding or differentiating, blanket "proceed as marked" when the defaults already match his read. Treat "proceed as marked" as authorization to apply every toggle default shown in the most recent widget, then close the file and move to the next batch without re-confirming per pattern.

**Update 2 (same session, ~7 brief-batches in):** Kevin named the batch cadence itself as too much load — "too many for kevins attention on this task... sort the pile into batches kevin can approve in 3 or 4 decisions total." This is a step beyond widening batch size: he wants the WHOLE remaining pile (18 briefs, ~89 patterns) collapsed into 3-4 bulk decisions, not more per-brief batches at a larger N. The fix: read every remaining source myself (parallel Explore agents for speed), bucket every item by its own already-written verdict (promote-ready / hold / compost / already-resolved), present one decision card per bucket with the full item list inside (collapsed, click to expand), and apply the whole bucket on one confirmation. This is the ceiling signal for [[feedback_triage_batch_pace]] — when a pile is large and mostly mechanical (verdicts already computed by an upstream pass), don't keep scaling batch size; restructure to bulk-decision mode instead. Watch for this same request on any future large mechanical-review pile.
