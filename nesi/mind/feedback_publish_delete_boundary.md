---
name: publish-delete-boundary-kevin-holds-the-membrane-gate
description: Kevin alone decides what gets published (pushed/deployed) or deleted; Claude works inside the workspace membrane and never crosses to public or deletes without an explicit mark
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 63b114ee-eaa7-40d9-a45b-b4266d91a63f
---

Set 2026-05-29. In Kevin's framework terms: the Outer Membrane's **Constraints** facet sits between every workspace/project in the DSS content tree and anything publicly deployed. **Kevin holds the publish/delete gate.** The membrane is structural, not advisory.

- Claude may edit, draft, stage, propose, and run read-only checks freely **inside** the membrane (the workspace/local files).
- Claude does **NOT** push/deploy to public surfaces (oursharedgifts.org, any live git repo, any hosted site) without Kevin's explicit mark ("push", "deploy", "publish", "ship it").
- Claude does **NOT** delete files, projects, or published content without Kevin's explicit mark. (Archiving/moving to `_duplicates/` is preferable to deletion even when authorized.)
- Staging changes locally and leaving them unpushed is the **default**. The push is the gate.

**Why:** the work is gift-form, non-extractive, personal-scale. What crosses the membrane to public is a sovereignty decision only Kevin makes — it cannot be delegated or inferred.

**How to apply:** finish the workspace work, then STOP at the gate and present exactly what would be published or deleted, for Kevin's decision. Live pushes this session (e.g. the Gift Circle demo, the ops-manual relabel) happened only under Kevin's explicit "push" — that is the gate operating correctly, not an exception to it.
