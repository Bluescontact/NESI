# STAGED — the gameplay debug session

> **■ OPENED AND RUN, 2026-09-01** (the keeper's word: "you dont need me to
> walk the game... you can play test it without me"). The walk ran whole;
> one real bug found by playing and fixed (the blind promise held only in
> the document — see the blind filter in the build). Then the keeper's
> scale bar arrived (750–2000 words) and the scale ladder ran: constants
> swept clean at 128 sentences. **Still open from this brief:** the
> 33-page dispositions (F1 — a design fork), the three in-page checks
> (F3), gift cards 5/11/14 (F4), and the second-session naming (F2 — the
> keeper's). The text below stands as staged, per the layering convention.

**Held, per crystal 10: staged with its named condition.** The keeper's mark,
2026-09-01: *"stage the next work session to debug, and resolve gameplay and
dead ends inside of nesi."* This document is the staging — the found-work
already on the table so the session opens moving, not searching. **The hold's
condition: it opens when the keeper sits down to play, or says go.**

**The bar the session works against, the keeper's own words (2026-08-10):**
*"I haven't stepped into Nesi yet, because i know i'll be frustrated by my
lack of ability to interact and name what i want inside the world. Its
nowhere close to ready to hold kevin yet."* Debugging dead ends is in
service of that: the session succeeds when the world can hold its keeper.

---

## The found-work (reconnaissance run at staging, 2026-09-01)

**F1 · The front door is an island — and that may be law.** `index.html`
links out to ZERO of game2d's 34 html pages. The twelve-seat door was
retired for being *"a door with a bench of thirteen links leading out"* —
so zero exits is plausibly the standing design, and the dead-end question
inverts: not "links that route nowhere" but **33 pages with no route in.**
Each needs one disposition: live organ (route it or leave it as a
deliberately separate surface, stated), experiment kept as record (say so
in the file), or compost-in-place. The rough families:
- *entry lineage:* day_one, lattice, the_page/the_weave (redirects), the
  pre-lattice backup — records, likely fine as-is.
- *the tetra dreams A–E* + dream_A/B/C — experiments; KNOWLEDGE doc §8
  already says which-develops-further is the keeper's call.
- *the level body:* ascent, descent, level_one, world, tank, field, node,
  tiles, traversal, crystal, daily, decisions, options, nesi,
  nesi_consolidated, regathered — the door_check-walked system. **How the
  level body and the writing surface relate is the largest unresolved
  gameplay structure: two play surfaces, one world?**

**F2 · The returning player's water — the most load-bearing gap, already
named.** THE_CATCHMENT §12: *"whether it is still flowing, has settled, or
has dropped is unnamed — and this decides what a second session even is."*
Today every session opens on the full retained document. Resolving this is
a naming first (the keeper's), then a build.

**F3 · Three checks wait for a page.** answer_check ("every act answers a
hand"), daily_walk (nineteen properties of the writing surface),
store_guard (writing identical before/after any work) run only in-page and
are never exercised headless. Wire them to the live surface, or state
where they stand. store_guard especially: it is the player-trust check.

**F4 · Three carded gameplay features, never routed.** gift_05
*arrived_edit*, gift_11 *set_it_down*, gift_14 *rule_for_the_sheet* — the
only gift cards never admitted. Each is a candidate interaction affordance,
which is exactly the "ability to interact" gap in the keeper's bar. Read
the three cards early in the session.

**F5 · Interaction affordances known thin.** From the record: refocusing
inside grounds was caught missing once already ("i dont see anyway to
interact with the weave") and fixed — the same walk should be made through
every ground: tarp, garden, categories, words, world face. The test is the
keeper's bar verbatim: can a hand *name what it wants* from inside?

## The method

1. **Play before reading.** Serve locally (`python -m http.server` in
   game2d — file:// renders static), walk as a hand: write, connect, drag,
   TAB the case, switch every ground, try to get stuck. Log every dead end
   as one line: *where · what the hand wanted · what happened instead.*
2. **Sort the log by jurisdiction.** Mechanical (broken/missing wiring):
   fix in-session, test live, check_all before commit — the projection
   build is the template (bugs found by playing, not by review).
   Design forks (what should a second session be; which pages live):
   stage for the felt read, never decided by the session.
3. **Dispositions for the 33** (F1) recorded in one pass — a line each,
   nothing deleted, layering convention throughout.
4. **Close by the projection:** verify state by fresh replay after the
   session's changes; run the full suite; catch what crossed at the gate.

## What is the keeper's, not the session's

The second-session naming (F2) · which tetra dreams develop (§8) · whether
the level body and the writing surface merge, link, or stand apart (F1) ·
the three cards' marks (F4) · any new ground or affordance that reads
content rather than volume (barred by the catchment naming regardless).

*Staged 2026-09-01. Held. Opens on the keeper's word.*
