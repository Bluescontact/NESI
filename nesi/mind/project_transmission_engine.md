---
name: transmission-engine
description: "Claude Code skill at ~/.claude/skills/transmission-engine/ that converts a finished article (markdown) into a folder of Substack-ready artifacts — recognition pattern (SVG), header-image prompt for Imagen 4, pull-quote share-cards. Built 2026-05-25."
metadata: 
  node_type: memory
  type: project
  originSessionId: f1d39e5a-72b0-47f1-8fd0-8e1db80edf36
---

Skill location: `C:\Users\KMEAR\.claude\skills\transmission-engine\`

**Pipeline:** structural read → symbolic distillation (SVG via geometry-keyed templates) → Imagen 4 header prompt → pull-quotes. Layered infrastructure stack: Layer 1 rasterization (`bin/rasterize.ps1` — ImageMagick at `C:\Program Files\ImageMagick-7.1.2-Q16-HDRI\`, with Inkscape and rsvg-convert as alternates), Layer 2 fonts (`bin/verify_fonts.ps1`, requires Cormorant Garamond / Cinzel / Inter — NOT yet installed as of 2026-05-25), Layer 3 asset library (`assets/defs.svg` — single source of truth for gradients/filters/glows). Stages 6-8 (generation models, composition, publication ledger) planned in `docs/pipeline_v2.md`.

**Geometry library** (templates/): threshold, distinction, circuit, source_emanation, tetrahedron, vertex_mapped, layered, filter. Engine picks one per article based on the load-bearing structure.

**Visual register anchor:** [[project_genesis_seed_and_os_view]] — the Minimum System 7-pack at `DSS content/frameworks/minimum-system/`. Dark field, luminous gold geometric figure, sans-serif, single named concept, minimal text. Output must be indistinguishable from work in that pack.

**Header image model:** calibrated for Imagen 4 / Gemini. Prompt template at `templates/header_prompt_template.md`. Locked aesthetic block — vary only subject + mood.

**Layer 6 generation — BUILT & LIVE (2026-06-08):** Replicate wired and working end-to-end. `bin/generate_image.ps1` (dispatcher: flux-pro/flux-dev/recraft/imagen/ideogram; auth ONLY from `$env:REPLICATE_API_TOKEN`, never in a file/git) + `models/registry.md` (model table + per-piece surreal dial + warm teaching-ground prompt preset). Token lives at Windows **User-scope env var** (Kevin set via `setx`, never pasted — the active one ends `…Bhdw` is clean; two earlier pasted tokens were exposed-in-chat and disabled). To run, load by reference: `$env:REPLICATE_API_TOKEN=[Environment]::GetEnvironmentVariable('REPLICATE_API_TOKEN','User')` then call the script — no restart needed. **Must run with `dangerouslyDisableSandbox:true`** (needs outbound net to api.replicate.com). PS 5.1 chokes on unicode in scripts — keep output ASCII. First ground generated: `substack/witnessing-without-merging/ground.dev.png` (flux-dev, ~$0.003) — strong. flux-dev ~$0.003/img iteration, flux-pro ~$0.04 finals.

**COMPOSITION LAYER (engine Layer 7) — capacity demonstrated 2026-06-08.** The infographic capacity = browser-composited layer stack: illustration (generated) + scrim + vector structure + typography, fused live by the browser; PNG export = screenshot the container. Demo: `substack/witnessing-without-merging/composed.demo.html` (reused an existing ground, $0 new). Kevin's correction that drove this: flat SVG diagrams ≠ infographics ("you don't have that capacity yet").

**VISUAL TRANSMISSION INVARIANTS — tetrahedral knowledge doc (`docs/visual_transmission_invariants.md`), built 2026-06-08** at Kevin's invitation to *develop the skill myself, not copy a style*. 4 vertices (STRUCTURE/what it says · LEGIBILITY/can it be read · GUIDANCE/where the eye goes · RESONANCE/does it land+stay) + 6 edges (craft couplings; edge 5 Legibility×Resonance = the scrim, predicted what the demo found) + 4 faces (failure modes). **Key diagnosis: flat diagrams sat on the omit-RESONANCE face** — structured/legible/guided but dead; that's exactly Kevin's complaint. Resonance = meaningful imagery/metaphor (dual-coding, picture superiority). The 4 vertices map onto the 4 composition layers. Grounded in Tufte/Gestalt/Paivio/Mayer research.

**SUBSTRATE GEOMETRY — canonized 2026-06-09 (`docs/substrate_geometry.md`), the generative principle above the invariants.** Kevin's insight: the asset is tetrahedral, the system is tetrahedral, they're NESTED — fractal/self-similar across scale. The doc names the **scale-stack of nested minimum-systems**: Framework (Differentiation·Connection·Boundaries·Architecture) ⊃ System/transmission (Geometry·Altitude·Stance·Modality) ⊃ **Substrate/page (WORD·IMAGE·SHAPE·COLOR = the 4 cognitive-metabolic channels)** ⊃ Asset (Structure·Legibility·Guidance·Resonance) ⊃ Mark/glyph. Four because 4 vertices is the minimum that *encloses a volume* (closure, no gap). Two laws: **Closure** (serve all 4 channels at every scale or a reader-type is locked out — the 4 faces = wordless-poster/dry-doc/flat-list/monochrome) + **Self-similarity** (every part carries the whole's shape; coherence emerges from recursion, not styling). Color carries the framework tetra into the page (not decoration). **Drafting method = recursive closure** (read geometry → instantiate page-tetra across all 4 channels → recurse into sections/assets/marks → close every face at every scale). Falsifier: take any part out, does it still carry the whole? Failure smell = "a pile of good assets" with no page-scale tetra imposed (= The Model now, pre-resynthesis). **NEXT: draft the resynthesis of The Model against this principle for Kevin's mark before tearing up the live page.**

**INFRA PIVOT — LOCAL generation (Kevin 2026-06-08): WATTS yes, MONEY no.** Replicate's per-image cost is out for ongoing work; go local, ≤5–10 min/run so the laptop stays usable. Plan (in the invariants doc Part 2): ComfyUI + a fast low-VRAM model (Flux.1 schnell GGUF / Z-Image Turbo / SDXL-Turbo — fits 6–8 GB, minutes), driven headless via ComfyUI API from a local script mirroring `generate_image.ps1` but pointed at localhost. Composition layer stays browser-side (already free). Replicate kept only as rare-finals fallback under the $20 brake. **OPEN: need Kevin's laptop GPU/VRAM to pick the model tier.**

**SPEND BRAKE (Kevin set 2026-06-08 — non-negotiable):** (1) **Manual authorization per paid call** — NEVER fire a generation without Kevin's explicit go-ahead for that specific run; state estimated cost first; no batch-firing on assumed permission. (2) **Hard $20 cap wired in** — `generate_image.ps1` reads `.spend_ledger.csv`, sums prior spend, aborts (exit 3) BEFORE calling Replicate if the run would cross `-Cap` (default $20). Each success appends cost + prints running total. As of 2026-06-08: **$0.021 spent of $20** (7 flux-dev drafts for the witnessing grounds). Policy documented in `models/registry.md` spend-governance section. This is THE GOVERNOR pattern applied to the engine; the $20 is Kevin's calibration zero.

**First validated run:** `DSS content/recognition-threshold/` produced from `recognition-threshold.md` on 2026-05-25. Geometry: threshold. Functions as both build-validation and first publishable artifact set.

**Second-altitude reframe (2026-06-08, spec only — `docs/explanatory_rendering.md`):** Kevin named the gap — the engine only renders at *recognition* altitude (compress to a glyph), nothing for the **cold reader** who needs *comprehension* (infographics / visual learning styles). Ran the tetrahedron on the engine itself: transmission has **4 irreducible axes** — Geometry (what shape, the only one v1 varies) · Altitude (how compressed) · **Stance** (where the reader stands: cold/practitioner/recognizer — the missing vertex; the engine sits on the "omit-Stance" face) · Modality (channel: see/follow/use/feel — `use` opens "tools not pages"). 6 edges = couplings; load-bearing one is **Stance governs Altitude** (select render off stance, NOT geometry). 4 faces = failure-mode diagnostic (omit a vertex). New: teaching register kept strictly separate from recognition register (`teaching_defs.svg` sibling, never edit `defs.svg`) — rhymes with OSG `v5-visual`. New gate = comprehension test (a never-read-it reader reconstructs the core distinction), run on a real cold reader. **Gated on Kevin:** felt-read the teaching-register base palette (warm-light vs dark+color-code); build sequence starts with one stance/one style/one article/one live reader.

**First cold-reader render BUILT (2026-06-08):** `substack/witnessing-without-merging/teach.cold-reader.html` + `teaching-notes.md` (the Stage-1 extension reads on a real article). 4 visuals on one page building the comprehension ladder: (1) example-pair — the membrane shape + the real afternoon instance; (2) three-position comparative — wall/HOLD/merge; (3) the gate mechanism — safe vs unsafe body, intervention is upstream; (4) two body-questions (the tell + the brake). **Both palettes live behind an in-page toggle** (warm-light default / dark = framework-gold continuous) so Kevin picks the register by feel — resolves the palette gate by demonstration, not guess. Render verified (preview server "teach" :8158, both palettes). **Still un-run: the comprehension test on an actual cold reader** — that's the real proof and needs a human, not Claude. Instance-not-model honored (first-person afternoon, footer names pattern-as-gift).

**Why:** Substrate production was outpacing transmission infrastructure. The recognition-threshold essay itself diagnosed that recognition requires the work be picked up, and friction-cost determines pickup rate. Engine exists to lower friction. Sits downstream of [[project_register_audit]] in the planned tool sequence.

**How to apply:** When Kevin says "run this through the engine," "make a recognition pattern for this," or "build artifacts for this post," invoke the skill against the named markdown file. Output goes to `<article-dir>/<article-slug>/`. The skill is gift-form internal infrastructure — does not pad outputs, does not add commentary to artifacts.
