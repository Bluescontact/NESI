---
name: reference-osg-deployment-membrane
description: The deployed work lives at oursharedgifts.org (repo github.com/Bluescontact/oursharedgifts) — beyond the membrane; the DSS content workspace is within it
metadata: 
  node_type: memory
  type: reference
  originSessionId: c2759f13-b8cb-4dde-9815-72a1a19046b8
---

The membrane is concrete — it is the deploy boundary, not an abstraction.

- **Beyond the membrane (deployed / public):** the live site **www.oursharedgifts.org** and its backend repo **github.com/Bluescontact/oursharedgifts**.
- **Within the membrane (private substrate):** this `DSS content` workspace, where we compost. "We are working within the membrane" — active work stays in the substrate; the deployed face is downstream of the gate.

Crossing the membrane = pushing / deploying to OSG. That is Kevin's gate alone (see [[feedback-publish-delete-boundary]]); nothing crosses without his git push.

**This resolves the map↔territory drift (tensions T1, [[project-the-governor]]).** Memory-referenced artifacts not on this disk — OS.html, the OSG Library surface, the Constitution / "Current" page, `/refusals`, and likely kit/ frameworks/ Project Lab/ — are the **deployed** versions living beyond the membrane in the OSG repo/site. They are not missing; they were the public face seen from inside the private workshop. **Do not prune memory for them — they are relinked here.**

Our substrate work (the Governor, negative_workspace, the crystallization drafts, the Constitution scaffold) feeds the OSG surfaces *across* the membrane on Kevin's mark. The relationship is [[feedback-tools-not-pages]] + the eliminate_the_gatekeeper pattern: the membrane is gated, the substrate behind it flows freely. See [[project-osg-personal-scale-frame]], [[project-genesis-seed-and-os-view]].

## Crosswalk — memory ref → deployed location (relinked 2026-06-04, repo read read-only)

The deployed site is **OSG v4** (`OSG_V4_SPINE.md`) — memory's "ship OSG v3" is **stale**; v3 is superseded, v4 is live. Top-level deployed pages: `index.html`, `about.html`, `current.html`, `framework.html`, `back/`, `automation/`, `framework/{edges,faces}/` (the tetrahedron as pages), and a large `library/`.

| Memory's stale ref (within / older) | Deployed location (beyond, OSG v4) |
|---|---|
| OS.html / the framework view | `index.html` · `framework.html` · `framework/edges/*` + `framework/faces/*` |
| _PERSONAL_OS.md / "OSG v3" | `OSG_V4_SPINE.md` — deployment is at **v4**, live |
| frameworks/register_audit.md | `library/register-audit.html` + `library/transmissions/register-audit/` |
| Project Lab/ SOURCE_LIBRARY + PATTERNS_AND_VOICE | `library/canonical-syntheses/` (operating-principles, substrate-cartography-v2, …) |
| Architecture_of_Coherence (manuscript) | `library/architecture-of-coherence.md` + `.pdf` |
| kit/ (RI Kit v1) | `library/operations-manual-field-edition.md/.pdf` + toolkits (`assets/cards/toolkits*`) |
| the Library | `library/` (framework, syntheses, transmissions, visuals, meta-tetrahedron, glossary, lineage, case-portraits) |
| /refusals (live) | `library/refusal-as-generative-act.html` + `assets/cards/refusals` |

**Divergence runs both ways (different versions is fine — Kevin, 2026-06-04):**
- *Deployed (beyond) is ahead* on the published framework/library — full transmissions, visuals, meta-tetrahedron, canonical syntheses, OSG v4 spine.
- *Substrate (within) is ahead* on the newest work, NOT yet deployed: THE GOVERNOR, the negative_workspace, the 8 crystallization patterns, the Constitution scaffold. These are the next to cross the membrane on Kevin's mark.

**Full deposition staged (2026-06-05):** a frozen copy of the whole corpus for a full metabolic pass lives at `_INTAKE/raw/FULL_DEPOSITION_2026-06-05/` — `published_v4/` (the deployed OSG v4 library, cloned) + `local_substrate/` (the within-membrane workspace snapshot) + `_NEW_SESSION_KICKOFF.md` (self-contained instructions). Kevin runs the metabolize-everything pass in a separate session, pointed at the kickoff. The prior `_INTAKE/raw/` piles (incl. the sealed IUEC/DOE closed ledger) are NOT in the snapshot — already in place, sealed for metabolizing.

**OSG V5 staged (2026-06-05):** a full V5 website synthesis lives at `_INTAKE/osg_v5/` (start at `_V5_README.md`) — the phase transition from v4: "V4 is the framework on a site; V5 is the Seed booted at the scale of one life." Kevin's personal instantiation of the Genesis Seed (upstream/general → downstream/personal). Spine + architecture + instantiation narrative + a built `governance.html` (gated scaffold, 0/8 slots checked). Holds the deploy filter — what governs the document crosses; the Governor + shadow registry stay within. Staged, nothing deployed; the push is Kevin's. The gating dependency is promoting `loop_closure` to master (downstream of ratifying `consented_ledger` first).
