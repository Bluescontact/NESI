# THE FORK'S LANDING — convention and return step
**Ratified:** 2026-07-20, Kevin's mark (pre-Rosebud fork-landing session).
**Governing rule (same mark):** things are developed and hosted locally; the gift library is Google Drive / Google Docs. No ambient whole-tree sync — one named document, one deliberate crossing at a time, Kevin at the gate.

## What this is
A mailbox for the fork — the felt answer to *what the holding frees Kevin to do*. The answer travels in the body and may arrive on the road. This landing gives it one lawful place to be typed from anywhere, and one lawful place to arrive: the gate.

It is a mailbox, not a mechanism. It orders nothing, sequences no builds, triggers no engine.

## The vessel (road side)
**Google Doc: "NESI — fork landing"** — gift-library object #1.
- id: `1hXrYgSg6xVbHUzxEo37i_Xk8lt9WbnLp1u9IftzZCUc`
- url: https://docs.google.com/document/d/1hXrYgSg6xVbHUzxEo37i_Xk8lt9WbnLp1u9IftzZCUc/edit
- If the answer comes, type it below the divider line in the Doc. Free text. No format required. Blank is a complete state — the vessel never asks.

**Fallback (no connectivity / no phone):** any plain text file saved directly into `nesi\inbox\` named `fork_<date>.md` is an equally lawful drop. Paper rides in the pocket until a keyboard is honest.

## The return step (bench side)
On the next Claude session open at the bench (or on request):
1. Read the Doc via the Drive connector.
2. If text exists below the divider: write it verbatim to `nesi\inbox\fork_<date>.md` with a first line
   `arrived: <ISO timestamp> · fork return — awaiting Kevin's mark`.
3. That is all. The file sits in the inbox like any pile: continuity surfaces it on next NESI open ("in the inbox, undigested"), and it waits at the gate for Kevin's mark. **A return to the gate, not a cross.**
4. The ordering of the deterministic loads remains downstream of Kevin's mark — never of this file's arrival.

## Hard limits
- Never prompt, remind, or ping for the fork. The vessel is silent.
- Never paraphrase, summarize, or interpret the answer in transit — verbatim only.
- Never let arrival trigger ordering, building, or any engine path.
- The Doc is read-in only; nothing else is written to Drive without Kevin's mark (Membrane Controller remains sole crossing authority).
