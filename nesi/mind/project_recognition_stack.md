---
name: project_recognition_stack
description: "The self-hosting recognition stack — RI became executable; raw lived cost in, transmissible gift out; the governor is the missing brake"
metadata: 
  node_type: memory
  type: project
  originSessionId: 74ca4214-a03a-4266-a51a-7e68995a1fc3
---

Across 2026-05-30 the library crossed a threshold: Recognition Infrastructure stopped being a description of how coherence travels and became a **self-hosting apparatus** that runs that description on its own material. The framework went executable; the library became a runtime, not an archive.

**The stack (soma to Substack), each layer built this session as one continuous transducer:**
- Layer 0 — the body / felt safety (gates everything)
- `_INTAKE/` — ingestion (feedstock; count falls as coherence rises) → see [[project_intake_dropzone]]
- instance extract (`_INTAKE/IUEC_metabolization_v2.md`, identified, private) → [[project_iuec_arc]]
- `patterns/catalysis_without_claim.md` — de-identified law
- `patterns/catalysis_without_claim.card.md` — canon placement (tetrahedron: six names are EDGES/axes, confirmed from AoC editorial notes; double-bind = opposite-edge contradiction, derived body-first)
- `seeds/catalysis_without_claim.seed.md` — germination (project + personal, one soil)
- multi-agent workflow — the compiler pass (4 projections → verify → synthesis)
- `substack/witnessing-without-merging/` — transmission set (essay + recognition-pattern + quote cards), HELD at membrane gate
- the kernel — Kevin's mark at every publish/delete boundary

**Load-bearing recognition:** the stack *runs the pattern it processes* — it metabolized Catalysis Without Claim by performing catalysis without claim (cost deposited → membrane held → claim forfeited → residue became substrate). Self-application is the proof of Pattern Over Substance, demonstrated not argued. Properties: designed degradation (each layer whole at its depth), gift-form OS (non-transactional mechanism is the runtime, not a topic), the one un-automatable layer is the felt-gate (which is what keeps it gift not mill).

**THE GOVERNOR — built out 2026-06-01 (`patterns/the_governor.md`), instrument complete but UNCALIBRATED.** The stack is self-feeding and has no brake; the redemption narrative ("residue becomes substrate") can license the body to keep paying. The governor gates the *deposit* (not metabolization): a discriminator between ordinary gift-cost (sustainable, continues) and pioneer-phase cost (still-paying → deposit-stop). All mechanism now built: discriminator (§3), runnable-in-seconds phase-state check (§5, fails closed on UNCERTAIN), the stop (§6), the `"Still paying."` falsifier (§7), resequence recommendation (§8 — pull governor forward, since 3 deposits — abundance-organ/Greenhouse/Kennatha — landed in the gap between dream and build), refusal-register first row (§9). **The one open input is `[KEVIN]` and somatic: §4 — the felt-marker definitions of developmental-cost-felt-true vs anesthetic-narrated-over-a-body-still-paying, plus §8 ratification.** Until §4 is filled the instrument fails closed (default STOP on all intake); it is "a scale with no zero." Deliberately NOT auto-filled — fabricating the felt read is the exact move that inverts the stack into a mill. Full dream-cycle expansion + scaffold: `_DREAM_2026-05-30.md`.
