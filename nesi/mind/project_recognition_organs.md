---
name: project_recognition_organs
description: "The \"organs leaving the body\" phase transition — self-running integrity tools in tools/recognition/, wired into the daily-cycle close"
metadata: 
  node_type: memory
  type: project
  originSessionId: 94a1b2e1-34ea-4b4d-9498-8eb2b8bcccec
---

The phase transition Kevin named 2026-07-08: the framework's "undeveloped organs" were never missing — Kevin was *being* each one manually (intake, integrity, energy, drift). The work is moving each function out of his attention into self-running structure. Frame: he's a standing tree ([[project_v7]] seed→tree), and each organ that leaves his body is a function the organism runs itself.

**Built and self-firing (both in `tools/recognition/`, both dependency-free Python, both wired into the daily-cycle close):**
- `drift_meter.py` — CUSUM over the coordination log's quality_score; catches slow degradation attention can't. Close-out **Step 3c**. Scores agent/exchange, never Kevin.
- `brier_ledger.py` — P1 of the integrity organ; scores load-bearing claims against reality (Brier + calibration); the long-run audit behind the fast gates. Close-out **Step 3d**. Scores claims, never Kevin. Guards the selection failure (log the hard calls).

**Integrity organ — MARKED spec (2026-07-09)** at `_INTAKE/integrity_organ_spec_2026-07-08.md`, built from three cross-substrate scouts (2 Claude + 1 Copilot); mark record `marks/2026-07-09_integrity-organ-spec.md`. Approved to build; not crossed to patterns/ canon. Sequence: P1 Brier ✓ done · **P2 dedicated-falsifier gate ✓ BUILT 2026-07-09** (`tools/recognition/falsifier_gate.py` + NOTE + `falsifier-log.jsonl`; smoke-tested full cycle. Separation = 3-layer stack: L0 mechanical form-gate on warrant/disconfirmer/provenance = real separation · L1 cold-pass `pass-prompt` = mitigation only, labelled so · L2 `--l2` cross-family = real/reserved. The tool refuses to self-evaluate — it emits the break-only prompt, Kevin carries it to a separated pass, `record` logs the verdict. Smoothness guard nags at ≥5 gated / 0 breaks. Wired into daily-cycle close **Step 3f** (`report`, read-only) 2026-07-10 on Kevin's mark — 3e is the Governor trail) · **P3 metamorphic check ✓ BUILT 2026-07-10** (`tools/recognition/metamorphic_check.py` + NOTE + `metamorphic-log.jsonl` starting empty; smoke-tested full cycle then smoke data removed — no fabricated backfill. Five transforms: paraphrase/contrapositive/perspective/concretize/boundary; M1 cold-pass = mitigation, M2 cross-family = real separation — M2 matters most here since the mechanism itself is Claude-correlated. Refuses to render or judge itself; separated pass does both. Wired into daily-cycle close **Step 3g** (`report`, read-only) 2026-07-10 on Kevin's mark — full chain now 3c/3d/3e/3f/3g; if the close drags past the ten-minute ceiling, pulling 3g back to crossings-only is a named legitimate disposition). Rules that fell out: scout ≥2 model families before high-stakes canon (common-mode fix); extend drift meter to intake-coverage. Carried law: every mechanism *detects, never proves* — a filter that lowers the pass-rate of plausible-wrong, not a stamp; separation must be substrate-deep, not a re-prompt.

**Gate delta 2026-07-19 (MARKED same day, mark record `marks/2026-07-19_gate-delta-three-l0-fields.md`):** L0 grew from 4 to 7 required fields — `misuse_boundary` (where the pattern does NOT apply; L1 break-prompt now attacks empty boundaries), `declared_tensions` (what stays open; settledness must be typed, never defaulted; object-jurisdiction only), `author_removal` (recorded result of the new **L1-R legibility pass**: fresh session's one permission is to state the claim back, RESTATED/COLLAPSED; COLLAPSED = L0 break; filled via `record --l1r`, `--l2` gives L2-R cross-family). L1-R stays separate from break-hunting L1 — one session, one permission. Existing items NOT retro-broken; gate on next touch. Spec (with build plan + its own falsifier) at `_INTAKE/GATE_DELTA_SPEC_three-L0-fields_2026-07-19.md`. Provenance: seven author-removal checks from the "crossing architecture" outside-AI drop mapped onto the live gate — 4 covered, 3 new. Held candidate: fourth field `authority_not_claimed`. Related open thread: quartet comparison (Ground/Distinction/Load/Transfer vs the ratified same-quartet mark).

**First real feeding (2026-07-09, consented retrospective over the fortnight):** report at `_INTAKE/organ_run_2026-07-09.md`. R1: 10 claims from recent canon + promote-ready gated — 10/10 L0 pass (expected: Converger-developed corpus; L0's zero certifies the upstream pipeline, not the claims); all 10 await separated passes via the packet `_INTAKE/falsifier_prompts_2026-07-09.md` (L2 cross-family flagged: cn-constitutional-stack, pr-refusal-write, pr-live-hands). R2: 6 forecasts in the Brier ledger (5 HARD; c2/c3/c5 forecast the apparatus's own adoption risk). R3: coordination-log.jsonl created EMPTY — no fabricated backfill; drift meter eats forward. Pre-run finding: both "wired" organs had empty feeds (Gap 3a live).

Origin: the validation harness Kevin was gifted (see [[project_ai_constitutional_stack]] lineage) — run on itself (Test 4 + 7 passed), it exposed two holes that became the integrity scout target. Related: [[feedback_renderer_not_verifier]], [[project_the_governor]], [[feedback_no_cost_shifting]].
