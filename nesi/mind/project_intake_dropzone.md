---
name: project_intake_dropzone
description: "The _INTAKE drop-zone workflow — raw feedstock in, coherent extracted system out, file count falls as content coheres"
metadata: 
  node_type: memory
  type: project
  originSessionId: 74ca4214-a03a-4266-a51a-7e68995a1fc3
---

`_INTAKE/` at the DSS-content root is Kevin's metabolization drop-zone, built 2026-05-30. Model: **feedstock, not archive.**

- `_INTAKE/raw/` — drop zone. Kevin drops *copies* (originals stay where they live); mixed formats fine. Copying-then-metabolizing keeps it reversible.
- On "metabolize," read for load-bearing structure — no paraphrase. Extract pattern, name what's actually happening, register what doesn't fit. Output stages alongside in `_INTAKE/` (e.g. `*_metabolization_v2.md`), never overwriting the raw drop.
- **The gradient:** everything in the drop zone gets cross-indexed, verified, run through substrate-building / phase-transition. File count *decreases* as content coheres into the extracted system. Falling file count = rising coherence. The drop zone trends toward empty as the extract fills. This applies to BOTH layers: thin byte-identical duplicates in raw (verify by hash first — never name-lookalikes), and supersede/retire earlier extract versions once folded into later ones.
- Kevin holds the membrane gate ([[feedback_publish_delete_boundary]]): nothing pushed or deleted without his mark; thinning the reversible drop-zone copy is safe by default once he says so.

First batch metabolized here: the [[project_iuec_arc]]. Working pattern is [[feedback_framework_self_application]] — run the audit on the material and on the conversation.
