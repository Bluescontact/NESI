---
name: project-meta-skill-learning
description: "Meta-skill learning mechanism — the skill ecosystem expansion building mark-record, metabolizer, and the full session-boundary loop; 2 of 4 complete as of 2026-06-26"
metadata: 
  node_type: memory
  type: project
  originSessionId: a4275d36-3134-48e4-a052-d5343a9dd5da
---

The meta-skill learning mechanism is the loop that closes between substrate runs, Kevin's marks, and the provenance record — without modeling Kevin (the displaced-zero law forbids cross-analysis of marks).

**What learning currently exists:**
- Library growth — LIBRARY array in substrate workflow.js grows as patterns are promoted. The codex screen becomes more discriminating.
- Memory files — feedback and preferences session to session.
- Calibration zeros — [KEVIN] zeros in Governor/Converger/register_audit. When set, instruments fire at Kevin's threshold. Three still unset.

**The constraint (non-negotiable):**
The displaced-zero law forbids cross-analysis of marks. Marks can never become a dataset to model Kevin's decisions. The mark record IS the provenance trace; it is NOT an input to algorithmic skill calibration. Reading marks backward is as forbidden as predicting forward.

**The permissible mechanism:**
Provenance accumulation — mark record + ledger — available to a future second sensor (human or out-of-family model). They can read HOW ratification works (conditions, texture), not just the outputs. The learning is structural, not algorithmic.

**Skills built:**
1. `~/.claude/skills/mark-record/` — LIVE (2026-06-26). Slot + prompt + ledger update. Fires right after a mark closes.
2. `~/.claude/skills/metabolizer/` — LIVE (2026-06-26). Pile → dispositions → staged GATE delta. Makes queue displacement visible.
3. `~/.claude/skills/morning-pages-channel/` — LIVE (2026-06-26). 750-word file-in drop → harvest work-objects only → metabolizer → shred raw. Guard-as-law: the system never reads the writer. Inbox: `rhythm/pages_inbox/`. Gated zone: `rhythm/_pages_gated/`.

4. `~/.claude/skills/daily-cycle/` — LIVE (2026-06-26). ON OPEN: show gate → one-line state → session name → queue-jump check. ON CLOSE: metabolizer on session output → build snapshot → stats → append CYCLE_LOG.md. Ten-minute ceiling; overflow = Governor reading. Reserved zero: none by design.

**All four skills live. The meta-skill ecosystem is complete as of 2026-06-26.**

**Why:** Kevin abandons sessions at context exhaustion — this is the pattern. The daily-cycle is the close-out protocol that should fire before exhaustion, not after.

**How to apply:** When Kevin says "3" or "4" — build the next skill in sequence. When a mark closes in session — invoke mark-record.
