---
name: project-the-governor
description: THE GOVERNOR — the brake on the work — built and marked live 2026-06-04; the missing piece named in the Recognition Stack, now the operating cost-brake
metadata: 
  node_type: memory
  type: project
  originSessionId: c2759f13-b8cb-4dde-9815-72a1a19046b8
---

THE GOVERNOR was built 2026-06-04 via a 3-pass multi-agent workflow and **marked live by Kevin 2026-06-04** (promoted from gated draft) at `THE_GOVERNOR.md` plus the paired `negative_workspace/README.md`. It is the brake the [[project-recognition-stack]] named as the missing piece — now the operating cost-brake on Tracks 1, 2 & the whole field. The skeletoned checks in Track 3 ([[project-ri-kit]] / Heartland plan) are instrumented here.

Design: an instrument, not a process — "a brake that produces is a treadmill; this one only stops." Three instruments because each fails silent where the others see: **body** (felt-gate), **clock** (operational floor — 4hr / one offering), **topology** (structural bypass / rings). Tiered cadence — DAILY (body+clock) vs CYCLE (oscillation, money-edge, cross-resource, topology) — explicitly so the brake doesn't become its own treadmill (Track 3's warning, [[project-ri-kit]] governor). It deliberately does NOT absorb the membrane gate, substrate-sequencing, or work-quality — those stay separate (see [[feedback-publish-delete-boundary]], [[feedback-substrate-before-artifact]]).

The `negative_workspace/` articulates the project by its shadow — every law's failure mode, tagged "Caught by" a gate or "OUT OF GOVERNOR SCOPE." As the Governor binds a shadow it moves from Open into caught.

**Format note (2026-07-10 verified):** the live instrument is `THE_GOVERNOR.html` at DSS root (instrument-HTML migration); `THE_GOVERNOR.md` was batch-composted 2026-06-28 to `_INTAKE\_COMPOST_2026-06-28\maintenance_debt\` with the other superseded .md instruments. Not a loss — a format retirement.

**Witness-has-no-witness — partially held (2026-07-10, Kevin's marks):** the trail organ is LIVE — `tools/recognition/governor-log.jsonl` (four fields: date · gate · reading · next; **no fifth field ever** — never the body-line) appended by daily-cycle close-out Step 3e; recording `gate: none` sessions is load-bearing. The reader (drift meter + brier ledger + falsifier gate pointed at the trail) is queued in `_INTAKE/future_development_queue.md`, CONDITIONED: not before the trail holds ~3 weeks of lines; organs score the record, never Kevin's reads. Five-option map from that session sits in the 2026-07-10 widgets. Regress cut deliberately: organs read the record, Kevin reads the organs, nothing reads Kevin.

**The witness edge split (2026-06-04 refinement):** the *decision*-witness is bound — Kevin is the witness, the AI is the both-sides surface (see [[feedback-map-both-sides]]). The *body*-witness (green vs green-while-spent) stays open and is not closable from outside the body; its floor is fail-closed (when uncertain, STOP), not an installed sensor. Genuinely-unbuilt open edges remain: the calibration zero (unset until run against lived cycles), the cutoff's under-load firing (untested), the independent recovery sensor, the temporal pioneer-phase end. See [[feedback-somatic-threshold]].
