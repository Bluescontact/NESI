---
name: project-pattern-promotion-and-holds
description: Open next-step from the 2026-07-15 pattern-marks sweep — 32 patterns marked PROMOTE still need actual membrane-crossing into canon; 57 marked HOLD each carry a named condition worth revisiting
metadata:
  node_type: memory
  type: project
  originSessionId: 87c5b512-f518-4e8b-9279-5f0df78bd1c3
---

Spun off from [[project_substrate_brief_pattern_marks]] when that sweep closed 2026-07-15 (113 patterns marked across ~25 `SUBSTRATE_BRIEF_*.md` files in `_INTAKE/`: 32 promote, 57 hold, 24 compost). Marking the checkbox is not the same as crossing the membrane — this is the layer underneath that.

**Thread 1 — Promotion to canon (32 patterns marked PROMOTE, not yet crossed):**
These patterns cleared the Converger's bar and Kevin's mark, but still sit inside their `_INTAKE/SUBSTRATE_BRIEF_*.md` files with `[x] Mark: PROMOTE` — none have been run through [[project_membrane_controller]] (the exclusive write authority at the Promote-Ready → Library crossing). A future session should: grep all SUBSTRATE_BRIEF files in `_INTAKE/` for `[x] Mark: PROMOTE`, list the 32 candidates, and run the membrane-controller skill per-pattern (or in a batch Kevin approves) to actually write them into `patterns/`.

**Thread 2 — Held patterns with named conditions (57 patterns marked HOLD):**
Each hold carries a specific, already-written condition — not a vague "not sure": things like "needs a second real-world instance," "name a falsifier separating X from Y," "rewrite without borrowing another pattern's scaffolding," "Kevin's own felt-read gate not yet crossed." These conditions are sitting in the brief files themselves, right after each Converger verdict. A future session's job is not to re-litigate the hold — it's to check whether life has since supplied the missing instance/falsifier/rewrite for any of them, and re-screen only those.

**Why this matters:** the prior sweep deliberately did NOT attempt promotion or hold-resolution — it was pure disposition-marking, kept intentionally separate so the marking pass could move fast (it ended up compressed into 4 bulk decisions per [[feedback_triage_batch_pace]]'s "Update 2"). This is the slower, higher-stakes layer that comes after.

**How to apply:** Do not default to "process everything again." Start by asking Kevin which thread he wants — promotion (concrete, bounded, ships 32 things) or hold-review (fuzzier, needs him to know if any held pattern's condition has since been met). Batch pace per [[feedback_triage_batch_pace]] — start small, watch for him asking to scale, and watch for the same "too many decisions" signal that triggered the bulk-rollup restructuring in the prior session before doing per-item review.

**Progress, 2026-07-15 (NESI phase-transition session) — Route A of a 4-route ordering (A → C → D, B last), CLOSED:** Ran the Converger re-screen on all 33 patterns staged by the earlier holds-pass (`gate/data/_delta_2026-07-15_holds-pass-to-zero.json`). An Explore agent enumerated all 33 (grouped by file, original hold condition + resolution + confidence read), sorted into ~18 strong / ~10 plausible / ~4 self-flagged-weak. Batch 1 (8 strongest) ran per-item; Kevin then asked for the same bulk-bucket mode as the original pattern-marks sweep ("doesn't want to sit through patterns for an hour") — the remaining 25 were sorted into 3 buckets (14 clean-promote / 6 promote-but-flagged / 5 stays-hold) and confirmed "proceed as marked" in one shot. **Final tally: 33/33 re-screened. 28 → PROMOTE, 5 stay HOLD.** A general-purpose agent applied all 25 bulk-bucket edits (batch 1's 8 were hand-applied earlier); every pattern's file got a new `[x] Mark: PROMOTE` or `[x] Mark: HOLD (confirmed)` line appended after its re-screen note, verified against neighboring patterns in multi-pattern files to avoid mis-anchoring. The 5 confirmed-stays-HOLD: Closed-Loop Anchor Loss, Refusal and Presence as Container, Intention-Gap as Trust Accumulator, Permanent Organ Feeding Relationship, Floor Converted to Fuel on Arrival — each admits its own thinness in-text (taxonomy-not-resolution, self-flagged "more speculative," or an untouched regress). **Route A is done.** Thread 1's original 32 PROMOTE-marked patterns are still untouched by this session — the combined pool now waiting on an actual membrane-controller crossing pass is 32 + 28 = 60 patterns, marked but not yet written to `patterns/`. That crossing pass is a separate future session, not run here. Next in Kevin's stated order: Route C (data/routing/wiring harness), then Route D (Genesis Seeds staging folder), Route B (NESI demo scaffold) last.
