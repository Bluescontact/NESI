---
name: loads-substrate-pass
description: "Independent substrate pass on NESI \"The Loads\" (2026-07-20) — 5 patterns, 0 promote-ready, 4 HOLD, 1 COMPOST; disagrees with other agent's 3-promote read; brief in _INTAKE awaiting Kevin's marks"
metadata: 
  node_type: memory
  type: project
  originSessionId: 786b519e-408b-4ace-a27c-51b0a8008b47
  modified: 2026-07-20T20:55:02.298Z
---

2026-07-20: Kevin pasted another agent's substrate development of `NESI_THE_LOADS_2026-07-20.md` (seven loads, SHED/KEEP line — source doc NOT on disk, lives only in that conversation) and marked a full independent pass. Source saved verbatim at `_INTAKE/raw/substrate_source_2026-07-20_nesi-the-loads.md`; brief at `_INTAKE/SUBSTRATE_BRIEF_nesi-the-loads-2026-07-20.md` (48-agent workflow, 96 library files screened).

Result: 5 patterns → 0 PROMOTE-READY · 4 HOLD · 1 COMPOST. Pile delta: zero crossed, zero staged promote-ready.
- `pass-level-falsifier` — COMPOST (folds into dissolution_encoded_at_origin; one instance, "earned" unmeasurable)
- `fold-on-collision` — HOLD: build evidence-accrual register + Kevin-held fold-mark rule into same_quartet_assembly_standing, else lineage note
- `true-but-not-crossable` — HOLD: articulate dependency-depth stopping rule before crossing (extends gate_as_f4)
- `holding-has-a-floor` — HOLD: bind falsifier + no-floor register + starvation mirror (extends unholdable_delegation)
- `condition-type-fault-line` — HOLD: supply checkable-vs-felt classification procedure + worked example (extends unholdable_delegation)

DEBRIS AMENDMENTS 2026-07-20 (Kevin's marks): Kevin ruled manual Converger confirmation "debris from before processes were in place" — confirmed CHECKABLE by the typing test. Amended with his mark: membrane-controller Condition 1 (machine-verified: Converger disposition + pile arithmetic, face-up, fail-closed), Condition 4 / D1 (metadata pre-filled, mark = yes/no on displayed bundle), Crossing 2 Condition 2 / D2 (five fields carried forward from body-question), daily-cycle step 1c / D3 (agent executes delta apply + verifies). Sweep verdict: felt architecture across all 11 skills clean; body-questions and marks named as the felt floor, untouched. F001 RATIFIED (first evidence entry, crossing razor). Three crossings (fold-on-collision, true-but-not-crossable, condition-type-fault-line) pending only Kevin's body-responses + yes at the membrane; Condition 1 machine-check already PASS (0 falsifier violations).

MARK-APPARATUS BUILT 2026-07-20 (Kevin's mark on the manifest): `tools/mark_apparatus/` — fold_queue.py (folds staged not final; F001 = the other agent's R4 fold awaiting retroactive mark), trigger_ledger.py (checkable anchors watched, felt never evaluated; dependency-depth = first-order named marks only), typing test + worked example in README §3, surface_audit.py (audits agent surfaces only, never Kevin). Seeded: 4 loads holds + intent-fork (felt). First check TRIPPED 3 re-screens (H001 fold-on-collision, H002 true-but-not-crossable, H003 condition-type-fault-line — the build itself was their condition); H004 holding-has-a-floor still held (shaped form unwritten); re-screens end at Kevin's mark.

MARKED 2026-07-20: Kevin ratified the pass verdicts ("proceed as marked") — COMPOST closed, four HOLDs stand with re-screen conditions; marks ledger appended to the brief; state_view regenerated. Nothing crossed.

Disagreements with the other agent's pass (in practice superseded by the ratified HOLDs, never explicitly ruled): (1) its three PROMOTE-READY cards all came back HOLD-with-conditions here; (2) THE_ENGINE_IS_LAST was not extracted as canon-transferable at all (reads as NESI build guidance, not pattern); (3) its governing-falsifier device itself was judged promising-move-not-yet-pattern and composted. Upstream: the intent-fork (what holding frees Kevin to do) still blocks BUILD_DECOUPLES_FROM_PRIORITY. Related: [[project_nesi]].
