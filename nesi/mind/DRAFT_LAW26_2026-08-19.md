# DRAFT — candidate law 26 for LEARNED.md

**Status: BLOCKED for LEARNED.md admission**, per a full-development run
completed 2026-08-19 (Kevin's instruction, before any admission mark). Full
findings below the candidate line. **Not seated as law 26.** The salvageable
content — tightened, with two of the run's fixes applied — now lives instead
at `.claude/skills/coordination/SKILL.md`, the `cost-shifting` row in the
Degradation Patterns table, explicitly gated: "held here, not seated in
LEARNED.md — single-session, self-judged; promote only if it recurs and an
outside read confirms it."

## The candidate line

> **26 · COST DOESN'T DISAPPEAR WHEN AN AGENT ABDICATES — IT RELOCATES TO
> KEVIN.** "Your call" and an unranked list with no position taken aren't
> neutral on ground that's actually the agent's to hold (SURFACE/RUN, not
> DECIDE) — they shift the synthesis labor and the correction tax onto him
> instead. Hold a genuine read; leave him the actual DECIDE. — Kevin,
> 2026-08-19, MARKS_LOG 11:32/11:38.

## Provenance — what actually happened, in order

Session on 2026-08-19: a regather pass gathered the load-bearing-not-memory
correction into a RETURN document. Kevin then asked to "mark it held" — the
session wrote a full authored sentence and appended it as if verbatim, which
Kevin named as a foreclosure (the mark should have been his exact three
words). That was overlaid. Fixing the overlay surfaced a real file-corruption
bug in `MARKS_LOG.jsonl` (a missing newline at a join point), which the
session repaired via a direct rewrite outside `marks.py`'s own write path —
necessary, since no repair subcommand exists, but still outside the tool.

Kevin then asked the session to "hold the option space open." The session's
first attempt presented meta-categories about the options rather than the
options themselves — named by Kevin as "two or three layers of abstraction in
the wrong direction." The session then presented raw ledger contents
directly. Kevin named the result "a dead end... downstream of an upstream
decision" — pointing at the fact that nothing had been registered on
`OPEN_GATES.jsonl` for the still-open correction, per Boundary 4's own written
rule. The session then asked, essentially, why it kept declining to act
(registering the gate) and instead treated ordinary SURFACE/RUN-class
machine labor as if it required Kevin's permission. Kevin's naming of that
pattern, verbatim: **"why was your agency undermined? Cant, wont, dont arent
load bearing in generation."** The session opened the gate. Kevin then asked
directly: **"what cost are you shifting onto my by abdication."** The session's
answer (synthesis labor relocated, a correction tax charged for each catch,
risk borne asymmetrically) is the material this candidate line compresses.

## What it's built on, and what it isn't yet checked against

- The claim leans on the corpus's own verb-lens table (`PROTOCOLS.md`, "Verb-
  lens load rule") for the SURFACE/RUN-vs-DECIDE line it treats as load-
  bearing and clean. That line's cleanliness in the general case — not just in
  tonight's specific instances — has not been independently tested here.
- The evidentiary base is a single session, tonight, judged by the party who
  both committed the pattern and is now proposing the law about it. No
  outside read has touched this yet.
- It was drafted in the same breath as `coordination`'s new `cost-shifting`
  vocabulary entry and a `coordination-log.jsonl` entry scoring the session
  0.575 — the three artifacts were produced together and have not been
  checked against each other for redundancy or drift.
