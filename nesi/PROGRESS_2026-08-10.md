# NESI · THE INTAKE — progress, 2026-08-10

*Written on Kevin's ask ("show me an md of all the progress today"), session 44297d01.
Everything below was run and seen this session; nothing is assembled from memory.*

---

## Where it stands tonight

**The 2d game opens on the intake — nothing precedes it.** The launchable is
`nesi\world2d\export\NESI.exe` (exported 16:13, running since 16:22 for Kevin's own
walk). Editor run: `& ..\..\tools\godot\Godot_v4.7.1-stable_win64_console.exe --path .`
from `nesi/world2d`. Kevin's live writing sits in
`%APPDATA%\Godot\app_userdata\NESI\writing_stones.json`, touched only by his own hand.

---

## Step 0 — the writing guarded (first, on the session's order)

- **Quarantine:** all 10 intake stores copied byte-identical to
  `nesi/_intake_quarantine_2026-08-10/` with SHA-256 proof. Nothing deleted, trimmed,
  or cleaned.
- **What the quarantine found:** the live 2d `writing.txt` held 16,923 B of machine
  filler (`word0…word1999`) plus one unattributed trailing fragment ("and this
  sentence is cut off mid-") — Kevin's to see. `NESIPROBE` holds real keyboard
  writing. The 3d day files `2026-08-06.txt` / `2026-08-07.txt` are **entirely
  machine text** — `walk_intake.gd`'s DEPOSIT_SAMPLE and `walk_menu.gd`'s sentence,
  written into the live store by the harnesses themselves.
- **The destroy-on-boot is out:** `world2d/main.gd:33-35` no longer deletes the live
  files on `--harness`; the Store redirects to a scratch pair
  (`Store.set_harness_store`). The `--forcequit` probe refuses to run without the
  harness scratch.
- **Every writer routed through the override pattern:** `Intake.dir_override` added
  (mirror of `IntakeDeposit.path_override`); `walk_menu.gd` and `walk_intake.gd`
  rewired to `.walk/` scratch; `menu.gd`'s reader follows the override. Already
  clean, verified by reading: `test_intake_deposit.gd`, `walk_stations.gd`,
  `deposit.gd`, `sorting_tarp.gd`, `land.gd`.
- **Proved:** both harnesses run against live-looking stores; every live mtime and
  hash unchanged; the writes landed in scratch.

## Step 1 — ground

- All nine of spec §7 **VERIFIED on disk**, nothing missing: the Bar, the thought
  strip, the board, the page+generate (`nesi/world/logbook.html`), the 3d intake,
  the Ctrl+Enter deposit, the sorting tarp, `path_override`, the membrane router.
- **`nesipoint.exe` does not exist** anywhere on disk or in the ledgers; nothing was
  created to satisfy the name. Every launchable named with path/size/mtime; the
  evidence (2D ruling, store.gd's authority note, freshest export) points at
  `world2d/export/NESI.exe` as the target — while §10's front-door fork stays open.

## Step 2 — the intake, built (Kevin's word)

Eight scripts + scene + `project.godot`, all in `world2d`:

- **Four sources** — self · given · fetched · the merged TABLE. Source stored on the
  stone, machine-immutable, drawn on every face.
- **Self, blind and tiles** — blind: each sentence leaves as finished, nothing
  accumulates, nothing said; tiles: each sentence its own tile, written order,
  grouped by run.
- **The cut** — punctuation fires; a bare `.`/`…` with no letter or digit is not a
  sentence; a period between two digits is not an ending; Enter drops what is held,
  finished or not; Enter on an empty bar closes the run.
- **Given / fetched** — enter coarse as one rock carrying who/asked; broken down on
  his click at the paragraph seam, source kept, rock marked dropped.
- **One store, one write path** — `writing_store.gd`, tmp → read-back → compare →
  rename; three dispositions in the schema from the first line: sent · dropped ·
  down (set down, no destination, ever).
- **The tarp** — eight rim regions, centre pile, outward only, landed never lifts,
  single pass, a region nameless until something lands (then a naming line for his
  hand), fold commits nothing.
- **By hand** — edit (his words), discard, label, link; drag stays; machine
  placement only at arrival, in written order.

Kevin walked it himself at 15:46 and wrote real writing into his live store.

## The evening session — the fourth face and the merge

- **THE MERGE** (level 2's first move, only it): MERGE picks tiles, JOIN makes one —
  in WRITTEN order regardless of pick order. **The seams are kept in the data**
  (every part snapshotted on the merged stone AND retained in the bank staged
  `merged`). No unmerge control — whether a merge can be undone is unmarked and
  Kevin's. Differing sources would all ride the face; never one picked, never
  blanked.
- **SEQUENTIAL** (the fourth face): a plain page, everything visible as typed, in
  the order typed, nothing leaves. **Depositing is his act** — Ctrl+Enter, ported
  from `world3d/scripts/intake/deposit.gd`: same cut, idempotent by content, arrival
  order, no per-stone timestamp, the page never cleared. No sentence cut fires on
  the page.
- Under it: stone numbers became names, not positions (`_row_n`), because a sheet
  re-deposit removes rows and index arithmetic would have pointed acts at the wrong
  stone.

## Proof, across the day

- **24 harness checks, all true** (real key events, real clicks, real drags), run
  repeatedly; **8 screenshots read** from launched builds (blind, tiles, sources,
  tarp landed, tarp folded, hold-through-reload, the merged tile, the page, the
  landing).
- One real failure caught by its own screenshot: the mode buttons sat under the
  table's z-order, making SEQUENTIAL unreachable — moved into the band's strip and
  re-proven.
- Every harness write went to `.shots/` scratch. Kevin's live stores were never
  opened by a tool at any point after step 0.
- The overview went to the "NESI game progress review" session on Kevin's word.

## Open — named, not asked

- **The lexicon:** the intake's own vocabulary (intake, tile, tarp, zone, blind,
  pool, source, self, given, fetched, seam, merge…) is not in `LEXICON.md`; the
  lint blocked ~33 identifiers across the day and each was rebuilt from words
  already on the list. The words wait on Kevin's line — the lexicon's only intake.
- **The layout:** one continuous surface built; the spec calls layout the
  reversible half, and tabs stay one word away.
- **§10 forks:** all untouched — front door unruled, the chunk-word unnamed, the
  word-count contradiction unreconciled (and not carried into this build), the
  tarp's close trigger unpicked, ordering-from-the-words unentered.
- **Not built, per the orders:** levels 2–4 beyond the merge, level-condition
  display, any ordering from the words, the world / water / stations / geometry —
  `scenes/main.tscn` untouched on disk.
- **Edges:** the 16:13 export packs exactly the proven project but was not itself
  re-walked; merging stones of differing sources and the tarp with sheet stones
  were not separately exercised.
