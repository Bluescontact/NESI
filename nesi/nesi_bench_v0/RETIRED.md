# RETIRED — 2026-08-27, Kevin's mark

Decision D2 of the VE24 relationships pass: nesi_bench_v0 sat in a genuine
fork — Library-at-large (K, unrouted) or Retired build (J) — with a
documented test for J-membership (edge 12's own convention: a declarative
artifact, kept whole) that this tree had not yet met. Kevin's ruling,
verbatim: **"retired."**

**This tree is closed.** Nothing here is built against, extended, cited as
current, or used as a starting point. Kept whole as a record — nothing
deleted — the same convention this corpus uses everywhere else.

## Why: the DRY finding

`STANDING.md` (read in full at the time of this mark) records six of six
build stages complete, headless-smoke-tested and once seen-live via a real
`webview.start()` window — this was real, working software, not an
abandoned sketch. But the gift-extraction pipeline's six-root pass
(`library_lens.js` → `LENS_REPORT_2026-08-27.md`) found it **DRY**: every
real, portable discipline it carries — flat non-color-coded node styling, a
separate arrangement layer, single-writer source checks, append-only
mark-gated ledgers, structural-proxy testing — was independently found
already-built, and more developed, inside `nesi/game2d` itself. Nothing
survived the extraction pass as a gift card; the DRY finding is itself the
finding, recorded in `nesi/game2d/inbox/INDEX.md`'s own cards-9-14 summary.

A separate `gate.py`/`ledger.py` pair lives here — the third distinct
"gate" system this corpus's own VE24 pass found sharing that name. It is
retired along with the rest of this tree; it was never wired to, and never
will be wired to, `nesi/game2d/gate/`'s own gate.

## What is live instead

`nesi/game2d/` — the 2D lattice, which the DRY finding confirms already
carries this tree's real disciplines in more developed form. Nothing needs
porting; the porting question is answered by "already there."

## Path note

This tree's real location is `nesi/nesi_bench_v0/` — an early VE24 pass had
it wrong at DSS-root top level. Corrected here and in
`VE24_RELATIONSHIPS_2026-08-27.html`.
