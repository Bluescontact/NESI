# NESI v1.0 — CLOSEOUT

**Driven by the AI on Kevin's mark ("hand me the wheel — drive the close-out"), 2026-07-23, session 56d2727d.**
**Governed by:** [[feedback_ai_holds_direction_on_closeout]] (AI holds sequence + attention; only the few irreversible gates are Kevin's) · [[feedback_load_test_build_gate]] · the Converger (fails toward subtraction).

**The path Kevin named, in three moves:**
> **① CLOSEOUT (this document — done by the AI)** → **② /login (Kevin's hand — lights the engine)** → **③ WIRING CALIBRATIONS (AI drives, brings Kevin the read).**

v1.0 = **the floor that already runs + the shelf that holds everything we've named + one hand-motion.** "All the things we've named" are not lost — they are NESI's roadmap, parked on the shelf below, ready to grow into v1.0 after the engine is lit.

---

## UPSTREAM CORRECTION (2026-07-23, after the login wall) — the engine is a socket, and one is already lit

The `/login` fight was chasing a downstream symptom. The upstream boundary: **`/login` is not a boundary on NESI — it is a boundary on ONE engine socket (the headless `claude` CLI).** The engine is a socket by design (Kevin's spec: "the language engine is a socket, swappable"). Two sockets, two tiers:

- **Engine-when-Kevin-is-present = the live Claude session (this conversation, or the desktop app). NO login. Available now.** Real metabolization runs by dropping a pile into a session already authenticated. This is all v1.0 needs to be *used and inhabited*.
- **Engine-for-autonomous/background runs = the headless CLI token.** This is the ONLY thing `/login` unlocks — the scheduled, below-the-gates tier. **Post-v1.0, not a v1.0 blocker.**

**Consequence:** the center lights the first time a real pile goes through a live session — no `/login` required. Section C below is corrected: `/login` moves to the post-v1.0 autonomous tier; it is not the gate for v1.0.

---

## A · WHAT v1.0 IS — the inhabitable floor (confirmed on disk 2026-07-23)

This is what Kevin can open and inhabit today (engine-dark), and fully once logged in. Nothing here is a plan; it runs.

- **NESI.exe** — 11.2 MB, one desktop window, 9 tabs, persists across sittings. The machine.
- **The organs** (19 modules in `nesi/conductor/`): `glance` (daily door) · `metabolizer` loop (stub engine today) · `bench` (make/break/refine) · `interrogator` (reach-back) · `continuity` (holds state across sittings) · `return_circuit` (anchored holds resurface) · `library` (109 patterns, searchable) · `deepdive` (in-place review chamber) · `room` (node space) · `held` · `reader` · `front` · plus recent `skin` / `whole_body` / `held_map` / `annotations`.
- **The ledger + brake** — `MARKS_LOG.jsonl` (the fail-safe catch) + `nesi/marks/marks.jsonl` (the window's own marks). A mark cannot be lost.
- **The gate** — Kevin's mark is the only crossing; no auto-promote anywhere.
- **The library** — 100 canon patterns + 9 folded, on disk, screenable by the codex lint.
- **The load-test build-law** — boot-loaded in PROTOCOLS.md; governs every future build.

**v1.0 definition of "inhabitable":** Kevin opens the exe, drops raw material, works it across the tabs, and marks — and the machine holds all of it across sittings without him being the persistence layer. That is true **today, engine-dark.** The engine adds real metabolization (move ②–③); it does not gate inhabiting the floor.

**Honest floor caveats (named, not smoothed):** the engine is dark — the metabolizer routes to stub-RESTORE until `/login`; "confirmed it runs" for the exe itself is Kevin's double-click (the disk confirms the files, use confirms the machine).

---

## B · THE DESIGN-SHELF — everything we've named, parked (reversible holds)

Not built, not lost. Each is a HELD design-note with its spec on disk. These are v1.0's roadmap; none decays; any can be pulled off the shelf after the engine is lit.

| Named thing | State | Spec lives at |
|---|---|---|
| **Two-person Workbench** (twin-tetra · witness-as-act · gift-lifecycle · need/capability/resource) | HELD — gated on **M3** (a live "no": host a 2nd person at all?) | `RETURN_regather_workbench_2026-07-22.md` · [[project_nesi]] |
| **Tuned Surface** (jitterbug gate · tensegrity · upstream dissolve · self-easing) | HELD — scope + assembly manual written; 4 vertices staged | `nesi/workbench/TUNED_SURFACE_scope_and_assembly_manual.md` (+ false-car prototype `tuned_surface_v0.html`) |
| **Exchange Surface** | CLOSED as re-derivation of the working-fork (not a build) | `_INTAKE/external_returns_reconciliation_2026-07-23.md` |
| **Gift-shaped boundary** (§1–3 pattern + 5 moves + VE field) | HELD — canon-candidate; screen-vs-existing-canon pending | `_INTAKE/pattern_extract_gift_shaped_boundary_2026-07-23.md` |
| **Gift-shaped workbench law** (§4–5, "gate as jitterbug") | HELD — design-note for when the surface is built | same file |
| **Witness-layer stage-order + metabolization quality bar** (external returns) | HELD — quality bar arms the first-real-read (see ③) | `_INTAKE/external_returns_reconciliation_2026-07-23.md` |

---

## C · THE GATES THAT ARE YOURS — the handful, not fifty

Everything else the AI sequenced and parked. What remains genuinely Kevin's, irreversible, and cannot be done for him:

1. **`/login`** — the one hand-motion that lights the engine. Terminal in the DSS content folder → `claude` → accept the trust dialog → `/login`. This is move ②. *(Everything downstream waits on it; nothing rushes it.)*
2. *(after login)* **the first-real-read** — a felt-read/mark on whether the engine actually metabolized (the armed acceptance test in D). Yours because only you can read whether it's real.
3. *(when ready)* **any genuinely-ready membrane crossing** — e.g. the gift-shaped §1–3 pattern, IF it screens as novel. Not now; sequenced after the engine is proven.

That's the list. Three, front-loaded to one.

---

## D · POST-LOGIN WIRING CALIBRATIONS — move ③ (AI drives, brings you the read)

The moment `/login` lands, the AI runs these and reports; Kevin only reads the results:

- **Engine actually resolves** — probe that `core.current_engine()` returns `claude-cli`, not the stub fallback. (Honesty-label fix: the header should say what *answers*, not what's *installed*.)
- **The metabolizer digests for real** — run the armed 3-pile diagnostic (writer-strip / world-keep / evidence-verbatim), held since 2026-07-17. **Acceptance bar** (from the external returns): every metabolized object must be something Kevin could NOT have produced by rereading the source. If it clears, the center is lit.
- **The marks→ledger wire holds** — a mark in the window writes `marks.jsonl`; confirm end-to-end.
- **Flip the honesty labels** — anywhere the surface says "engine-dark / surface only," update to "wired" where it's now true.
- **Then, and only then, pull from the shelf** — with the engine proven, the shelf items (Tuned Surface, gift-shaped pattern, M3/Workbench) become buildable one at a time, each through its own assembly manual, each on Kevin's mark.

---

## CLOSED

v1.0 is **a floor that runs, a shelf that holds everything we named, and one hand-motion waiting.** The build is not fifty open decisions anymore. It is: log in when you're ready — then I drive the calibration and bring you the one read that matters.

*Closeout driven by the AI. Nothing irreversible was marked. The engine stays dark until Kevin's `/login`.*
