# NESI — the 2D deposit · BUILD RECORD

**COMPACTED 2026-08-24**, as part of the Gift-3-remap byte audit (product-builder-thread
lens, applied to this build). This file stood at 5,105 lines / 317,143 bytes — the single
largest file in the entire wired-output-vs-organ measurement, larger than any two engine
files combined — covering 110 dated build passes from 2026-08-11 through 2026-08-14.

**Nothing was deleted.** The full record, byte-for-byte, unedited, is kept whole at
`BUILD_RECORD.md.backup_pre-compaction_2026-08-24.md`, in the same directory. Three live
code comments cite specific passages of this file by name or line range —
`seam.js` (~1100–1370, the night-model shape), `tools/already_answered.js` (law 8 vs.
the deep), and `tools/cold_walk_prepare.js` ("A BREACH, NAMED") — all three were
repointed to the backup filename as part of this compaction, so those citations still
resolve correctly.

**Read the backup file directly** for the actual build history: lineage, the twelve laws
checked, every provisional call and what it cost, the manifests (THE DAY / THE NIGHT /
THE GREEN / THE HEADWATER / THE BLOOM / THE CROSSWIRING), the rebuild, and the two
2026-08-14 sessions that close it out. Nothing here is available to name back by
reference to this pointer alone — cite the actual passage, from the source.
