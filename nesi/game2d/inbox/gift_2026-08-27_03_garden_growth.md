# GIFT — a living growth-form fed by the writer's own activity, no score

- **title:** the local rule with no objective (GARDEN growth)
- **what:** a small reaction-diffusion field — each cell's next value is a function of its neighbors alone, run to a fixed point on entry. No fitness function, no target, no score anywhere in it; the absence of an objective is the organ, not an oversight.
- **source:** `nesi/game2d/_compost/ascent_2026-08-21_pre-rebuild.html:2103` (`const v=f[i]+D*(l+r-2*f[i])-decay*f[i]`), documented at `nesi/game2d/ORGANS.json` under seat `GARDEN`
- **when:** built and verified before 2026-08-21; cut in that day's full ascent rewrite, kept whole in compost
- **quote:** *"400 step() calls change the bed by nothing; same seeds give the identical bed; an empty garden stays empty."* — `ORGANS.json`, its own verification note. And, on why this shape specifically: *"any optimiser is refused at this seat by definition, because an objective is a statement about what the growth means."*
- **capacity:** the page's `categories` ground already reports the writer's own structural facts back as a table (hinge count, declared count, arrival order). This organ offers the same material a different way — as a visual form that grows from what already happened, with nothing to chase and nothing scored. It would give the page something to *look at* growing from a session's own writing, without adding a single new capture or reading anything for meaning — the read path already has no write in it, by the same construction `ORGANS.json` verified for the sibling "side-effect accumulator" organ at the same seat.
- **unrouted_because:** same as the culling organ above — cut in the 2026-08-21 rebuild along with the rest of the pre-rewrite ascent file, never re-sited into anything since, including the page/lattice that didn't exist yet when it was cut.
- **routing:** a way in → feed the diffusion field from real page facts already computed (hinge-touch count, declared-edge count, sentences-per-day) instead of ascent.html's own "shoots" → an act → run it to a fixed point once per page load, exactly as the original did, never iterating while the hand is present (law 8) → a visible consequence → an eighth ground, or a standing corner of the page, that shows a shape grown from the writing itself, answerable to no target.
- **reading:** capacity: M · effort: M · confidence: M

---
*Ordered nothing. Waiting for a mark.*
