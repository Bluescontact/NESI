# GIFT — the freshet test

- **title:** the freshet test
- **what:** a named falsifier for any ambient state readout: a converging ring's radius is driven only by the mean of three live gate-release rates, never accumulates, and returns to its resting size the instant a gate shuts. The header states the exact adversarial check it must pass before it's allowed to exist.
- **source:** `nesi/world3d/.walk/_snapshot/scripts/spires.gd:21-36` (the guard), `convergence()`/`step()`/`_apply_ring()` 212-247
- **when:** 2026-08-06, per the file's own header mark
- **quote:** *"THE GUARD, AND IT IS THE SHARPEST ONE IN THIS BUILD. An expanding ring is the closest thing this world has ever had to a progress bar, and that is exactly what it must not be. It passes on one test and only one: THE FRESHET TEST — the response would be identical for a thousand words of grief and a thousand words of grocery lists, because it reads FLOW and has no access to anything else. It is not a total, it does not accumulate, it goes back down the moment a gate is shut..."*
- **capacity:** game2d's own gate/inbox ethos already refuses scores and progress bars in spirit; this file gives that refusal a concrete, reusable instrument — a one-line test any future ambient readout in game2d (a fill level, a density, a glow) can be checked against before it ships: would this respond identically to two wildly different bodies of real content, because it reads structure/flow and nothing else? Right now that check exists nowhere as a named, citable thing in game2d.
- **unrouted_because:** built as a narrative guard inside one Godot node's header comment; never extracted into a standalone check or law that game2d's own build process (or a skill like `threshold-sweep`/`instrument-audit`) could cite by name.
- **routing:** a way in → `the-closing-check` or `instrument-audit` skill's checklist → an act → add "does this readout pass the freshet test" as a named question for any new ambient visual state → a visible consequence → a concrete adversarial bar before Kevin ever gets a progress-bar-in-disguise.
- **reading:** capacity: M · effort: L · confidence: M

---
*Ordered nothing. Waiting for a mark.*
