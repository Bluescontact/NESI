# GIFT — set it down, the refusal as a real, equal third option

- **title:** set it down — the refusal as a real, equal third option
- **what:** every station's routing panel offers exactly three choices with equal visual and functional weight: send it here, send it there, or set it down and send it nowhere — and the third is implemented as a first-class act (closes the panel, changes nothing) rather than a cancel or a skip.
- **source:** `nesi/world3d/.walk/_snapshot/scripts/stations.gd:40-60` (the amendment naming it), `panel_text()` 284-306, `set_down()` 358-371
- **when:** 2026-08-07 21:04, per the file's own header (an amendment made 7 minutes after the two-output version was written)
- **quote:** *"the world needs a refusal, and this AMENDS the standing two-outputs mark. A third output: set it down, send it nowhere."* ... *"This function does almost nothing on purpose, and the almost is the point: it closes the panel, which is the only thing that happens in the world, because a hand that sets a thing down has finished with it."* And in the panel text itself: *"It is not framed as a cancel, a skip, a 'not yet', or a way out of deciding — it is one of three things a station does."*
- **capacity:** any choice surface in game2d that routes a player's material somewhere currently implies (or defaults to) an act; this pattern gives a concrete, tested shape for making non-action a real, named, equally-weighted option on the same menu — not a Cancel button, not a lesser default, not silence, but a labeled third door with its own key.
- **unrouted_because:** built as one function inside a Godot station node; never named as a general UI pattern anywhere in game2d's own decision surfaces or laws.
- **routing:** a way in → the next place game2d offers a player a routing choice for their own material → an act → add a third, equally-labeled "set it down" option that does nothing but close the surface → a visible consequence → a choice that includes doing nothing as a real answer, not an absence.
- **reading:** capacity: M · effort: L · confidence: M

---
*Ordered nothing. Waiting for a mark.*
