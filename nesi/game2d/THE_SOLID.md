# THE SOLID — pinned, 2026-08-14

> **■ EXTENDED 2026-08-16, and one line below is now false.**
>
> This file pinned the **vertices and the edges**. The solid also has **fourteen
> faces and a centre**, and those are now derived in `solid.js` and checked in
> `tools/solid_check.js` — eight triangles, six squares, and the position every
> circuit plane passes through. **`THE_CENTRE.md`** carries them, together with
> Kevin's ruling: *"the centre is the game. everything else serves it."*
>
> **§4 says "Nothing in the running game reads the edge set yet."** It does now.
> `ascent.html` loads `solid.js` and THE STATIONS derives its three outputs from
> it — the set-down has no member in the geometry, so it has nothing to
> acknowledge with, and law 6 became a consequence of the solid rather than a
> rule the file remembers.
>
> Everything else here stands, unedited.

**Kevin's line:** *"↓TANK and ↓FILTER are joined — pin the placement."*

It is pinned. The world is a cuboctahedron and this file is which one.

---

## 0 · HOW IT WAS CUT

```
placements satisfying everything the corpus wrote ........ 192
     + ↓TANK—↓FILTER joined ................................ 3 distinct worlds
     + the counsel brief's own census of crossing edges .... 1
```

His one line took 192 to three. **The brief itself cut the last three**, and that
is the correction below.

### Confirmed twice, independently

A second session pinned this in parallel and wrote `solid.js`, deriving
everything from its own circuit table. **The two tables are written differently
and generate the identical 24 edges** — same members, same triangle count, same
answer on his line. Two derivations from different rotations of the same solid,
which is the strongest form this could have been confirmed in, and neither
session saw the other's work.

### It is the solid, checked

```
vertices ..... 12
edges ........ 24
regularity ... 4-regular
triangles ..... 8      ← a cuboctahedron has exactly 8; the graph as previously
                        written had 4, which is how we knew it was not the solid
antipodes ..... 6 pairs, none adjacent
↓TANK to ↑LENS  3 steps
```

---

## 1 · THE FOUR CIRCUITS, AS THE SOLID GIVES THEM

Three falling, then three rising, on every one.

```
circuit 1   ↓DAM      → ↓STATIONS → ↓DEEP  → ↑HELIOSTAT     → ↑OVERWINTERING → ↑CAST      →
circuit 2   ↓FILTER   → ↓GROUND   → ↓DAM   → ↑SEATING       → ↑GARDEN        → ↑HELIOSTAT →
circuit 3   ↓STATIONS → ↓GROUND   → ↓TANK  → ↑OVERWINTERING → ↑GARDEN        → ↑LENS      →
circuit 4   ↓TANK     → ↓FILTER   → ↓DEEP  → ↑LENS          → ↑SEATING       → ↑CAST      →
```

**Circuit 1 is exactly as the corpus wrote it.** Circuits 2, 3 and 4 were written
in an order the solid does not carry; these are theirs.

---

## 2 · THE TWENTY-FOUR

| # | level | | # | level | |
|---|---|---|---|---|---|
| 1 | ↓DAM — ↓STATIONS | | 13 | ↓STATIONS — ↓GROUND | |
| 2 | ↓STATIONS — ↓DEEP | | 14 | ↓GROUND — ↓TANK | |
| 3 | ↓DEEP — ↑HELIOSTAT | **turn** | 15 | ↓TANK — ↑OVERWINTERING | **turn** |
| 4 | ↑HELIOSTAT — ↑OVERWINTERING | | 16 | ↑OVERWINTERING — ↑GARDEN | |
| 5 | ↑OVERWINTERING — ↑CAST | | 17 | ↑GARDEN — ↑LENS | |
| 6 | ↑CAST — ↓DAM | **return** | 18 | ↑LENS — ↓STATIONS | **return** |
| 7 | ↓FILTER — ↓GROUND | | 19 | ↓TANK — ↓FILTER | **his line** |
| 8 | ↓GROUND — ↓DAM | | 20 | ↓FILTER — ↓DEEP | |
| 9 | ↓DAM — ↑SEATING | **turn** | 21 | ↓DEEP — ↑LENS | **turn** |
| 10 | ↑SEATING — ↑GARDEN | | 22 | ↑LENS — ↑SEATING | |
| 11 | ↑GARDEN — ↑HELIOSTAT | | 23 | ↑SEATING — ↑CAST | |
| 12 | ↑HELIOSTAT — ↓FILTER | **return** | 24 | ↑CAST — ↓TANK | **return** |

### The four turns — water becomes light

`↓DEEP—↑HELIOSTAT` · `↓DAM—↑SEATING` · `↓TANK—↑OVERWINTERING` · `↓DEEP—↑LENS`

### The four returns — the only way back down

`↑CAST—↓DAM` · `↑HELIOSTAT—↓FILTER` · `↑LENS—↓STATIONS` · `↑CAST—↓TANK`

**↑CAST returns twice**, to the dam and to the tank — the only seat that does.
Giving is the world's principal way back down. And **↓DEEP turns twice**, to the
heliostat and to the lens: the bottom is the source of both the light and the
worked material.

### The six antipodes, none adjacent

`↓DAM ↔ ↑HELIOSTAT` · `↓STATIONS ↔ ↑OVERWINTERING` · `↓DEEP ↔ ↑CAST` ·
`↓GROUND ↔ ↑GARDEN` · `↓FILTER ↔ ↑SEATING` · `↓TANK ↔ ↑LENS`

**↓TANK is three steps from ↑LENS** and nothing in the world is further. Writing
is the farthest thing from a worked lens the solid contains — that survived the
repinning unchanged.

---

## 3 · WHAT HIS LINE DID TO LEVEL ONE

`↓TANK — ↓FILTER` is **level 19**, an edge of circuit 4 — and the built level one
walks tank → filter as its first two faces. **The level's opening step is a real
member of the solid**, which it was not under the previous reading.

The rest of level one's chain, measured:

```
↓TANK — ↓FILTER        a member          the level's first step
↓FILTER — ↓STATIONS    not a member      the second step crosses the solid
↓STATIONS — ↓GROUND    a member          }  the perimeter route the brief named,
↓GROUND — ↓TANK        a member          }  and both its steps are real
```

The brief's perimeter route is real: **↓TANK → ↓GROUND → ↓STATIONS** are both
members, and ↓GROUND is the settling seat. That is the route that would put a
settling gesture between separating and routing — the exact gap that made the
stations finish on nothing. **The choice between the two routes is still open**
and is now a choice between one real step and two real steps rather than a guess.

---

## 4 · WHAT IS NOW SAFE TO BUILD ON

- The 24 levels have real ends, and `THE_24.md`'s accomplishment lines can be
  re-cut against the true pairs.
- The reveal trigger — the 24th edge closing the solid — is now a computable
  condition rather than a description.
- Every claim in `THE_WORK_SURFACES.md` about seats stands untouched; nothing in
  it depended on the placement.

**Nothing in the running game reads the edge set yet.** Pinning it changes no
behaviour today and unblocks the circuit walk.

---

## 5 · A THIRD DERIVATION, GRADED — 2026-08-16

Kevin handed in **`THE_SOLID.html`** (23 KB, 416 lines, 2026-08-16), an
articulation written by a session that had the build's *numbers* and not the
build. Its own closing lines state the terms exactly, and they are the reason it
can be graded rather than merely read:

> *"This document is a hypothesis about `solid.js`, not a reading of it."*
>
> *"What one file would settle: the 24-row member table in `solid.js` — which
> seat joins which."*

**That file is on disk.** It was loaded and its own functions were called —
`antipodeOf`, `isMember`, `MEMBERS`, `TRIANGLES`, `SEATS` — rather than its
source being read for prose. This section is the settlement it asked for.

**§ 0 above says "confirmed twice, independently." This is the third, and it is
the first one that was wrong in an interesting way.**

### WHAT IT GOT RIGHT — and one of these is a real result

| its claim | the build |
|---|---|
| the solid is the cuboctahedron; 12 · 24 · 4 circuits; Euler 12 − 24 + 14 = 2 | ✓ |
| ↓TANK and ↑CAST are **adjacent** — so the door out is a real member and neither seat is a pole | ✓ `isMember('TANK','CAST')` → true |
| **the seam has exactly two possible shapes, and no others are reachable** — brute-forced over 46,080 candidate assignments → 768 solutions, 16 distinct after quotienting the solid's 48 symmetries | **✓ and the build is one of them.** Counted from `MEMBERS`: **8 sills · 8 members inside the falling half · 8 inside the rising half.** That is its 8-sill family exactly. |
| that family's degree signature: **2 2 2 3 3 4** | **✓ on both halves.** falling — TANK 2, DAM 2, FILTER 3, STATIONS 3, GROUND 4, DEEP 2. rising — HELIOSTAT 2, OVERWINTERING 3, CAST 2, LENS 2, GARDEN 4, SEATING 3. |
| that family has **2 triangles per half** | ✓ falling: `TANK·FILTER·GROUND` and `DAM·STATIONS·GROUND` |

**The enumeration is the result worth keeping.** A session with no access to the
edge table proved the seam could only take one of two shapes, and the build turned
out to be one of them, down to the degree multiset on both halves. That is a real
independent confirmation of the solid's structure and it should not be lost in
the corrections below.

### WHAT IT GOT WRONG

**1 · The antipodal pairing — 1 of 6.** It assumed the station numbering pairs
antipodally at an offset of twelve. It does not.

```
                    its hypothesis          solid.js
TANK        <->     HELIOSTAT               LENS            wrong
DAM         <->     OVERWINTERING           HELIOSTAT       wrong
FILTER      <->     CAST                    SEATING         wrong
STATIONS    <->     LENS                    OVERWINTERING   wrong
GROUND      <->     GARDEN                  GARDEN          ✓
DEEP        <->     SEATING                 CAST            wrong
```

§ 2 of this file already carried the true six, and `THE_CENTRE.md` § 5 already
singled out ↓TANK↔↑LENS and ↓DAM↔↑HELIOSTAT — **both confirmed exactly here.**
Where the two documents disagree, this file and `THE_CENTRE.md` are right.

The semantic readings it built on the wrong pairing go with it: *"DAM holds then
lets go against OVERWINTERING waits then comes back"* is a good sentence about a
pair that is not a pair.

**2 · The sounding is not a triangular face — and this was one of its two
constraints.** It asserted *"TANK → GROUND → DEEP … three mutually adjacent
seats: a triangular face."*

```
TANK—GROUND    a member
GROUND—DEEP    NOT a member
TANK—DEEP      NOT a member
```

One edge of three. The sounding is a **path the player walks through the game**,
not a face of the solid, and reading a gameplay loop as a geometric face is the
error underneath. **Its enumeration was therefore run with a false constraint and
still landed on the right family** — worth naming, because it means the 768
figure is not the number it would have been, while the two-shape conclusion
survives.

**3 · It picked the wrong family, and by its own argument.** It ruled *"the
12-sill family is the more likely of the two"* — 192 of 768 — on the grounds that
its single triangle per half would make the sounding *the* triangle in the
descent. **The build is the 8-sill family, 576 of 768.** The tie-break rested
entirely on correction 2, so it fell with it. The larger family was the answer and
the semantic argument talked it out of the count.

**4 · The ↓STATIONS reading goes with it.** It argued STATIONS carries internal
degree 1 — *"the least-connected station in the descent, sending into a hemisphere
it barely touches"* — and read that as agreeing with its mechanism names, *the
void return* and *the null sink*. **Its internal degree is 3.** The reading is
unsupported by the geometry it claimed support from.

### WHAT THIS IS EVIDENCE OF

The document did the honest thing twice — it announced itself as a hypothesis,
and it named the one file that would settle it. Both of those held up. **What
failed is every place it reached past the computation into a semantic tie-break:**
the offset-12 pairing, the sounding-as-face, the 12-sill preference, the STATIONS
reading. Its computed claims are 4 for 4. Its reasoned-from-meaning claims are
0 for 4.

*Which is the same finding `THE_CENTRE.md` § 11 reached from the other side, in
its own words:* the geometry scored 1872 of 40320 matchings equally and *"the
mechanical evidence does not pick one."* When the shape stops deciding, saying so
is the result. Filling the gap with a reading that sounds right is where both
documents lost.
