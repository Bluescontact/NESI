# THE DEPOSIT AS A GIFT — the world keeps what it takes, and gives it back

**2026-08-14, session 56dc26d3. Kevin's order: "…and then demonstrate the deposit as a gift."**
**Walked on a copy of his real poured water. His store untouched.**

---

## What was wrong, found by four seats reading blind

`ascent.html:193-195` — the hundred words committed at THE TANK went into `L.sent`, level scratch. `finish()` at `:185` discarded `L`. **The words became a pool height and nothing else.** On reload they were gone. The level named for holding the writing did not hold the writing — at the surface his own store calls *"the most load bearing surface of everything"* (`kevins-water.json` n:42), in the file built the day after he wrote *"notice how what was built never actually held the writing"* (`MARKS_LOG.jsonl:1020`).

And `ascent.html:419` — a sounding's outcome was `Math.random()<0.25`. **Nothing the hand had given the world determined where a deep was.**

## The articulation this serves — cited, per SEAM S3

| passage | what it says |
|---|---|
| `nesi/mind/LEARNED.md:121-124` | **"THE WORLD MUST RETURN WHAT IT TAKES."** Filed 2026-08-13, one day before the file that broke it. |
| `kevins-water.json` n:42 | *"Leve one the daily jounral is the most load bearing surface of everything."* |
| `kevins-water.json` n:32 | *"the soil view shows every sentence scattered, and hed individually."* |
| `counsel/reference/NESI_VISION_as_it_stands.md:14` | *"NESI never responds to what you write. It responds to that you wrote."* |
| `nesi/spec/THE_SOUNDING_2026-08-04.md:61` | *"the line is yours… the machine has no line."* |
| `nesi/spec/THE_VISION_WHOLE_2026-08-06.md:189-191` | *"Nothing puts a pattern in the ground. It was always down there."* |

**Why this is the gift, and not merely a repair.** A gift is given without a bill attached. The world now takes the writing, keeps it whole, says nothing about it, asks nothing for it, and — later, on a day you did not come back to check — hands one of them back to you unlabelled, unranked, with no reason given for that one. **Recognition without extraction, in one mechanic.** It is also the precondition of every other seat's dream: the sounding had nothing verbatim to surface, the confluence had nothing to mix, and the cast had nothing but counts to carry, until the world kept what it asked for.

---

## What changed — four edits, two files

1. **`level_one.html:99`** — the spawn moved from `RADIUS+300` (50 px **outside** the exit threshold) to `RADIUS+200`, inside it. The leave-fade no longer runs from frame one.
2. **`ascent.html`** — **Escape closes the writing panel** without committing. The level is left unfinished and re-enterable; nothing is kept and nothing is said.
3. **`ascent.html`** — `S.kept`: every sentence committed at THE TANK, held verbatim, appended, never trimmed, never reordered, never read for content, never counted at any door. **It is not in the cast and never will be.**
4. **`ascent.html`** — THE DEEP's sounding reads the ground instead of a coin. Sentences lie along the width in the order they came, **oldest deepest**. A line over settled ground finds bottom and brings one back verbatim. A line where nothing settled runs out — a deep, and nothing records where the deeps are, because **a deep is an absence and absence is not written down.**

`drawKept(i, x, y)` takes an **index into the store**, never a string. Machine wording cannot reach the screen through it — `own()`'s shape, from `world.html:433-452`.

---

## The walk — played from a cleared store, no edited save, no console shortcut

Real events dispatched into the page; the loop advanced frame by frame because the pane does not composite. Levels 1→6 played in order; **THE DEEP was reached by walking, not by unlocking.**

```
the door out ......... PASS  typed "this sentence must not survive escape." → Escape →
                             panel closed, view=map, done[1]=false, kept=[], store: absent.
                             Quitting mid-sentence lost nothing and nothing said so.
the tank keeps ....... PASS  92 words of his real water typed through beforeinput.
                             typedIntact=true (no corruption). 9 sentences kept, verbatim.
persistence .......... PASS  full page navigation, fresh JS context.
                             JSON.stringify(S.kept) === his 9 sentences — byte-identical.
↓2 DAM ............... PASS  head 1.00 while held; wheel turned 8.4 only after release.
↓3 FILTER ............ PASS  6 caught by hand across film / suspended / bedload.
↓4 STATIONS .......... PASS  spire 2 · lake 2 · set-down 2. Set-down produced nothing.
↓5 GROUND ............ PASS  14 settled. unlocked(6)=true — walked, not edited.
↓6 THE RETURN ........ PASS  line dropped over settled ground at x=198:
                               ran out .... false
                               came up .... "buckminster fuller, The first tetra and
                                             level is the tank."
                             line dropped 38 px away, over nothing, at x=236:
                               ran out .... true   (depth === screen height)
                               what came up unchanged — a deep shows nothing
                             oldest sentence lies deepest (260) — derived, never shown
```

**The pixels, read off the canvas buffer rather than from state:**

```
with a sentence up ... 1329 lit pixels, x 69 → 643      ← his words are actually on screen
with nothing up ......    0 lit pixels                   ← and nothing is drawn when nothing came up
restored ............. 1329 lit pixels, identical
```

**Level One's opening frame, the counterfactual run both ways:**

```
old spawn (y=550) .... 2 distinct colours · centre pixel [181,173,158] ≈ the flat wash
repaired  (y=450) .... 20 distinct colours · centre pixel [42,42,48]   ≈ a world
```

**His store:**

```
kevins-water.json SHA256 before : 928F860E…A70D59BB
kevins-water.json SHA256 after  : 928F860E…A70D59BB
UNTOUCHED                       : true
```

`nesi.html` was not touched; its integrity gate stands.

---

## What was deliberately NOT built, because it is his

- **The cast was not altered.** It still carries `lenses · ground · shoots · rooted · soundings · routed · sank`. All four seats found this and all four declined to rule it. Cowan's warning governs: *"stripping the export does not answer whether the increment may exist; it makes the open fork harder to see."*
- **The set-down counter stands.** Removing it would close a fork Kevin holds open.
- Law 9's three readings, the twelve-level shape, which surface is the game, the fourth number, and the workshops-versus-rungs question: all untouched.

## At the gate — the gift itself, escalated

Under the build order, three things still reach Kevin: sovereignty, body, and **a gift at the gate**. The cast is the gift at the gate.

> **May the cast carry counts at all, or must the shape be geometric?**
> **And what exactly departs when the fruit departs — the fruit only, or the stand and the ground it grew from?**

Today the file that leaves is seven tallies of a person's acts, including a count of the acts whose whole content is that they leave no trace. Composite's line, offered as reading and not as a ruling: *a cast of counts is a scoreboard that happens to be outside the game.*

---

## Standing

**UNWITNESSED.** This walk was run by the hand that made the change. Per `LEARNED.md:19-21` there is no WALKABLE without a stranger read. The same re-marking is owed to `THE_ASCENT.md:3` and `LEVEL_ONE_REPORT.md:3`, on game-craft's verdict — **not applied here, because re-marking another pass's report is that pass's business or Kevin's, not this one's.**

**What was checked:** the store, the persistence, the return, the door, the spawn, the pixels on the canvas buffer, the hash.
**What was not:** the screen as a human eye sees it — the Browser pane does not composite in this session, so no screenshot exists. The pixel counts above are read from the canvas buffer, which is stronger than state and weaker than sight.
**Also not fixed, and named rather than lost:** `ascent.html:455` still halves the 500 with `Math.ceil(w.length/2)` — the machine's hand where the design says the player's. Six level-number comments are still on the wrong levels, the filter comment still claims a fourth fraction that does not exist in code, and `cap:500` still sits dead on THE DAM.

## Run it

```bash
python -m http.server 8731 --directory "nesi/game2d"
```

`http://localhost:8731/` · write at THE TANK, walk down to THE DEEP, drop a line where you think you put something.
Walk it fresh: clear `localStorage` key `nesi.ascent`.
