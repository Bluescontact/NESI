# FORKS RESOLVED — 2026-08-23, swarm pass

**Standing: SWARM-DECIDED. Not MARKED.** Per THE_VISION §14's own taxonomy, this
is a distinct kind from the seven listed there — call it what it is: a
computation over the corpus by agents, not Kevin's own hand at the gate. He
authorized the swarm to actually decide these forks (explicit override of
"never defaulted, each one Kevin's," given directly, not inferred) — that
authorization is real, but it authorizes the *deciding*, not a silent promotion
to canon. Each line below is a candidate until he marks it. Strike, amend, or
mark-as-is, per line.

**Method:** one agent grounded every decision in `solid.js` itself (the file
these docs already name as authority) rather than in the docs' own prose. Four
decision clusters then read that grounding plus THE_VISION and THE_SHAPE OF THE
WHOLE in full and decided. A fifth agent reconciled all four clusters against
each other and caught one real conflict (line 10) before this list existed.

---

## GEOMETRY

1. **Diameter is not a level. Count stays 24.** — `solid.js`'s own code
   disqualifies radii/diameters from `MEMBERS`/`ADJ`/`ROUTES` by construction;
   promoting one would contradict the code's existing vocabulary, not complete
   it. Closes THE_VISION §7 and §16.

2. **The four loops are the shape; the ascent is the contents.** — No single
   walking order is even representable without revisiting seats (every seat
   sits on 2 of 4 circuits); every circuit already closes on its own. Closes
   THE_SHAPE §VII gate item 1 exactly as that section's own reading suggested.

3. **The walking order stays undecided, on purpose — "permits many, enforces
   none" is itself the answer.** — Imposing one order would privilege one
   circuit over another for no reason the rigid structure supplies (rank 24, 0
   redundant, symmetric across all 6 mechanisms). Closes THE_VISION §16 as
   *decided not to decide*.

4. **The short circuit (TANK↔CAST, one member) stays — legitimate, uncut,
   uncosted.** — Confirmed a real, rigidity-verified member (PyRigi-checked),
   not an artifact. Cutting or taxing it would edit a cross-confirmed fact to
   serve a narrative preference. Closes THE_SHAPE §VII gate item 2.

## ASCENT DIRECTION AND THE COLLAPSE

5. **The build travels up the ascent, toward the vector equilibrium. No
   separate "descend the jitterbug" mode gets built.** — Siting a window is
   simultaneously an ascent-step and a mechanism-spend — one event, two
   readings, not two directions to choose between. Closes THE_VISION §16's
   direction fork.

6. **The cap at five still holds — but as the world's one permanent breathing
   point, not as portability protection.** — The original reason was already
   found wrong (first window sited ends the coordinated fold, not the sixth).
   Corrected reason: full siting (6/6) would drain every mechanism to zero —
   total rigidity, no square anywhere still folds. One square stays withheld
   permanently so the finished world keeps exactly one live, local fold
   forever. F6's mark stands; only the reason changes. Closes THE_VISION §10's
   open gap.

## CONTENT LAW

7. **Law 8 is corrected: the deep works on what's been sunk while the player
   is away. Nothing new can be sunk without the player present.** — Absence-
   as-mechanism is structural load-bearing across §5, the retention engine,
   and §3's "THE DEEP ANSWERS — while you are gone." THE_GAME.md's line is the
   standing law; THE_VISION §13's "stops when you leave" was the one-clause
   outlier and gets rewritten. Closes the §13/§16 collision.

8. **Law 13 gets a built instrument: `cadence_check`.** — Two checkable rules,
   the same shape as the existing eighteen instruments: (1) refuse any
   sub-day timestamp persisted to a player-history store; (2) refuse any value
   derived from comparing two-or-more timestamps against each other (interval,
   frequency, streak). A single stored date stays legal — display law source
   3 needs it. `daily.html`'s `S.past`/`S.returns` need auditing against rule 2
   immediately. Closes THE_VISION §16's "who enforces the thirteenth law."

9. **Colour routing: site sets base hue, hand's accumulated light-routing sets
   temperature/intensity — both, and this is which is which.** — THE_VISION
   §13 and THE_SHAPE §V already state the site half as settled; the heliostat
   mechanic's whole point is the hand's causal contribution, so temperature is
   the hand's channel. Both inputs stay strictly non-content — position and
   light-accumulation, never a string. Closes THE_SHAPE §VII gate item 3.

## STRUCTURAL PLACEMENT

10. **A worked seat draws nearer the centre — but not through the six
    jitterbug mechanisms.** *(adjusted by the reconciliation pass — the
    decision clusters initially collided here.)* Direction stands: more work
    shortens a seat's distance to the centre, because the centre is what
    everything else serves (§8). Mechanism corrected: the six TRADES
    mechanisms are already fully allocated as the ascent's own cost ledger
    (line 6) — spent once per window-siting, floored at 1 permanently. Routing
    continuous per-seat work through that same finite pool would double-book
    it. Key `RADII[n].length` directly to a seat's own accumulated
    worked-traversal count instead, as an independent quantity. Closes
    THE_VISION §16's sign question.

11. **The descent bowl's lens holds the five whole lower panes** — not nine
    facets, not twelve struts. A lens holds something transmissive; a whole
    pane is the only one of the three candidates that is a transmissive unit
    rather than a structural fragment (facets double-count shared edges,
    struts are skeleton). Closes THE_VISION §9/§16.

12. **What departs at the cast is the fruit only. The stand and its ground
    stay resident, permanently.** — CAST sits inside a closed circuit
    (confirmed, one member from TANK); if the stand/ground departed too, the
    circuit wouldn't close. Cast is copy-out, never move-out. Closes
    THE_VISION §12/§16.

13. **Arm siting, fixed to existing geometry, no new structure needed:**
    personal writing development → the six falling seats (TANK · DAM · FILTER
    · STATIONS · GROUND · DEEP); commons extraction and development → the four
    circuits (pre-authored closed walks — reading what's already there,
    matching the arm's own description); gift library → the six rising seats
    plus CAST as exit gate (HELIOSTAT · OVERWINTERING · CAST · LENS · GARDEN ·
    SEATING). The fall/rise split is the solid's own primary binary, already
    stored in `SEATS`. Closes THE_VISION §16.

14. **The centre test, applied:** the twelve-level ascent *sequencing*
    (`ascent.html`'s imposed single order) **serves itself** — demote it from
    enforced sequence to a suggested index; free circuit-hopping becomes the
    default, the fixed ascent an optional guided mode. *(This does not reopen
    line 5's direction — only the walking order among the four loops, which
    line 3 already left unenforced.)* The three arms **serve the centre**
    (they're the centre's circulation, per line 13, not a bolted-on
    abstraction). The cast mechanic **serves the centre**, once line 12
    applies (stand stays rooted; only the artifact ships). Closes THE_VISION
    §8's "unapplied" note for these three pieces of apparatus.

---

## What this doesn't touch

Nothing here overrides COLD_WALK's open items — those need an actual human
stranger (fraction-picker readability, the other 11 seats walked start to
finish, the multi-day week mechanic, the `visit-writer.js` file-save dialog).
No swarm can close those; they're not forks, they're unfinished verification.

Nothing here closes the pattern THE_LEDGER names in its own last line — a
document read once, even a short one, isn't the filesystem fact that would
make "locate the decision, don't make Kevin re-derive it" actually enforced.
That's still open, and it's bigger than this file.
