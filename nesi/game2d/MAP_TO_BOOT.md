# MAP TO BOOT — NESI as infrastructure Kevin opens tomorrow morning

**His ask, 2026-08-16:** *"lets build a map toward completion of nesi as
functional personal infastructure kevin can boot tomorrow morning."*

---

## WHAT "BOOT TOMORROW MORNING" MEANS — already ruled, not re-decided

`index.html` carries the ruling in its own comment, quoting his brief:

> *"I want a game that holds my writing… I want to use the game rather then
> claude to process my dailly writing of 750 to 1500 words a day."*

~~**The door is `daily.html` and `index.html` already redirects to it.**~~

> **■ FALSE AS WRITTEN, AND IT CONTRADICTS HIS OWN MARK MADE 76 MINUTES EARLIER.**
> *(Corrected 2026-08-17, reconciliation pass. Original line struck above, not
> deleted.)*
>
> Kevin's mark, 2026-08-16 16:42, verbatim from `MARKS_LOG.jsonl`: *"THE DOOR IS
> THE NEXT BUILD. One world: index.html opens into a world holding daily,
> ascent, field, regathered, tiles — **not a redirect to one page.** Chosen
> against 3-of-59 reachable."*
>
> And the build agrees with the mark, not with this file: `index.html` is 230
> lines carrying ten links and a `HREF.TANK`/`SUB.TANK` table built on the solid.
> It is a world. It has never redirected.
>
> This line was written at 17:58 and reads as though the door question were still
> a redirect. The failure is the one `ALIGNMENT.md` §C names: *"I convert your
> rulings back into options."* Here it went further — a ruling came back as a
> settled fact that was its opposite.

So the target is not a new thing. It is: he opens one page, writes his morning
pages into it, and the system holds them — without Claude, without a login,
without a network.

---

## WHAT ALREADY WORKS

```
index.html                   the door — a WORLD, ten links, built on the solid.
                             NOT "→ daily.html". See the correction above.
   ├─ daily.html             HREF.TANK · "write into it"
daily.html                   takes writing, persists it, 1068 lines
   ↓ arrives into
nesi.ascent                  the twelve seats, 20 mechanics, reads solid.js
node.html                    the jitterbug (verified rigid), the gate,
                             and export/import as a portable file
```

**The daily → ascent loop is built.** Writing committed on the daily surface
arrives at the head of the tank.

**The jitterbug is built.** In `node.html`, and it is the real transformation:
strut spread 0.0e+0 at every angle, R/e running 1.0000 → 0.7134 toward 1/√2.
This session spent the morning specifying it; that file already had it.

---

## THE GAP, IN ONE LINE

**Everything exists and lives in three places that do not know about each other,
and none of it leaves the browser.**

---

## THE MAP — ordered by what it unblocks per hour

### 0 · BRING THE NODE IN — done

`node.html` is in the tree, parses, committed. It was in Downloads.

### 1 · THE NODE IS A FILE  ·  the largest single gap

Right now every word he writes lives in one browser's `localStorage`. Clear the
browser, change machine, and it is gone. **That is a page, not infrastructure.**

`node.html` already solves it and uses no `localStorage` at all — export to
`node-YYYY-MM-DD.json`, import back. Lift that pair into `daily.html`.

**After this step alone he can boot tomorrow and not lose anything.** Everything
below is enrichment; this one is the floor.

### 2 · THE GATE ON THE DAILY SURFACE

*Can you hold this today?* — arrivals, and open vectors that have not returned to
zero. Nothing stored about him: no radius, no profile, no history of what he
declined. **Silence is legal and leaves no trace.**

This is what turns a writing page into personal infrastructure — the surface
starts holding what is arriving at him, not only what he writes. Built, in
`node.html`, transplantable.

### 3 · THE JITTERBUG INTO THE ASCENT

`THE_SIX_MANIFESTS.md` has promised the transformation for months. It exists,
verified, in `node.html`. `ascent.html` already loads `solid.js`, which now
derives the six mechanisms, the six squares and the six trades.

Lift the canvas. The summit of the ascent becomes a thing that moves.

### 4 · ONE BOOT

`index.html` → `daily.html` carrying the writing field, the gate, and the export.
The other surfaces stay at their own addresses, unmoved, as `index.html` already
says they do.

---

## WHAT IS DELIBERATELY NOT ON THIS MAP

- **The 27-organ catalogue, the 26 interior positions, the sitings.** Mapping,
  not play. They keep.
- **NESI's ten.** Catalogued, unsited, and nothing is blocked on them.
- **The squares.** They stay empty; siting one jams a hinge.

---

## THE HONEST SIZE

Steps 1 and 2 are transplants of code that already runs, into a file that already
runs. Step 3 is a canvas and a slider. **None of this is new invention, which is
why it can land in a morning.**

The risk is not difficulty. It is that `daily.html` is the surface he actually
writes in, and it has nineteen asserted properties in `daily_walk`. Anything
lifted into it runs against that check before it counts.
