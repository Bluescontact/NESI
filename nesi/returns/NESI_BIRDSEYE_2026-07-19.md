# NESI BIRD'S-EYE — as-built vs vision · 2026-07-19
**Status:** STAGED — extraction/audit only. Nothing modified, nothing run, no engine called. Classifications are observations; every verdict is Kevin's mark.
**Read:** full `nesi/` tree · canonical vision doc found: `_INTAKE/NESI_SPEC_v1_2026-07-15.md` (v1.1, DRAFT — used as authority, session-prompt checklist as backstop) · plus `_INTAKE/2026-07-18_nesi_vision_booking_membrane.md` (raw, unscreened).

---

## 1 · THE MAP

| Vision element | Status | One-line note |
|---|---|---|
| Stack NESI → DOOR → OSG, private-first order | ALIGNED | Build order matches: interior first; front.py hard-walls outward intent ("membrane line unratified, the door is not open") |
| Interior: **digests** (metabolizer) | PARTIAL | drop→metabolize→lint→card→mark loop runs end-to-end, but every run lands on the stub — CLI installed, not logged in; fallback is loud, as law requires |
| Interior: **asks** (interrogator) | PARTIAL | v0 live, deterministic by law; Moves A+B built and wired to NESI-side ESCALATION_CONDITIONS.md; Move C deferred with a named seam |
| Interior: **remembers** (continuity) | ALIGNED | state.json + config + 6 real close snapshots (latest today 10:01); checkpoint-on-mutation on |
| Interior: **returns** (return circuit) | PARTIAL | Built, deterministic, writes nothing — but the 2026-07-17 audit found 0/23 holds carry machine anchors, so it can never fire; hold-time condition typing is a named deferred seam |
| Interior: **makes** (bench.py) | PARTIAL | Internal-complete: invoke() socket + 4 ops + land() with origin="made", no crossing flag; all output stub-badged by design |
| Surface: visual morphic workspace, app open = system up | ALIGNED | Native tkinter NESI.exe, two panes, worker-thread organ calls, single-instance mutex; organ-panes experiment composted 2026-07-17 with a note |
| Surface: conversational front, plain words at the door | PARTIAL | front.py v0: ROUTE→ASSEMBLE→RETURN, no-brain by design, asks on miss; semantic "classify" op stubbed on the bench socket |
| Gate: cross / hold / compost, Kevin marks | DIVERGENT | marks.jsonl is real (9 lines, all Kevin-era) but carries two verdicts outside the triad: "correction" and "uncross" — extension in practice vs the stated mark set |
| THE DOOR | ABSENT | Confirmed absent and lawful: membrane-controller deliberately excluded from bench.json ("crossing is unrepresentable as a job"); front enforces the wall at the entrance |
| OSG through the stack | ABSENT | OSG exists (deployed, outside nesi/) but nothing in NESI routes to it — expected, held behind the unbuilt DOOR |
| Engine seam: swappable, memory NESI-owned | ALIGNED | metabolize() is the one seam; ORGAN_CONTEXT.md passed explicitly; honest weld ledger names the 3 residual welds (engine boot, auth, vendor chat hook) — none carries NESI memory |
| Memory in NESI-owned files, not the engine's store | PARTIAL | mind/ + bench/ copies done and authoritative-on-paper, but vendor copies still EXECUTE everything (hook, dispatcher, skills, pointer); cold-start test not run; migration 5/7 steps done |
| Law: Terminal Law (daily conversational; interface ≤ movement) | ALIGNED | front defaults conversational; escalation rule extracted NESI-side with provenance and wired to Move A; ten-minute close ceiling carried in bench.json falsifiers |
| Finish test: runnable · holds Kevin · membrane lawful · circulates without him | PARTIAL | Runnable and mark-real: yes. Membrane lawful: yes (by incapacity). Circulates without him: no — engine stub + door absent means nothing real flows yet |
| (unnamed by vision) web shells index.html + demo.html | AHEAD | Pre-native-mark "Day 1 shell" + a public-shaped demo page; superseded by the native-window mark 2026-07-16 — reads as: scaffold |
| (unnamed by vision) conductor.py HTTP server, port 8799 | AHEAD | The pre-native web-serve path still in the codebase alongside nesi_app.py — reads as: scaffold, superseded |
| (unnamed by build) booking / coordination membrane vision | ABSENT | 2026-07-18 feedstock names Nesi as Kevin's booking membrane — vision-ahead-of-build, raw, staged for a substrate run per Kevin's same-day mark |

## 2 · AS-BUILT, IN BRIEF

- **The machine:** `NESI.exe` (tkinter, stdlib-only) over `conductor/` — core loop, metabolizer seam, lint bridge, plus four organ modules (interrogator, continuity, return_circuit, bench) and a conversational front. Two organs are deterministic by law (interrogator, return circuit); two are engine-socketed and currently stub-only (metabolizer digestion quality, bench production).
- **The loop:** drop → metabolize → lint → staged card → Kevin's mark runs whole. It breaks at digestion *quality*, not mechanics: `claude-cli` is installed (v2.1.212) but not logged in, so every engine call falls back loudly to stub-v0 (log + staged objects + close snapshot all badge it). The lint bridge also logged one bare `lint-error` on 07-17.
- **The gate:** `marks/marks.jsonl` carries 9 real marks (3 compost, 1 hold, 2 cross, 1 uncross, 1 correction) — all test-pile era plus one probe. Working, and already richer than the three-verb law (see mark 2).
- **The mind:** `mind/` holds PROTOCOLS, ORGAN_CONTEXT, ENGINE_SOCKET, ESCALATION_CONDITIONS, ARTIFACT_GRAMMAR, reflexes.md (at root), seed_construction_language, and the full memory mirror. `seed/` exists and is empty. Vendor side still executes all reflexes; migration checklist: steps 1–4 done, 5 contradictory (see mark 3), 6–7 not run.
- **State right now:** inbox 2 files · staged 5 objects (4 stub-drafts/runs awaiting marks) · held 23 · felt-read queue 17 — which is over the interrogator's felt_read_threshold of 15, so Move B should currently be asking.

## 3 · OPEN MARKS — awaiting cross / hold / compost / rebuild-later

1. **Engine go-live.** CLI installed, not logged in; every organ run is stub. The go-live gate (ENGINE_SOCKET.md) needs your one-time hand: terminal in DSS content → `claude` → trust → `/login`. reads as: vision-ahead-of-build.
2. **Mark-set divergence.** marks.jsonl carries `uncross` and `correction` beyond cross/hold/compost. Ratify the extended vocabulary, or rule those entries annotations-not-marks. reads as: build-drift (or vocabulary growth).
3. **Record contradiction.** reflexes.md checklist says step 5 (ARTIFACT_GRAMMAR.md) "not run," but `mind/ARTIFACT_GRAMMAR.md` exists. One record is stale. reads as: build-ahead-of-record.
4. **Migration back half.** Steps 6 (necropsy — now 5 vendor project dirs, 4 stale, the live two-minds hazard) and 7 (cold-start test + vendor retirement) not run; vendor copies remain load-bearing for everything that fires below cognition. reads as: vision-ahead-of-build.
5. **Return circuit starvation.** 0/23 holds carry machine anchors, so the organ that brings things back can never fire; the hold-time condition-typing seam is named but unbuilt. reads as: vision-ahead-of-build.
6. **Scaffold pair.** index.html + demo.html (and conductor.py's HTTP-serve path) predate the native-window mark. Compost, or hold with a named condition. reads as: scaffold, per anatomy_is_not_the_cockpit.
7. **seed/ is empty.** Created 07-16, never filled. Confirm empty-is-correct resting state or compost the dir. reads as: indeterminate from disk.
8. **Booking-membrane vision unscreened.** The 2026-07-18 feedstock (Nesi as booking/coordination membrane, villager-anywhere) is a vision element no build names. Substrate run owed per your stage-only mark. reads as: vision-ahead-of-build.
9. **M2–M5 unmarked.** Job grammar M1 ratified; bench.json stream placements are explicitly best-guess UNMARKED pending M5; M2–M4 open. reads as: vision-ahead-of-build.
10. **Live queue pressure.** 4 stub staged objects + 2 inbox files await marks; felt-read queue (17) is over threshold (15) — Move B is, or should be, asking. reads as: the machine returning to the gate, working as designed.

---
*Extraction pass complete. Read-only on the machine; this file is the only write.*
