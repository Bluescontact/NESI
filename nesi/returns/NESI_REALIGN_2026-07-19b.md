# NESI REALIGN — delta return (build pass) · 2026-07-19
**Marks executed:** 3 cross · 4 cross · 5 cross · 6 compost · 9 cross. Held: 1 (engine — all engine-dependent work skipped, as §3 requires), 7 (seed/ stands), 10 (no cull), 2 (fork, prepared earlier, untouched), 8 (yours to screen). Supersedes the all-held return of earlier today (`NESI_REALIGN_2026-07-19.md`).

## 1 · MOVED

- **Mark 5 — anchor seam BUILT (returns row: PARTIAL → ALIGNED at the seam).** The hold gesture can now carry an anchor: `core.record_mark(sid, "hold", condition=...)` stores the tag on the staged object and the ledger line; the native window's HOLD button asks once for an optional tag (blank/cancel = felt hold, exactly as before); the return circuit now scans NESI staged holds alongside gate_data, same two-shape grammar, still writes nothing. All three modules compile; a live scan ran silent (correct — no anchors exist yet). **Rebuild note:** NESI.exe is frozen; the seam lives in source until the exe is rebuilt (staged as new mark 3 below).
- **Mark 6 — scaffold COMPOSTED (AHEAD rows close).** `index.html`, `demo.html`, `conductor.py` (the port-8799 web path), and `start_conductor.bat` archived to `nesi/_compost/` with date suffixes and `COMPOST_NOTE_2026-07-19_web_scaffold.md`. Nothing imported conductor.py; live data (conductor_log.jsonl, seen.json) stayed. **The duplicate `uncross` resolved itself** — core.py's copy is now the only one.
- **Mark 3 — record contradiction FIXED.** reflexes.md checklist line corrected with a dated, struck-through note (discrepancy named, not smoothed); steps 6 and 7 lines updated to today's true state.
- **Mark 4 — necropsy STAGED (`NECROPSY_2026-07-19.md`).** Ground truth moved under the record: the four stale minds R7 named are **already gone** — only the live mind and a 6-file transcript-residue dir (no memory/) remain. Three one-line burials await your marks. **Cold-start test: BLOCKED on Mark 1** (needs a fresh engine), stated, not faked.
- **Mark 9 — M5 sheet STAGED (`M5_PLACEMENT_SHEET_2026-07-19.md`).** All 12 executables' best-guess placements in one table, one mark line each. Marking it closes M5.

## 2 · STILL HELD
- Mark 1 — engine; every stub stays a stub, loudly.
- Mark 2 — the fork (prepared in the earlier return; note: resolution B just got cheaper — conductor.py's copy is composted, so B now touches three files, not four).
- Mark 7 — seed/ stands as the named empty socket.
- Mark 8 — booking-membrane, yours to screen.
- Mark 10 — queue uncalled; felt-read 17 > threshold 15 still stands.

## 3 · NEW MARKS
1. **Necropsy burials ×3** — see NECROPSY_2026-07-19.md (vanished minds · residue dir · R7 rewrite).
2. **M5 sheet ×12** — see M5_PLACEMENT_SHEET_2026-07-19.md.
3. **Exe rebuild** — the Mark 5 seam (and the hold-anchor dialog) reach the running NESI.exe only after a PyInstaller rebuild; the current exe keeps working on the old behavior meanwhile. One mark authorizes the rebuild (backup kept, per the _compost pattern).
4. **R7 provenance** — the four stale dirs were removed by a hand no record shows. If it was you, one line closes it; if not, worth knowing.

---
*Build pass complete inside the marked scope. Nothing unmarked was touched; nothing was marked by the engine.*
