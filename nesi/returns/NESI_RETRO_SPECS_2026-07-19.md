# NESI RETRO-SPECS — the standing organs through the new canon · 2026-07-19
**Status:** STAGED — five organs filled through the ratified 9-field form, backward. Each spec awaits Kevin's mark: **stands** (becomes the organ's standing spec) / hold / compost. Findings are observations, not verdicts. Read-only on the machine; this file is the only write.

---

## 1 · METABOLIZER (digests)
```text
ORGAN:      metabolizer
PROBLEM     raw piles accumulate faster than Kevin can sort; every sit-down restarted from zero
FORCES      staging-only law vs useful dispositions · strip the writer, keep the world ·
            evidence local-only · engine absent (cost + Kevin's order)
FORM        dropped pile → staged object (items · dispositions · evidence, raw_text_inside)
            + lint annotations → card at the gate. Takes in: files in the inbox ·
            hands off: staged JSON to the mark queue · stops: at staging — never marks
FALSIFIER   if Kevin re-sorts by hand what it staged, or reading its output costs more
            than reading the pile did — compost
PLUMB       gate law (marks Kevin-only) · ORGAN_CONTEXT laws · the lint library — all true
FALSE CAR   stub-v0, loud fallback badge — running today
ENTRANCE    the inbox folder; Explorer drag IS the drop
INTERLOCK   never leaves the machine · never writes gate_data · never marks
SIGN-OFF    OPEN — the first-real-read quality check (staged 07-17) can only run on the motor
```

## 2 · INTERROGATOR (asks)
```text
ORGAN:      interrogator
PROBLEM     the escalation conditions and drift-awareness lived only in Kevin's head;
            nothing in the machine ever reached back
FORCES      asking vs prompting-the-body (body-line is never prompted) · silence-default
            vs usefulness · deterministic law vs richer matching (Move C deferred)
FORM        a drop → matched questions with marker provenance; own state → proprioception
            questions. Takes in: drop text + its own ledgers · hands off: questions only ·
            stops: never marks, never answers
FALSIFIER   a question with no matched marker (fabricated trip), or non-silence by default —
            compost
PLUMB       ESCALATION_CONDITIONS.md NESI-side with provenance · Kevin-editable thresholds — true
FALSE CAR   NONE NEEDED — deterministic by law; it has always run real
ENTRANCE    its own tab in the window
INTERLOCK   Move C, when built, must not route through an engine
SIGN-OFF    mostly done (defaults marked, live trip observed); one open: Kevin has not yet
            SEEN Move B ask in the window while the felt-read pile sits over threshold
```

## 3 · CONTINUITY (remembers)
```text
ORGAN:      continuity
PROBLEM     Kevin was the persistence layer; every open re-derived where things stood
FORCES      derived-view-only vs becoming a store he must feed · zero new gesture allowed ·
            quiet cycles must cost nothing
FORM        ledgers → resume board on open · change-detected checkpoint · close snapshot
            with retention. Takes in: existing ledgers · hands off: a read-only board ·
            stops: never authoritative, never asks
FALSIFIER   the day Kevin has to feed it, or the board contradicts the ledgers — compost
PLUMB       marks.jsonl + gate_data as sources of truth — true
FALSE CAR   NONE NEEDED — deterministic; always real
ENTRANCE    the resume board, first thing on open
INTERLOCK   never a store; the cross-sitting drift seam (Move B) stays documented, unbuilt
SIGN-OFF    DONE in practice — six real closes on disk, latest this morning
```

## 4 · RETURN CIRCUIT (returns)
```text
ORGAN:      return circuit
PROBLEM     held things never came back; Kevin was the reminder system for his own holds
FORCES      anchor-only law vs a tray of 23 felt holds (honest starvation) ·
            never fabricate "condition met" · prose never parsed
FORM        scan gate holds + NESI staged holds for fired anchors → a question card, no
            buttons. Takes in: the hold trays · hands off: questions to the gate · stops:
            writes nothing, anywhere
FALSIFIER   one fire without an explicit tag — compost on the spot
PLUMB       anchor grammar (two shapes) · hold-time condition typing (Mark 5, built) — true
FALSE CAR   none — deterministic; real but functionally idle (zero anchors exist)
ENTRANCE    the "returned to the gate" strip under the resume board
INTERLOCK   never marks, never promotes, never crosses
SIGN-OFF    OPEN — it has never fired on a real anchor; the first tagged hold is its
            commissioning test
```

## 5 · BENCH (makes)
```text
ORGAN:      bench
PROBLEM     pattern → new-object work (draft, break, refine) was hand-cranked in chat
FORCES      making vs crossing (no eligibility flag, ever) · internal-only production ·
            pull quality without pattern tags · workbench-last (built early anyway, marked)
FORM        invoke(op): draft / break / refine / semantic_pull → landed staged object,
            origin="made". Takes in: intent + pulled patterns · hands off: objects to the
            existing mark queue · stops: inside the private organ, always
FALSIFIER   a made object carrying crossing-eligibility, or bench output moving outward —
            compost. Second edge: if the loop goes unused, it is scaffold — cut it
PLUMB       PARTIAL — bench.json registry is M1-ratified, but the stream placements are
            explicitly UNMARKED (M5). The organ stands over one un-plumb line.
FALSE CAR   all four ops stubbed and badged — running
ENTRANCE    the bench tab
INTERLOCK   engine ops absent until the motor; making is not crossing
SIGN-OFF    internal-complete (Kevin's mark 07-17); production quality awaits the motor
```

---

## FINDINGS ACROSS THE FIVE (facts, not verdicts)
1. **Two organs never needed a false car.** Interrogator and continuity are deterministic rails that have always run real — fully commissioned today. The grammar's FALSE CAR field is honestly "none needed" for them, which is itself a reading: not every rail needs temp power.
2. **The motor gates exactly two sign-offs.** Metabolizer and bench — the two engine-socketed organs — are the only ones whose commissioning cannot finish on the false car. Everything else can reach sign-off dark.
3. **Bench has the one un-plumb line.** Its PLUMB field wouldn't fill clean: the M5 placement sheet (staged, unmarked) is literally the plumb line under a standing organ. The grammar found this in one pass — the M5 sheet is not bookkeeping, it's rail-truing.
4. **The return circuit is a rail set true awaiting first load.** Built lawfully before any anchor existed; its commissioning test is one tagged hold away.
5. **The conversational front is not an organ — it's an entrance.** It filled the form badly on purpose (no PROBLEM of its own; it routes). That's Line 4 territory (the held entrance rule), and evidence the rule is real.
6. **One live commissioning item is free right now:** the felt-read pile sits over threshold (17 > 15), so opening the window and seeing Move B ask closes interrogator's last sign-off line with zero build.

## MARKS SHEET — one line each
| # | Organ | Spec verdict awaited |
|---|---|---|
| 1 | metabolizer | stands / hold / compost: ______ |
| 2 | interrogator | stands / hold / compost: ______ |
| 3 | continuity | stands / hold / compost: ______ |
| 4 | return circuit | stands / hold / compost: ______ |
| 5 | bench | stands / hold / compost: ______ (finding 3 rides with it: M5 is its plumb) |

*Marked "stands" specs get written into the organ's bench/ dir as its standing spec. None written yet.*
