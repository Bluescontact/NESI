# NESI BENCH SPEC — through the construction language · 2026-07-19
**Status:** RENDERED, NOT CANONIZED. The center is left open — it is the first line awaiting Kevin's mark, and every other field hangs off it. Read-only on the machine; no engine lit; this file is the only write.

## 1 · THE REAL BENCH vs the sketch

The sketch said draft · break · refine. The code says more, and the more matters:

- **Five ops, not three.** `invoke()` carries draft / break / refine — and also **semantic_pull** (which IS the interrogator's deferred Move C, one shared matching call, Kevin's confirmed default) and **classify** (the front's semantic-routing upgrade). **Finding: the bench is already a shared socket serving two other organs' semantic needs.** Its de facto boundary is wider than "maker" before any center is chosen — whatever center Kevin names, the socket also carries the asking-organ's library-matching and the front's routing.
- **One op is already real.** `keyword_pull` — the deterministic pattern-pull floor (slug + H1 + bold-thesis word overlap, misses quietly) — produces true content today, engine-off. Everything else is loud stub.
- **Break exists in code, honestly stubbed:** the stub says exactly what a real reader would do ("attack the draft's claims against the pulled patterns") and does nothing — correct.
- **The code already leans on a center-ish line** (fact, not verdict): its own docstring reads "the digestor runs world → pattern; the bench runs pattern → object — outward, but into the PRIVATE organ, never out the door," and `land()` enforces it (origin="made", NO crossing-eligibility, membrane unknown to the module). As written, the code's posture is internal. That is the current state, not the mark.
- **History is kept per object** — every draft/break/refine appends to `history`, and break notes ride in the landed object. The trail law already lives here.

## 2 · THE SPEC, RENDERED (canon 9-field form; CENTER open)

```text
ORGAN:      the bench
PROBLEM     pattern → new-object work was hand-cranked in chat; NESI's one-line
            purpose (raw living → transferable pattern → made thing) had no organ
            where the MAKING half happened
FORCES      making vs crossing (no eligibility flag, ever) · generative freedom vs
            the monument risk (draft+refine without break) · one socket now serves
            three organs (bench ops + Move C + front classify) · workbench-last law
FORM        invoke(op) → draft / break / refine / semantic_pull / classify;
            working object with full history; land() → staged, origin="made".
            Takes in: intent + pulled patterns (+ held things / + outward intents,
            per the center) · hands off: objects to the existing mark queue ·
            stops: inside the private organ — the membrane is downstream and shut
FALSIFIER   a made object carrying crossing-eligibility → compost. A bench that
            emits without breaking → it's a monument mill; cut the emit until
            break is real. A bench unused → scaffold; cut it.
CENTER      ⟨OPEN — A / B / C below, or one-bench-three-modes. Kevin's mark.⟩
BOUNDARY    ⟨follows the center: specs / held-things / gifts / all⟩
PLUMB       the construction language (canon today) · bench.json M1 · M5 now the
            named truing line (per the standing spec's plumb-gap note)
FALSE CAR   running now — all five ops stub-badged; keyword_pull real
ENTRANCE    the bench tab (its own way in)
INTERLOCK   center A: none outward · center B: reads the tray, never marks it ·
            center C: THE DOOR — can forge a gift, cannot cross it; possession
            isn't permission. All centers: making is not crossing, ever.
SIGN-OFF    Kevin's mark on the center wakes the rest; production quality still
            motor-gated after that
```

## 3 · THE CENTER FORK — three candidates, held open, consequences mapped

| | **A · organ-spec generator** | **B · held-thing maker** | **C · transmission engine** |
|---|---|---|---|
| One breath | the bench drafts organ specs through the 9-field form and breaks them against their falsifiers | the bench takes a rich felt-hold and forges it into a made object | the bench is where living becomes transmissible gift |
| BOUNDARY | emits: specs | emits: made-things from held feedstock | emits: gifts, essays, transmissions |
| RAILS | the canon seed form · the library | the return circuit · the hold tray · the library | OSG · the transmission-engine templates · the library |
| INTERLOCK | none outward | reads the tray; never marks or culls it | THE DOOR — forge yes, cross never, until the membrane exists |
| Faces | inward (the language itself) | the tray (23 holds as feedstock) | the world (through a shut door) |
| What it commits | the language becomes generative — it specs its own next organ | held ≠ stuck: the tray gains a destination besides return/compost | the "internal tooling" was the gift-forge all along; DOOR rises in priority |

**Observation, not verdict:** the code's shape doesn't force a single center — `invoke()` is op-agnostic and would serve all three as *modes* behind one socket. But the sketch's deepest line cuts the other way: C isn't a mode, it's an identity claim about where NESI's purpose lives. Whether these are three modes of one bench or a true single-center fork is exactly what the mark decides. Both readings are honest; neither is chosen here.

## 4 · THE BREAK STEP — developed, center-independent

Break is the falsifier moved inside the making. Developed concretely:

- **What break attacks, in order:** (1) **the object against its own bearing patterns** — does the draft contradict a pattern it claims to bear? (each pulled pattern is a stated commitment; break reads the draft against each); (2) **the object against its own falsifier** — every made object MUST carry a falsifier line at draft time (grammar convergence: the 9-field form's FALSIFIER, applied to outputs, not just organs); a draft that can't state how it would fail doesn't proceed to refine; (3) **the depletion test** — does holding/maintaining this object cost Kevin more than it gives (personal-scale law, applied at the forge).
- **What break emits:** typed notes — `{claim, attack, verdict: survives | falls}` — never prose mush. Refine must address every `falls` or record why not; unaddressed falls ride visibly on the landed object (the same never-silent law as un-mark).
- **When break is done:** loop-until-dry — break runs again after refine; the object is forge-done when a break pass returns nothing new. An agreeable break (zero falls, first pass, every time) is a dead break — the same falsifier as a dead tetra cycle.
- **What's runnable TODAY, engine-off:** a deterministic partial break exists in the machine already — the lint (nearest-pattern distance against the 89) is an honest first attack ("your object claims novelty; it sits 0.3 from an existing pattern"). Wiring lint-as-break-floor needs no motor. The adversarial reading itself is motor-gated and stays loudly stubbed until then.

## 5 · LINES AWAITING A MARK
1. **THE CENTER** — A · B · C · or "one bench, three modes." First line; everything else hangs off it.
2. The rendered spec (§2) as the bench's amended standing spec, once the center fills.
3. The break-step development (§4) as the bench's break law — markable under any center.
4. Lint-as-break-floor — the one break piece buildable dark; a small build mark if wanted.
5. The shared-socket finding — semantic_pull (Move C) and classify formally named in the bench's boundary, whatever the center.

---
*Rendered fully, chosen nowhere. The center is the mark, and the mark is Kevin's.*
