# NESI — FULL BUILD REPORT (as-built, whole machine)
**Date:** 2026-07-20, pre-travel close. **Compiled from:** live disk state, nesi/mind/, nesi/returns/ (07-19 set), bench STANDING_SPECs, marks/gate ledgers, this session's seal.
**Status line:** window LIVE and current · engine DARK by design · all interior organs built and specced · order of the deterministic loads waits on the fork, which now has a landing.

---

## 1 · The frame
NESI = the recognition machine Kevin is extracting from himself; private-first (NESI → door → OSG). Governing laws standing: anatomy_is_not_the_cockpit · staging-only, the mark is Kevin's touch, always · fail-closed · no sign-in ever (D3, Kevin's law: local desktop-gated exe; vendor auth is engine plumbing, not a NESI gate) · the elevator sequence (seed_construction_language.md, PARTIALLY CANONIZED): plumb/laws → rails/organs → **false car on temp motor = STUB ENGINE as a required phase** → cables+motor = engine LAST → surfaces last.

## 2 · The window (the built thing you double-click)
`nesi\NESI.exe` — tkinter native, onefile windowed ~11MB, **rebuilt + swapped twice 2026-07-19**; current build carries the anchor-seam hold dialog and the ↺ un-mark toggle. Rollbacks in `nesi\_compost\` (pre-anchor-seam, pre-mark-toggle). Web scaffold (index.html, demo.html, conductor.py HTTP shell, port 8799) COMPOSTED 07-19 — the window is the only surface. Desktop shortcut stands. Tabs: **nesi (front, landing tab) · metabolizer · bench · interrogator**, plus the continuity resume board and the return-circuit strip.

## 3 · Organs — as built (all deterministic; one engine seam total)
| Organ | State | Notes |
|---|---|---|
| **Metabolizer loop** | LIVE | drop → metabolize() seam → staged JSON → lint (codex_index bridge) → card → Kevin's mark to marks.jsonl. STANDING_SPEC on file. |
| **Bench** | LIVE (stub) | THE one socket `invoke(op)` — draft/break/refine/semantic_pull, all loud stubs; land() → origin="made", no crossing flag. Bench center question OPEN (A/B/C/modes, Kevin's read). |
| **Interrogator v0** | LIVE | check_drop (7 escalation conditions, verbatim markers) + proprioception (first-seen ledger; 23 helds stamped 07-17 → age question wakes ~08-07). Move C deferred to bench.semantic_pull seam. |
| **Continuity v0** | LIVE, exercised | state.json + history/ (retention 14); resume board on open; X-click hooked to close_snapshot. **Ran today for the travel seal.** |
| **Return circuit v0** | LIVE, truthfully silent | fires only on explicit anchors (`until YYYY-MM-DD` / `when file:` ). Anchor seam now in the window (first HOLD asks once; blank = felt). All 23 current holds are felt → silence is correct. |
| **Front v0** | LIVE | deterministic verb table, no brain, never marks; outward wall: send/publish → "membrane unratified," stop. |
| **Fork landing** | **BUILT TODAY** | Google Doc "NESI — fork landing" (gift-library object #1) + `mind/FORK_LANDING.md` return step → nesi\inbox\ at the gate. Mailbox, orders nothing. |

Mark machinery: cross is sync-loud (write → index rebuild → then mark; rollback proven) · **uncross RATIFIED** as fourth Kevin-only verb · un-mark toggle round-trip verified · second mark cannot overwrite the first · no approve verb anywhere.

## 4 · The engine (dark, lawfully)
Stub = engine #0, the socket-thesis proof; every stub output loudly badged, real-engine failure falls back LOUD. Claude Code CLI v2.1.212 installed, **not authenticated** — go-live is Kevin's hand alone (terminal in DSS content → `claude` → trust → /login). Badge honesty fix flagged (label says installed, not live). Motor gates exactly **two sign-offs: metabolizer + bench**. Membrane line (Move 3, "the cross-touch is the crossing") still UNRATIFIED — named precondition before the first real cross. Per the elevator sequence and BUILD_DECOUPLES_FROM_PRIORITY, engine-last is not lag; it is the build order.

## 5 · The mind (NESI-side authority)
PROTOCOLS.md (vendor CLAUDE.md inverted to pointer; test boot PASSED) · ARTIFACT_GRAMMAR.md RATIFIED · ORGAN_CONTEXT.md (laws + fixed vocabulary for headless runs) · ESCALATION_CONDITIONS.md · seed_construction_language.md (lines 1/2/3/5 CANON; 4 + 6 held) · COLD_START.md · FORK_LANDING.md (new) · full memory mirror. Migration 1–6 ✓; **remaining: step 7 cold-start + vendor retirement · UserPromptSubmit hook still vendor-side (R1) · R4 permissions prune.** Mirror discipline: NESI-first, vendor mirrors.

## 6 · Retro-specs and construction language
All five organs carry STANDING_SPEC.md in bench dirs (07-19, all marked STANDS). Grammar proven by generating the booking-membrane spec (9 fields, false car = hand-carry template). Findings on file: two organs born real, no false car needed · front is an entrance, not an organ (evidence for held Line 4).

## 7 · Everything awaiting Kevin's hand (the complete open-marks board)
**Engine chain:** /login (his hand) → first real metabolization quality read → membrane-line ratification → first REAL cross.
**Hand-tests:** hold dialog (anchor prompt) · one un-mark round trip — both one minute each, in the live window.
**Marked sheets:** M5 stream placements ×12 (sheet staged) · necropsy burials ×3 · Mark 8 booking-membrane screen · Mark 2 fork — RESOLVED 07-19 (D1=A).
**Construction language:** Line 4 entrance rule · Line 6 booking spec.
**Bench:** center question (no decay clock).
**Substrate/gate:** 3 swarm promotions AT the membrane (verified today, not crossed) · H001–H003 rescreen_due · H004 held (anchor unmet) · H005 = the fork, held by design · mark-apparatus daily-cycle wiring proposed-unmarked (floor question attached) · gift-aporia 4 holds + 5c/5e forks · loads-pass marks pending.
**Infrastructure:** OneDrive whole-tree sync vs today's ratified storage rule (local ground / Drive gift library) — relocation held post-Rosebud · badge honesty fix · felt-read 18 > threshold 15 (Move B will ask; free commissioning item noted 07-19).

## 8 · Today's delta (this session)
Storage rule RATIFIED (local dev/host · Google Drive/Docs = gift library, per-item crossings at Kevin's gate). Fork landing BUILT (Doc + return step). Travel seal RUN: everything pending verified holding without decay (tray 23 · felt 18 · inbox 2, unchanged), falsifier clean, snapshot written, cycle log appended. **Next open resumes on named threads.** The one live thing — the fork — now travels with a vessel instead of waiting at the bench.

**The machine in one line:** interior set closed, window current, engine dark on purpose, every gate human, and the only thing it is waiting for is the one thing it must never ask for.
