# NESI REALIGN — delta return · 2026-07-19
**Status:** STAGED. The §2 fill arrived with every line unedited (all options still standing). The fill doc's own law: an unedited line reads as HOLD — silence is not "proceed." So the build gate stayed closed. No engine login, no seam work, no compost moves, no code touched. The only work done is what the prompt fixes as engine-lawful regardless of marks: preparing the Mark 2 fork and surfacing the Mark 8 substrate. Both read-only.

## 1 · MOVED
Nothing. All eight fillable marks (1, 3, 4, 5, 6, 7, 9, 10) read as HOLD per the unedited-line rule.

## 2 · STILL HELD
- Marks 1, 3, 4, 5, 6, 7, 9, 10 — held by silence; re-send the fill with edited lines to open any of them.
- Mark 2 — constitutional fork, held by the prompt itself (prepared below, not resolved).
- Mark 8 — Kevin screens (surfaced below, not screened).

## 3 · THE MARK 2 FORK, PREPARED (not resolved)

**Where the two verbs actually live — the fork is asymmetric:**

- **`uncross` is BUILT, not a stray ledger line.** It is implemented twice (`conductor/core.py:332` and `conductor/conductor.py:331` — a rollback verb that pulls a crossed pattern from canon into `patterns\_folded\`, stamps the staged object `uncrossed`, and appends `verdict: "uncross"` to marks.jsonl), surfaced as a button in the native window (`nesi_app.py:694`), and counted as a first-class verdict by continuity (`continuity.py:79` filters to exactly `cross/hold/compost/uncross`). It fires only on Kevin's touch. The build treats it as law already.
- **`correction` is NOT in code anywhere.** It exists as one hand-appended marks.jsonl line (2026-07-16, the stale-zombie-server incident). Every reader ignores it (continuity's filter excludes it). And `mind/ARTIFACT_GRAMMAR.md` §4 already sanctions the *principle*: "every correction stays on the page struck-through or noted, never silently overwritten."

**What each lawful resolution would touch:**

- **A · Raise the law.** Ratify `uncross` as a fourth mark verb (Kevin-only, rollback-with-record) and rule `correction` a ledger *annotation*, not a mark. Touches: vocabulary text only — PROTOCOLS/spec mark-set lines, ORGAN_CONTEXT vocabulary block, the bird's-eye DIVERGENT row. Zero code changes; the build is already coherent with this reading.
- **B · Strip to the law.** Remove `uncross` from core.py, conductor.py, nesi_app.py (button), continuity.py (filter), and rule the two ledger lines annotations. Touches: four code files plus the native UI; removes the only lawful walk-back path (the alternative becomes compost-and-recross). Note: this cuts a capability Kevin has used twice in the ledger's nine lines.

**Observation held as observation:** the build, the ledger, and ARTIFACT_GRAMMAR all lean A; the stated triad leans B. The verdict is Kevin's mark.

## 4 · THE MARK 8 SUBSTRATE, SURFACED (not screened)

- **The feedstock:** `_INTAKE/2026-07-18_nesi_vision_booking_membrane.md` — Nesi as booking/coordination membrane; hosts name their asks in a form Kevin can filter without downshifting into transactional logic; "pre-transactional infrastructure that allows Kevin to operate as a villager anywhere"; the somatic diagnostic (anxiety of not feeling both edges of an exchange) named as the load-bearing part.
- **What the vision canon says:** `NESI_SPEC_v1` (v1.1) does not name it. Nearest existing structure is THE DOOR (outward membrane) — but the booking membrane is *inward-facing coordination with specific people*, which neither NESI-interior nor DOOR/OSG currently covers. It is a genuinely new pole, not a refinement of an existing one.
- **Status:** raw, stage-only per Kevin's 2026-07-18 same-day mark ("no Nesi work"). A substrate run is the named next step; Kevin screens.

## 5 · NEW MARKS
1. **The fill-slot delivery path.** The §2 fill arrived unedited twice (the realign prompt, then the fill doc itself). If the intent was all-hold, this return is correct; if marks were meant, the crossing didn't survive the paste. Worth one line either way.
2. **`uncross` duplication.** The verb is implemented in both `core.py` and `conductor.py` (pre-native and native paths) — a two-copies drift risk of exactly the kind the mirror discipline names. Fold whenever Mark 2 resolves (either resolution touches it).

---
*Delta return staged. The gate is Kevin.*
