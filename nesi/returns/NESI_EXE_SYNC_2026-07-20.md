# NESI EXE SYNC — delta return · 2026-07-20 (session 2/3)

**Status:** DONE. Rebuild only, no relight. Old exe archived before swap; rollback intact.

## 1 · NOW LIVE (in the running window, after your next double-click)
- **Move A, open-reach (session 1).** The interrogator reads its own state and speaks first on the "nesi" tab, before you've typed anything, when there's actually something to say.
- **Move B wired broadly (session 1).** The 7 escalation conditions now check every front drop, not only ones you route to the interrogator by keyword.
- **Move C, absence-against-library (session 1, new organ).** Drops get checked against the pattern library's own titles; unnamed load-bearing patterns get named.
- **Bench type tag (session 2, new).** Any intent you give the bench — front tab or bench tab — gets classified as letter / note / instrument / spec / reflection / gift / object, deterministically, off your own words. Shows as a badge next to the engine badge on the bench tab; rides into the staged pile name when you land it.

## 2 · THE LOAD MODEL — unchanged from 07-19
Hybrid: code is a bundled PyInstaller onefile snapshot (baked in at build time — this is why today's wiring needed a rebuild to reach the window); data (inbox/staged/marks/mind/config) resolves live from disk next to the exe, unchanged.

## 3 · STILL DARK / STILL HELD
- Mark 1 — engine dark; no login attempted; stubs still loudly `STUB`.
- Cold-start — still blocked on Mark 1.
- Mark 2 — fork untouched.
- Felt-read queue — still 18, three over the standing threshold of 15 (Move A already named this once, unasked).
- Bench TYPE_MARKERS — six guessed tags, unreviewed against your actual drop history (session 2's open card).

## 4 · VERIFICATION RECORD (before swap)
- Headless smoke, this session: `interrogator.open_reach()`, `interrogator.check_absence()`, `front.handle()` end to end, `bench.new_object()` → `run_break()` → `run_refine()` → `land()` — all run against real files, engine dark, correct output. Smoke-test staged object removed from the live mark queue after verification.
- Zombie check first (lesson of 07-16/17): no NESI.exe running before build or swap.
- Built to scratchpad dist (PyInstaller 6.21.0, onefile + windowed, 11.2MB), copied to nesi\ as NESI_new.exe so data resolved to the real dirs, launched and confirmed two live processes (bootloader + child, ~43MB working set — consistent with a running tkinter app, not a crash) during the verification window, then swapped in.

## 5 · ROLLBACK
`nesi\_compost\NESI.exe.bak_2026-07-20_pre-bench-typetag` — move back over NESI.exe to restore the pre-session-2 window. Prior baks (07-16 through 07-19) still stand in `_compost/`.

## NEW MARKS
None surfaced by the build itself. Standing ones unchanged: necropsy burials ×3 · M5 sheet ×12 · Mark 2 fork · Mark 8 screen · engine go-live · felt-read queue read · bench type-table review.
