# NESI SEAL — cold-start freeze · 2026-07-19
**Status:** SEALED. No build, no engine, no mark crossed. Writes this seal + `nesi/mind/COLD_START.md` only.

## 1 · SETTLED GROUND (law at next cold start — do not re-derive)
- **Window is current.** NESI.exe rebuilt from source, boot-verified, swapped 2026-07-19. Rollback: `_compost\NESI.exe.bak_2026-07-19_pre-anchor-seam` (one move).
- **Anchor seam live in the window.** First HOLD asks once for an optional anchor (`until YYYY-MM-DD` / `when file:<path> exists`); blank/cancel = felt hold, old behavior exactly. Return circuit scans NESI staged holds + gate_data; writes nothing.
- **Load model is hybrid.** Code = bundled snapshot (rebuild required to ship code); data = live-from-disk. Not a fork.
- **Engine dark by choice.** Mark 1 held. Every stub loudly badged; cold-start test blocked on Mark 1 — stated, not faked.
- **Mark 2 intact.** core.py `uncross` byte-unchanged under the held fork; conductor.py's duplicate composted and out of the bundle.

## 2 · THE BOARD (carried forward)
- **Held:** Mark 1 engine · Mark 2 fork (prepared: A = raise law, zero code; B = strip, 3 files) · Mark 7 seed/ stands · Mark 8 booking-membrane (Kevin screens) · Mark 10 no cull (felt-read 17 > threshold 15).
- **Open sheets:** necropsy burials ×3 (`returns/NECROPSY_2026-07-19.md`) · M5 placements ×12 (`returns/M5_PLACEMENT_SHEET_2026-07-19.md`).

## 3 · COLD-START HEADER
Written to `nesi/mind/COLD_START.md` — §1 + §2 verbatim as settled ground, nothing added. Next session reads PROTOCOLS.md → COLD_START.md and opens on the board instead of re-litigating it.

## 4 · THE ONE OPEN LIVE-VERIFY (Kevin's hand, unautomated, whenever)
Open the window, make one hold, confirm the anchor dialog fires once and blank behaves like every hold before it. Two minutes. Recorded here, not scheduled, not automated.

---
*Sealed. The window rests current and dark — which is exactly where it should be.*
