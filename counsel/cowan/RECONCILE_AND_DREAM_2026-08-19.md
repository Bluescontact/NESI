# COWAN — ROUND ONE, THE DELTA READ

**2026-08-19, five days after the 08-14 pass. Same order: alone, blind to the other three seats.** This extends `counsel/cowan/RECONCILE_AND_DREAM_2026-08-14.md`; it does not restate it. Read against `nesi/game2d/THE_BOOT_2026-08-18.md`, `solid.js`, `seam.js`, `tools/solid_check.js`, `tools/traversal.js`, `field_kernel.js`, `ascent.html`, `tank.html`, and the `MARKS_LOG.jsonl` tail (2026-08-17 through 08-19).

## WHAT THE DELTA CONFIRMS

**1 · "A level is one edge" is the ruling my own STOP 1 was asking for, and it landed the right way.** `MARKS_LOG.jsonl` 2026-08-18 21:46: *"a Level is an edge connecting two faces, and two nodes."* This is not a new fact about the geometry — `solid.js` already returned one triangle and one square per member — it is the fact being *named as governing* rather than sitting as an unclaimed property. My STOP 1 asked whether the twelve-level ascent was the world's shape or its table of contents. The answer that landed 2026-08-19 (*"geometry wins"*, `MARKS_LOG.jsonl` 00:20) is sharper than either branch I held open: it isn't twelve of anything. It's thirty — 24 members + 6 diameters — and `ascent.html`'s ROOMS, the thing STOP 1 was actually worried about (*sequential rooms with an unlock order nothing in the vision names*), is flagged superseded in its own header, 2026-08-19. **This is a layer closing, not a theme arriving.** The act (walk a member) now produces the same response (deposit into tool+window) and the same persistence (`seamsTaken`, `deposits`) everywhere in the solid, not just at TANK. `seam.js` is the instrument: `waterSeamsFor()` reads live off `solid.js`'s own `ADJ`/`memberBetween`/`facesAlong`, so a seat this file has never named still gets the right two faces. That is gate 1's own test inverted correctly — not "could someone screenshot the optimal setup" but "does the same code path work at a seat nobody wrote a special case for," and the 2026-08-19 build pass answered it live: GROUND-DAM and GROUND-FILTER walked end to end on the generalized path, 7 more following the identical seat-agnostic code without being hand-walked. That's an anti-build law holding at scale, not just at one seat.

**2 · THE SOUNDING is fixed, and my STOP 5 is the wrong shape now.** I wrote on 08-14: *"the ledger left the walked path... nothing verbatim ever comes up."* Read `ascent.html:1804-1813` now:

> *"THE GROUND DECIDES, NOT A COIN. Until 2026-08-14 a sounding's outcome was `Math.random()<0.25`... Now the writing that settled IS the bottom: sentences lie along the width in the order they came, oldest deepest. A line dropped over settled ground finds bottom and brings ONE of them back, verbatim, unlabelled, with nothing saying why it is that one."*

And at the `sounding` seat itself (`:1758-1764`), a second die was found and removed the same way — *"This line used to read `i>=0 && Math.random() < 0.35+0.65*L.clear`, which re-introduced the exact die the corpus had just removed from THE DEEP."* Clarity now sets reach deterministically, not odds. **Two dice, found and pulled by the same discipline, five days apart, in two different mechanics that turned out to be reading the same water twice.** My library's own instrument — Dream 2, *"THE LINE TAKES THE TIME THE GROUND TAKES"* — is now half built: the verbatim surfacing is real and running; the *duration as the depth read* (you drop it, you wait, the waiting is the reading) is not — the line still draws instantly. That's the honest remainder, not a new stop. I was right that this was the sharpest gap in the ledger and wrong that it was still open; it closed the same day I found it, in a part of the file my prior pass didn't reach.

**3 · Growth is now built exactly on the instrument I named it should use, without my having named it back.** `seam.js`'s `growRoot()` — asymptotic (`rooted += (1-rooted)*0.22`), day-gated (`lastFed`), silhouette-scaled (`rootStanding()`, floor 0.16, never a number) — is *carried in*, not re-derived, from `world.html`'s shoot mechanic and a second independent build of the identical pattern in `BUILD_RECORD.md`. This is autopoiesis's own corollary working correctly at the level of code discipline: a session found the pattern had already been paid for twice and refused to pay for it a third time. It's also, quietly, an answer to part of my own Dream 6 concern about heterogeneous persistence — `growRoot()` is *per-object* state (`state.rooted`, `state.lastFed` on each seam/stone), which is the right shape for "this bank recovers, that one does not, and which is which is a property of the site." **But it does not yet decide anything about the site** — every seam grows at the identical rate 0.22, gated only by calendar day, never by anything about *which* seam it is. The mechanism that could carry heterogeneous persistence now exists; the heterogeneity itself has not been asked for. That is exactly the shape my hard limit requires me to leave alone: **a fork, not a default, and I am not naming which sites should differ.**

## WHAT THE DELTA BREAKS

**Nothing I said on 08-14 turns out to have been wrong.** That is worth saying plainly rather than manufacturing a contradiction. What breaks is scope: STOP 1's "three-way core loop" collision is resolved in a direction I didn't offer as a branch (not five-move round, not rain/shape/deep, not twelve rooms — thirty edges, three mechanics inside each). STOP 6 (word-caps vs. threshold retirement) and STOP 8 (smallest act vs. most load-bearing surface) are untouched by anything in this delta — they're still exactly where I left them, which the prestress test in `THE_BOOT_2026-08-18.md` would call HELD, not REACTIVE, so they're not stale, just unaddressed.

## WHAT OPENS NEW

**A. Eight orphan channels, named as orphans, for the first time in the record rather than silently drifting.** `MARKS_LOG.jsonl` 2026-08-19 00:20 names them outright: *rain, channel, shoal, sounding, stilling, membrane, spring, table* — real, built mechanics in `ascent.html` with no edge in `solid.js` to stand on now that geometry governs. This is the exact watershed image made literal: named tributaries with no channel bed the map recognizes. **Filter fired: nothing flows upstream, read structurally rather than metaphorically** — these eight currently drain into a superseded ROOMS system that no longer routes anything. Their fate (resite onto an edge, retire, or stand outside the 30 as a separate system) is Kevin's, stated as a fork in the boot file itself and not defaulted by the build. I hold it open the same way.

**B. `field_kernel.js` is a new latency instrument, and it does not yet talk to the other twelve I catalogued on 08-14.** DAWN_CROSSED seals a period under its own identity; REWIND_TO_DAWN undoes only the currently open one, verified adversarially against a sealed period surviving. This is genuinely a different channel than anything in my prior latency list (dam at dt/2600, seating at 44 frames, heliostat at 800ms, overwintering at a day) — it operates on *calendar dawns*, not frame-time or day-gates, and it's event-sourced rather than direct-mutation, which means replay-equals-live is now a checkable property (verified three times, per the boot note) rather than an assumed one. **Named explicitly in the boot file as "the motor, not the rails" — not wired into `tank.html`'s actual mechanics yet.** My 08-14 finding stands and gets a fourteenth entry: still no surface juxtaposes any two of these latencies against each other, and now there's one more of them, running further from the player than any of the rest.

**C. The set-down fork (STOP 2) has moved houses, not moved outcome.** `DECISIONS.md`, the file STOP 2 was sourced to, was composted entirely 2026-08-18 — but the fork itself was carried forward loose rather than bulk-migrated, and now lives as first-class prose directly inside `solid.js`'s own header (`:150-159`): *"THE QUESTION: what is a set-down's trace?... THE CANDIDATE ANSWER here: genuinely nothing... THIS PASS LEAVES IT STANDING. Kevin's, not this file's."* This is a real improvement in kind even though the ruling itself hasn't changed: the fork now lives in the file that is *the authority for every countable claim about the geometry*, not in a retired decision log a stranger would have to know to go looking for. `solid_check.js:113` asserts the candidate answer as a running test (*"the set-down has no destination and leaves no member — law 6"*). The floor test I'd apply here: a fork that only a document names is easier to quietly close by code drift than a fork a passing test enforces. This one is now enforced. Still not ruled. Both true.

**D. Every level now has two faces (tool, window) by construction, which is new ground for the edge-effect filter, not yet built on it.** With `waterSeamsFor()` reading live off `facesAlong()`, all 24 members carry the same tool/window pair TANK's four originally proved. My Dream 3 (THE CONFLUENCE — a dropped line surfacing a settled sentence from either adjoining watershed, mixing without ever naming origin) asked for content to accumulate *at a shared face*. That substrate is now everywhere in the solid rather than nowhere — every square is shared by four seats, every triangle by three — but nothing in `seam.js` or `ascent.html` yet reads a face's *contents from more than one seat's deposit* as anything other than a running total (`deposits[face]++`). The richest-content-at-edges filter has thirty times the surface it had on 08-14 and zero times the behavior.

## THE FLOOR, RUN AGAIN

Two places in this delta are doing exactly what law 20 (*"no law becomes a lever to give Kevin less"*) says a floor should do, in code rather than in prose, and I want to name them because they're the right shape: `migrateRoot()` seeds legacy data at one asymptotic step rather than zero, reasoned explicitly as *"real time has already passed... a bare 0 would understate what was already true"*; `migrateLight()` writes a `"legacy"` sentinel rather than fabricating which window a past hand meant, reasoned against inventing a fact law 23 forbids inventing. Both are migrations — the moment code most wants to quietly reset a player's standing to make its own accounting simpler — and both refuse to take anything the player already had. That is the floor's own test, passed twice, without either passage citing the floor by name. It did not need to; the discipline is the same whether or not the word is present.

What still fails the floor's test, unchanged from 08-14: the CAST door. `↑CAST`'s `emit()` carries shape, not text (`THE_BOOT_2026-08-18.md`, open item 2, verbatim). A player who walks the one member out of the water half of the world hands over writing and receives back a shape with no legible account of what departed. Dream 5 — *the cast costs the world what it gives, and the cost must be visible at the pull, before the pull completes* — is unbuilt, exactly as it was five days ago, and it remains the sharpest thing this seat can still point at: **the one irreversible act in the whole solid is also the one act whose cost is not shown at the moment of choosing it.**

## FORKS HELD OPEN (mine to hold, none defaulted)

- **The eight orphan mechanics** (rain, channel, shoal, sounding, stilling, membrane, spring, table): resite onto an edge, retire, or stand outside the 30 as their own system.
- **What departs along a non-CAST member**, and what departs along CAST itself — shape only, or shape and text.
- **Whether growth should ever be heterogeneous by site**, now that the mechanism (`growRoot`, per-object `rooted`/`lastFed`) exists and currently treats every seam identically. Building this without his word is exactly the default my own canon (autopoiesis, heterogeneous persistence) forbids.
- **The unlock order of the 30 levels** — confirmed in `THE_GROWTH.md`'s own words as not computable from the geometry, his alone, unchanged from how the boot file states it.
- **The set-down's trace** — same fork as 08-14, now living in `solid.js`'s own header and enforced by a passing test, still not ruled.

Set beside what I said on 08-14: this seat is still, structurally, a subtraction-and-reconnection instrument, and this delta is more of the same — every confirmation above is a die removed, a duplicate folded, a fork relocated to somewhere it can't drift. Nothing in five days' work asked this seat's kind of question in the direction of *adding a surface nobody has imagined*. That is still true, still worth saying before the other three seats are unblinded, and still the reason to weigh this read against theirs rather than instead of them.
