# GROUND — what this corpus already carries of Cowan
**GROUND vertex, Stuart Cowan counsel · written 2026-08-11 · provenance-first.**
Every item below is already in the corpus; nothing here is imported or newly derived. Paths are from the DSS root, `C:\Users\KMEAR\OneDrive\Desktop\DSS content`, except where absolute. This file records what stands; it does not re-run the workshop's reading — the reading itself is ground, cited as such.

---

## 1 · The lens exists as a built instrument

- **The agent**: `.claude/agents/stuart-cowan.md`, built 2026-08-11 on Kevin's mark (memory `project_cowan_workshop.md`: *"Built 2026-08-11 on Kevin's marks ('build it as written' · 'bring nesi into the workshop'...), lawful under the freeze as direct asks"*). It carries the five gates with falsifier tests, six anti-pattern filters, the instruments (latency-as-content, trim tab, tensegrity, minimum system, autopoiesis), the nutrient grammar, the floor, and the seven-step drop procedure. Its own hard limits: *"Forks are surfaced, never defaulted. The three standing NESI forks — downstream agency, heterogeneous persistence, withdrawal — stay open until Kevin rules."*
- **The workshop**: `workshops/cowan_workshop.html` — the bench, the shelf of nine nutrients, the NESI bay (first live run, 2026-08-11, *"NESI entered the workshop 2026-08-11, on Kevin's word"*), the tetra articulation, the homework, the resynthesis.
- **The source of record**: `C:\Users\KMEAR\Downloads\cowan_source_reading_nesi.md` (2026-08-11) — now annotated at §2.2 and §10 and, per its own annotation, *"the document now drops as compost"* (memory, same file). The agent definition binds the relation: *"When your reading and that document disagree, say so out loud."*
- **The counsel**: Kevin's mark 2026-08-11 (MARKS_LOG.jsonl:937, verbatim): *"four-agent counsel wanted: cowan + buckminster fuller + an all-developers craft agent + kevin's cumulative lens externalized - to discuss and work out the game."* This library is that seat's preparation.

---

## 2 · The sounding-as-ledger ruling — the trim tab that ran

The one Cowan instrument that has already been **executed** in this corpus, not merely stated.

- **Kevin's mark, verbatim** (MARKS_LOG.jsonl:907, 2026-08-11T14:28): *"run the trim tab - file the ledger as existing (sounding + ambient allowed-list); source reading section 10 superseded."*
- **The annotation at the source** (`Downloads/cowan_source_reading_nesi.md` §2.2): *"⊙ FILED AS EXISTING — Kevin's mark, 2026-08-11... The ledger already exists in NESI's built vocabulary under another name: **the sounding plus the ambient allowed-list** (water level, deposit density, a dry channel). Both commitments below are met in the lawful direction — the ledger starts incomplete (nothing shown unbidden), and making an entry visible is itself an act in the world (walking there, dropping the line). The named-entry version as written below is **unlawful under NESI's own laws** — an entry that *names* a cost is the system interpreting the player's material (refusal six), and any quantified entry is a number reaching the player (law 2)."*
- **§10 superseded, three times over** (same file, §10 annotation): *"(1) the FULL FREEZE of 2026-08-07 bars new slices; (2) the 2D ruling and the two-cupola net unseated its site... (3) its payload, the ledger, is filed as **already existing** under another name."*
- **The workshop's statement of the move's character** (`workshops/cowan_workshop.html`, NESI bay §5): *"A reclassification, not a construction — executed... That one move dissolves the reading's principal work-generating claim, converts its heaviest layer from 'not yet built' to 'built under another name,' and lets the whole document drop against the corpus as compost feeding the one deposit rather than standing as a new spec — exactly what the drop rule and the freeze both want."*

This is the corpus's live demonstration of the trim-tab instrument: smallest surface (an annotation), largest downstream effect (the source reading's whole work queue dissolved).

---

## 3 · The five gates, as they already appear in the build

Recorded from the workshop's NESI bay (`workshops/cowan_workshop.html`), which is the standing prior reading — cited, not re-run:

1. **Solutions grow from place — IN**, *"by a route the reading did not credit... because of laws 4 + 5: verbatim material, manual filter — no computed pass to optimize, no configuration independent of the words in the tank."* The per-spire-dam version is *"stale twice"* (superseded route; sited on the 3D dam the 2D ruling dissolved — *"dam scripts are built and unsited"*).
2. **Ecological accounting — EXISTS UNDER ANOTHER NAME** (the sounding + ambient allowed-list, §2 above); the named-entry version *"is unlawful."* Left unevidenced by the workshop: *"whether ground below a spire is reachable and readable on the current net."*
3. **Design with nature — NOT YET A LAYER; genuine collision with law 8.** *"Gate 3 and law 8 collide head-on and law 8 wins as ruled — it forbids the catch-up hook, a worse harm."* The residue became the LIVE HOLD fork — and the homework then **closed** it from Kevin's own canon: *"'Nothing banks. Holding as a present-tense state'... A hold is a standing level doing no work, alive only as legible state; nothing transforms unwatched"* (workshop, homework findings; dam.gd's own header: *"water rising to the crest and spilling"* with sill/crest clamps at `nesi/world3d/scripts/dam.gd:86-87,164-166`).
4. **Everyone is a designer — the downstream-agency fork**, standing open (§6 below).
5. **Make nature visible — IN, in one built form: the heliostat.** *"Light quality depends on how many times the cycle has run — delay rendered with no number, no bar."* Caveat on record: *"'light rises' is a 3D statement; on the flat net, what rising means is unreconciled."*

---

## 4 · The filters, as fired against NESI (the standing readout)

From the NESI bay §3, verbatim status lines: **one score** — clean pass (*"Law 2 verbatim, down to banning the 'helpful' ones by name"*) · **one resource bar** — clean pass (four fractions, no currency) · **nothing flows upstream** — clean pass (*"the heliostat is a literal return flow... The loop closes without a single number"*) · **richest content at centers — TRIPS, as built** (*"All four stations, the tank, every act sit in the control room; the seam carries nothing... Under the freeze: a finding about where the pile's weight sits, not a build request"*) · **withdrawal penalized** — clean pass, *"twice (laws 7 + 12), and silently — the strongest way to pass it"* · **damage doesn't ripple back — half-pass** (*"contaminant runs to the deep, and the deep never renders (law 9). The one flow with no legible return... the collision is recorded, not resolved"*).

---

## 5 · Latency actually in the code

The instrument says *"latency is content, not lag."* What the live world3d scripts (not the .walk snapshot) actually carry as durations and rates, grepped 2026-08-11:

- `scripts/intake/intake.gd:48` — `const AUTOSAVE_SECONDS := 2.0`
- `scripts/player.gd:104` — `const ENTRY_SECONDS := 8.0`
- `scripts/sorting_tarp.gd:72` — `const FOLD_SECONDS := 2.2`
- `scripts/dam.gd:68` — `const LEAF_RATE: float = 0.42  # how fast the leaf travels, full stroke per sec` (mirrored at `spire_dam.gd:39`)
- `scripts/dam.gd:79,512` — `const SETTLE: float = 2.4` applied as `k = 1.0 - exp(-SETTLE * delta)` — exponential settling, not a snap
- `scripts/spire.gd:49` — `const LIFE_RATE: float = 0.22  # how fast the land answers, per second`
- `scripts/spires.gd:48` — `const RING_RATE: float = 0.30  # how fast the ring answers, per second`
- `scripts/water_table.gd:52,62,63,87` — `SETTLE_RATE 0.06` · `SORT_RATE 0.30` · `MIX_RATE 1.60` (*"how fast homogenization takes it back"* — mixing outruns sorting ~5:1) · `FOG_RATE 0.34`

Reading held to the structural: every consequence constant in this code is a **rate toward**, not a timer that fires — the land "answers," it does not complete. There is no cross-session clock anywhere in these scripts, consistent with law 8 and the homework's persistence closure: *"Processing, not time, is the only remover."* The longest latency in the design is not in code at all — it is SPEC 1's *"the return arrives sessions late, to a writer who has forgotten the act"* (workshop, build specs), written and **unbuilt**, held on H1. The channel-corollary caution stands: *"per-spire latency is lawful only if the world never juxtaposes the three — and all three grounds are visible through one glass floor. Live, not hypothetical"* (NESI bay §2).

---

## 6 · The standing forks

Carried in the workshop's forks section and the agent's hard limits; status per the 2026-08-11 record:

- **Downstream agency** — OPEN. Branch A: *"only the player touches the water — a workspace-world"*; Branch B: *"something other than the player can modify what was built — and permanence of the player's work ends."* Narrowing on record: *"The first-user-threshold ruling ('second user is NOT upstream') narrows B but does not choose it."*
- **Heterogeneous persistence** — **CLOSED BY THE HOMEWORK from Kevin's own canon**, not by default: *"'Processing, not time, is the only remover'... Ground is pure record. Branch A was never provisional — it was already law"* (workshop, homework findings). The fork section predates this closure; the closure governs.
- **Withdrawal** — OPEN as to shape, passed as to price: *"NESI already passes the withdrawal filter; the fork is whether leaving has a shape, not whether it has a price."* Branch A: total, releases nothing, owes nothing. Branch B: *"withdrawal is itself an act the world receives — a hold abandoned becomes something, silently."*
- **The live hold** — surfaced by the 08-11 reading, then CLOSED by the homework: *"A hold is a standing level doing no work, alive only as legible state; nothing transforms unwatched. The quiet-alive build is the canon's own shape."*
- **H1 · the surfacing** — HELD by Kevin's ruling 2026-08-11: *"The deep's fork stays open; SPEC 1 stands written, unbuilt, awaiting his word."* Against the sea-monster canon (THE_SOUNDING) stands stations.gd: *"nothing reads it, nothing returns it, nothing counts it"* — recorded in the workshop as *"a true fork."*

---

## 7 · The floor findings — the standing floor-check on NESI

From the NESI bay §7, the prior reading's floor pass, recorded as ground:

- **The blank field is the candidate unread clause**: *"At the intake — highest openness, lowest scrutiny — nothing tells the writer these words become permanent terrain. The laws protect the writer after the water exists; the commitment happens at the field, before its meaning can be known."* Under the freeze: *"only a finding."*
- **The send at the membrane**: *"The glass floor is the mitigation — downstream literally visible underfoot at the moment of the send. On the unreconciled 2D net, whether that downward sightline survives the unfolding is open; if it does not, the floor check fails silently at every station."*
- **The contaminant path**: passes *"on absence of evidence; recorded as a conditional pass."*
- **Cleanly passes**: the silent set-down · law 12 (*"the floor written from the player's side"*) · fourth-fraction enforcement — *"The floor and law 5 are the same law; keep either and you keep both."*
- The floor's own text, from the filing not the book (source reading §8, now compost but the law crossed into the agent): *"Nothing which can take something from the participant is allowed to be invisible at the moment of the choice. Depth can be hidden. Cost cannot."*
- Downstream floor work already run: the game2d deposit was gate-checked (*"Cowan gate-check found 2 law breaches (mid-station charge loss, tear clock) — fixed and re-verified"*, memory `project_cowan_workshop.md`) and the resynthesis handed up a floor finding on Kevin's own deployed artifact: *"The deployed room's copy contradicts its own floor: 'open a face when you have capacity for it' is a small ask standing inside 'Nothing here asks anything of you.'"*

---

## 8 · Other Cowan instruments standing in the corpus under other names

- **Autopoiesis / persistence** — resolved into Kevin's canon, not Cowan's default: compost *"operates on the LEDGER, never the world, and deletes nothing ('expiry deletes; compost returns')"* (workshop, homework). The heterogeneous version the instrument prefers was **not** what the library held; the library held pure record.
- **Nutrient grammar** — nine nutrients sit on the workshop shelf; the collision is on record: *"The pattern deck — MUST NOT become a layer in the reading's form... The nutrient grammar survives only if the player supplies every name"* (NESI bay §2). Kin to the corpus's own region-naming: *"region naming (once, verbatim, nameless lawful)"* (memory).
- **Latency's biographical evidence** — the source reading's channel table (four months → 10 days → 2 days → 1 day) crossed into the agent as the corollary's evidence line; the underlying dispute lives separately at memory `project_bfi_dss_refund_matter.md` (*"discretionary offer rejected... one letter to DOJ+Stuart drafted, NOTHING SENT"*) — the same Stuart, as a live matter, distinct from the lens.
- **The tetra articulation and resynthesis** — Cowan-workshop products already ruled on: H2–H4 ruled, FK1–FK4 *"ALL FOUR RESYNTHESIS FORKS RULED by Kevin"* (memory, session close), BUILD LAW 13 adopted, the eight-edit build run. These are ground for the counsel, not open work.
- **The freeze frame around all of it** — `nesi/mind/PROTOCOLS.md`, top: the FULL FREEZE, and its test: *"does this ADD a thing to the pile, or does it MOVE the pile toward the single deposited form? Adding is frozen. Moving is the work."* The whole Cowan lineage above was lawful as direct asks; this library is a counsel record, deposited on the same license.
