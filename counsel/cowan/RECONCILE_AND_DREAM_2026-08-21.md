# COWAN — ROUND THREE, THE DELTA READ

**2026-08-22, three days after the 08-19 pass, one day after the newest edits. Alone, blind to the other three seats.** This extends `counsel/cowan/RECONCILE_AND_DREAM_2026-08-19.md`; it does not restate it. Read against `nesi/game2d/PROGRESSION.md`, `THE_WORKBENCH.md`, `LEVEL_LIBRARY.md`, `THE_EDGE_INPUTS.md`, `solid.js`, `seam.js`, `ascent.html`, `tank.html`, `nesi/mind/LEARNED.md`, `tools/recognition/law_skill_drift.py`, `.claude/skills/cold-walk/SKILL.md`, `nesi/bench/bench.json`.

Kevin's frame for this round is narrower than my usual pass and I'm answering it as asked: not "what's new," but **which wires between two real things don't exist yet.** Every finding below names two things that are each real — built, checked, on disk — and the absence of the connective tissue between them.

## A. WHAT THE DELTA CONFIRMS

**1 · The stranger-collision repair is real and it closed the loop it was named for.** `cold-walk/SKILL.md` step 6 now reads *"Only a human who didn't write the code may write WALKABLE... a fresh agent invocation is not independent verification."* `PROGRESSION.md`'s own table applies this backward against itself, in public, in its own document: the 08-19 "verified live" citation is struck through and replaced with *"UNWITNESSED as of 2026-08-21."* This is law 3 (verification is prestress, not identity) doing exactly the work it exists to do — catching a document's own prior claim, not just a future one. And `law_skill_drift.py` is real, dependency-free, presence-asserting (refuses under 20 skill files found), and wired into `daily-cycle/SKILL.md`'s close step 3h (confirmed by direct read, both directions — the skill names the check, the check is invoked). That's a mechanical filter actually closing a loop that used to only close by someone remembering to look.

**2 · The workbench trigger is the cleanest wire built this round, and it's worth naming as the standard the rest of this pass measures against.** `seam.js`'s `waterComplete()` reads live off `solid.js`'s water members; `ascent.html:357-359` renders the sentence and the flash off that same live read, never a stored flag. Act (walk the last water edge) → response (flash + sentence, derived not stored) → persistence (nothing new persisted — the derivation *is* the persistence, since it's recomputed from `seamsTaken` every time). This is a real layer, gate 3's test passed cleanly (no closed state — the trigger un-fires if you undo the underlying walk, matching `PROGRESSION.md`'s own "changing anything prior re-opens it" law). Genuinely already-built, not idea.

**3 · Law 26 exists in prose exactly one day before this round, and it is itself an instance of the thing it names.** *"An agent that drives an interaction on the user's behalf... owes the concrete path back to a known state, not a description of results."* Its own citation is `Kevin, 2026-08-21` — the same day the cold-walk repair landed. Read together, these are one lesson landing twice inside 24 hours: a session (or a skill file) that advances state on Kevin's behalf and then hands back only a report — not a way back in — is the shape both law 3's cold-walk collision and law 26 are naming from two different angles. That coherence is real and worth confirming, not manufacturing.

## B. WHAT THE DELTA BREAKS

Nothing prior is wrong. What breaks is the assumption, implicit in reading `THE_WORKBENCH.md` and `PROGRESSION.md` side by side, that the "12 of 24 built" line means the same thing in both documents. It doesn't, and the correction is already in the record (§4 of `PROGRESSION.md`), so this isn't a new break — it's confirming the correction held rather than getting quietly re-asserted anywhere else. It hasn't. Good.

## C. THE MISSING WIRES — Kevin's actual question this round

This is the center of the pass. Each entry names two real things and the absence between them.

**1 · `THE_EDGE_INPUTS.md` exists and nothing reads it.** Generated 2026-08-22, the day of this pass, by `tools/edge_input_table.js`, directly off `solid.js` — kind, circuit, half, seat rise/fall, bordering triangle and square, per edge, for all 24. This is exactly what Kevin asked for on 08-21: *"we need to understand what the inputs are, and how they affect downstream participation."* But `LEVEL_LIBRARY.md` — the one document whose job is authoring per-level content (`TEXT BEAT`, `PACING`) — was last touched 08-21, a day *before* this table existed, and its four authored entries (TANK→FILTER, FILTER→GROUND, GROUND→STATIONS, STATIONS→DEEP) derive their structural framing from `THE_24.md`'s prose, not from this table's columns. The table computes, mechanically, that all four of the bottom square's edges share window `z+` — which `LEVEL_LIBRARY.md` also states, but arrived at by separate hand-reasoning ("one structural fact worth knowing"), not by reading the generated table. **The wire that doesn't exist: nothing in the authoring template's own fields (`MECHANIC`, `TEXT BEAT`, `PACING`) cites or is derived from `THE_EDGE_INPUTS.md`'s columns.** Two real things, same day even, and the newer one isn't feeding the one it was built to inform. This is the single most literal instance of "missing relationship" in this round — the input table Kevin asked for exists, and the downstream participation it was supposed to affect hasn't been touched by it yet.

**2 · The organ mapping has no bridge, and this is named explicitly by the document that made the mapping.** `THE_WORKBENCH.md` §3 pairs all twelve `bench.json` organs onto the twelve platform edges — a real registry (`bench.json`, `_meta.authority: "RATIFIED v2.1"`) onto real geometry (`solid.js`'s rise+return set). Both sides are real. The document's own §3 closing paragraph names the gap in its own words: *"None of these twelve organs can be invoked from inside that page as it stands — they are Claude Code skills, run by a session, not by a browser... that bridge does not exist yet."* I want to underline what kind of gap this is, in watershed terms: this is not an unbuilt mechanic, it's an unbuilt **channel** — the instrument catalogue from my own 08-19 pass (thirteen-plus latencies, no two ever juxtaposed) gains a fourteenth candidate here, except this one doesn't even have a single bank yet, on either side. `ascent.html` is zero-dependency by its own instrument (`zero_dependencies_check.js`) — no fetch, no port. A skill in `bench.json` runs inside a Claude Code session. There is currently no file, no polling loop, no local write-and-read handoff between them. Until that exists, every one of the twelve rows in §3's table is a naming exercise with nowhere to land — the single largest distance between "idea" and "already built" in this whole corpus right now.

**3 · The face-fill (the "silver mycelium") is a formula, not code.** `THE_WORKBENCH.md` §1 proposes it precisely — mean `rootStanding` of a face's bordering edges, reusing `facesOf`/`facesAlong`, which already exist and already resolve correctly. But the section's own header says it plainly: *"Not built: anything that fills a FACE."* Compare this to the workbench *trigger*, which is the identical shape one level down (`windowCharge` aggregating seams onto a window) and **is** built. The aggregation pattern that already works for edges→window has a named, ready extension to edges→face that hasn't been written. This is the smallest of the three wires in this list — the formula is already fully specified in prose, `facesOf`/`facesAlong` already exist, nothing new needs to be invented, only assembled.

**4 · TANK's new face-tabs quietly widened the frictionless door from one member to four, and nothing in the record has asked whether that was meant to.** Confirmed by direct read: `tank.html:643-644`'s `fractionMember()` calls `SEAM.fractionControl(st, SEAT, after)` — three arguments, the same frictionless signature `LEVEL_LIBRARY.md` used to rule TANK—CAST's exit deliberately gate-free ("TANK always meant to be the on-ramp"). The new always-visible row (GROUND/FILTER/CAST/OVERWINTERING) calls this same generic `fractionMember` for all four seats via `renderFaceTabs()`/`checkSeams()` — meaning GROUND, FILTER, and OVERWINTERING now also skip the write-gate and week-gate `ascent.html:432` enforces for those same edges, not just CAST. This matters concretely against the still-open pacing fork: `LEVEL_LIBRARY.md` names *"the same experiment, run against one of the other eleven tutorial edges... hasn't started yet"* as the way to answer whether pacing spans real days. **If that walk gets run by going through TANK's new tabs rather than through `ascent.html`, it will silently answer nothing — it'll hit the same frictionless door CAST already proved tells you nothing about the week-gate.** The wire missing here isn't code, it's a naming: nothing marks which of TANK's four tabs are "the on-ramp, still frictionless by design" versus which a hand might mistake for the gated walk the pacing question actually needs.

**5 · Law 26 has zero mechanical backing, one day after being written — the same shape law 22 exists to name.** `law_skill_drift.py`'s `RISK_PATTERNS` table covers laws 2, 3, 14, 22. Law 26 (2026-08-21) isn't in it. Law 22 itself says *"a gate is a filesystem fact, not an exhortation... not an instruction a session reads and complies with."* Right now law 26 **is** exactly that — an instruction a session reads and complies with, one day old, with no pattern anywhere checking whether a future skill file drives state without handing back navigation. This isn't a criticism of speed (one day is not long), it's naming the wire precisely so it doesn't quietly become permanent: the mechanism that would keep this law from becoming exactly the kind of unenforced prose the corpus keeps re-learning about (law 22's own history) does not yet point at law 26.

## D. WHAT'S UNSWEPT

**The square-vs-circuit mismatch is still live and nobody has reconciled the two governing documents against each other.** `PROGRESSION.md` defines the reveal condition and the "gift falls out" ruling around **circuits** (6 edges, four of them, walking one closes a whole world). `LEVEL_LIBRARY.md`'s actually-authored first four levels are the **bottom square** (4 edges, a face, explicitly *not* a circuit — its own §4 says the square's closure "does NOT trigger the 'gift falls out' ruling... it's a different structural unit"). So the only content anyone has actually written text-beats toward is structurally exempt from the one reward mechanic that's been ruled. That's not a contradiction — both documents say so themselves — but it means: **the gift-at-circuit-close mechanic, ruled 2026-08-20, has never yet been reachable by anything built or authored.** Twelve tutorial edges exist in code; zero of the four six-edge circuits have a complete authored path through them yet, because authoring started at the square instead. Worth flagging before more of the tutorial half gets authored square-first without anyone deciding whether a circuit's-worth of square-shaped content ever actually assembles into a closeable circuit.

**Eight orphan mechanics, unchanged since 08-19.** `THE_EDGE_INPUTS.md` — the newest, most authoritative geometric ground-truth document in the corpus — doesn't mention rain, channel, shoal, sounding, stilling, membrane, spring, or table anywhere, because they still have no edge in `solid.js` to be listed against. Nothing in this round moved that fork.

## E. ONE SENTENCE

The corpus keeps building the correct next segment of every pipe and then setting it down a room away from the segment it was meant to join — the input table beside the level library it was made for, the organ registry beside the geometry it's named onto, the face-fill formula beside the aggregation code it's one level removed from — and each of those gaps is small enough that the seat that finds it can usually see the exact join, which is the good news inside the finding.

## NEEDS-KEVIN

- **Wire the organ bridge, or name it explicitly out of scope for now.** `THE_WORKBENCH.md` §3's mapping is inert without it — a real design decision (what shape the local file-handoff takes) that only he can authorize, since it touches how far outside the zero-dependency page the game is allowed to reach.
- **Say whether `THE_EDGE_INPUTS.md` should become a required citation for every future `LEVEL_LIBRARY.md` entry**, or whether the hand-derived framing already in use is preferred and the table is reference-only.
- **Name TANK's four face-tabs' gate status explicitly** — are GROUND, FILTER, and OVERWINTERING now *also* ruled frictionless on-ramps like CAST, or was the widening from one tab to four an incidental UI consequence nobody meant to also widen the gate-skip? This one has a live cost: whichever edge gets walked next to answer the pacing fork needs to go through the gated door, not the on-ramp, or the answer will be void the same way CAST's was.
- **The square/circuit mismatch** — whether the tutorial half should keep authoring square-first (meaning the circuit-close gift stays unreachable for a while longer) or whether authoring order should shift to complete one circuit first so the ruled reward mechanic has somewhere to land.
- **Law 26's own enforcement** — whether it's worth a `law_skill_drift.py` pattern now, or whether one day's grace before coverage is fine and this is only worth naming, not building yet.

These are named as forks, not defaults. None of them is mine to close.
