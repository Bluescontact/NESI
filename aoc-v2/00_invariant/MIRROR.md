# MIRROR — this Invariant has a compiled twin

**Layered 2026-08-31 (crystal-7 resolution — a fact lives in one place; a
copy is a drift waiting to happen).** The `INVARIANT.md` and `GLOSSARY.md` in
this folder are the **source of truth for doctrine** — the manuscript copies
of *The Architecture of Coherence v2.0*. A compiled, locked deployment
rendering of both lives at `coherence-codex/` (see its `VERSION` file, which
declares itself `Compiled-from` this manuscript and carries the reciprocal
half of this declaration).

**The rule, same as the corpus's other declared mirrors (`nesi/mind/ROUTER.md`
convention):** if the two copies ever disagree on substance, this manuscript
copy wins, and the disagreement is a **drift event to surface, never to
silently resolve**. Formatting may differ; doctrine may not. The codex's
read-only-after-lock status governs its own deployment stability, not the
doctrine's evolution — a doctrinal change happens here first, then propagates
by an Invariant version bump per the codex's own lock terms.

Verified content-equivalent 2026-08-31: the register-names canonicity clause
appears as prose here and as the "(canonical)" table marker there; the codex
copy additionally carries a version-bump clause this copy does not.
